# Buenos Aires Program — Student App

## Overview

A mobile-first web app for Pepperdine University's Buenos Aires Program students. It provides quick access to class schedules, the semester calendar, announcements, health providers, local churches, recommended apps, and FAQ entries. The app reads all content from a Google Sheet, so the Program Director can update information without touching code. A localStorage stale-while-revalidate cache makes repeat opens essentially instant.

Live URL: https://baprogram.vercel.app/

## Architecture

```
Google Sheet (source of truth)
        ↓
   Apps Script Web App ←→ CacheService (1-hour TTL)
        ↓                  (or fallback: 15 parallel gviz CSV fetches)
   fetchAllData()  ←→  localStorage cache (stale-while-revalidate)
        ↓
   React app (Vite build)
        ↓
   Vercel (hosting)
```

The app is a single-page React application built with Vite. On every open it reads localStorage synchronously during first render to display cached content instantly, then kicks off a background fetch in the background. When the fetch returns, it silently swaps in fresh data and updates the cache. If the fetch fails, the cached data stays on screen; if no cache exists yet (first-ever open), the app falls back to hardcoded default data embedded in the code.

The background fetch has two paths. By default it hits a single Apps Script Web App endpoint that returns all 15 sheet tabs as one JSON blob; the script caches its response for 1 hour via `CacheService`, so most opens never re-read the spreadsheet. If that endpoint is unreachable, returns a non-200, or returns non-JSON (e.g. a Google login page when deploy permissions are wrong), the app silently falls back to the legacy path: 15 parallel gviz CSV fetches, one per tab. Both paths feed the same `normalizeData()` function, so the rendered output is identical.

## Key Files

| File | Location in Repo | Purpose |
|------|------------------|---------|
| `App.jsx` | `src/App.jsx` | The entire app; all components, data fetching, cache layer, brand tokens, and default data |
| `PlacesMap.jsx` | `src/PlacesMap.jsx` | The Places map view (Leaflet + CARTO Positron). Deliberately its own file so `App.jsx`'s `lazy(() => import("./PlacesMap.jsx"))` code-splits Leaflet into a separate chunk that loads only when a student first opens the map (and is excluded from the SW precache). Online-only |
| `vite.config.js` | Root | Vite build config; includes the `vite-plugin-pwa` setup that generates the service worker. `workbox.globIgnores` keeps the lazy `PlacesMap-*` chunk out of the precache |
| `index.html` | Root | Entry point; includes preconnect hints, font stylesheet, PWA manifest, apple-touch-icon, and favicon links. The plugin auto-injects the service worker registration here at build time |
| `manifest.json` | `public/` | PWA configuration (app name, theme color, icons) |
| `apple-touch-icon.png` | `public/` | iOS home screen icon (180×180, BAP logo on Pep Blue background) |
| `icon-192.png` | `public/` | PWA icon (192×192, BAP logo on Pep Blue background) |
| `icon-512.png` | `public/` | PWA icon (512×512, BAP logo on Pep Blue background) |
| `favicon.ico` | `public/` | Browser tab icon (contains 16×16 and 32×32) |
| `favicon-32x32.png` | `public/` | Browser tab icon (32×32) |
| `favicon-16x16.png` | `public/` | Browser tab icon (16×16) |

The `index.html` `<head>` includes these performance and asset tags:

```html
<link rel="preconnect" href="https://docs.google.com" crossorigin>
<link rel="preconnect" href="https://fonts.googleapis.com" crossorigin>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400&family=EB+Garamond:wght@400;700&family=Roboto:wght@400;500;700&display=swap">
<link rel="icon" type="image/x-icon" href="/favicon.ico" />
<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
<link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
```

The preconnect hints start TLS handshakes to Google's servers before the JS bundle parses, shaving 100–300ms off cold loads. The font stylesheet is loaded in parallel with the bundle rather than waiting for React to mount and inject it. `App.jsx` also injects the font link via `useEffect` as a safety net; the browser dedupes duplicate `<link>` tags, so this is harmless.

## Google Sheet

**Sheet ID:** `1Bn1wpsKr6-3eXRZtH-_6IxmTiQA4I157-nt-0tdmyaA`

**Requirements for the sheet to work:**

- Must be **published to the web** (File > Share > Publish to web > Entire Document > CSV)
- Must be **shared publicly** (Share button > General access > "Anyone with the link" > Viewer)
- Both steps are required; publishing alone is not enough

**How the app fetches data:**

The app has two fetch paths, controlled by the `APPS_SCRIPT_URL` constant in `App.jsx`. The consolidated path is preferred; the per-tab path is the fallback.

**Consolidated path (preferred).** When `APPS_SCRIPT_URL` is set, the app makes a single GET request to a deployed Apps Script Web App that returns every tab as one JSON blob. The script reads the spreadsheet via `SpreadsheetApp` and caches its response for 1 hour using Apps Script `CacheService`, so most opens hit the script's in-memory cache instead of re-reading the sheet. This is the path students actually take 99% of the time. See "Apps Script Endpoint" below for deploy details.

**Per-tab fallback path.** If `APPS_SCRIPT_URL` is empty, the consolidated request fails outright (network error, non-200), or the response isn't valid JSON (e.g. an HTML login page from a misconfigured deploy), the app silently falls back to fetching each tab individually using:

```
https://docs.google.com/spreadsheets/d/{SHEET_ID}/gviz/tq?tqx=out:csv&sheet={TAB_NAME}
```

It fetches all 15 tabs in parallel using `Promise.all`, parses each as CSV with PapaParse, and feeds them through the same normalization function the consolidated path uses. Slower, but it has no script-deploy dependency, so it's the safety net.

Both paths funnel into a shared `normalizeData(raw)` helper that turns the per-tab arrays into the app's data shape, so the UI behavior is identical regardless of which path served the data.

### Tab Structure

Column headers must be **lowercase** and **exactly as shown**. Tab names are case-sensitive.

**Settings**

| Key | Value |
|-----|-------|
| semester | Summer 2026 |
| last_updated | 2026-04-28 |
| finals_window_start | 2026-08-29 |
| finals_window_end | 2026-08-29 |

The `finals_window_start` and `finals_window_end` keys define the program-wide final-exam window (typically a single day for Summer terms; 2-3 days for Fall and Spring). The app uses the window in two places: it gates when the Finals UI surfaces (the FinalsCard on the Schedule tab and the Finals coming up tile on Today both appear 14 days before `finals_window_start`), and it provides the "TBD · {window}" copy on per-class rows whose individual `final_date` hasn't been assigned yet. Both keys must be `YYYY-MM-DD`. Leaving them blank disables the finals UI entirely.

**Classes**

| Column | Example | Required |
|--------|---------|----------|
| code | SPAN 350 | Yes |
| title | Advanced Conversation | Yes |
| professor | Castro | Yes |
| honorific | Prof. | No |
| firstname | María | No |
| days | Mon, Tue, Wed, Thu | Yes |
| time | 9:00–10:50 | Yes |
| location | Cuyo | Yes |
| color | #00205B | Yes |
| email | mcastro@pepperdine.edu | No |
| start_date | 2026-05-11 | No |
| end_date | 2026-08-28 | No |
| final_date | 2026-08-29 | No |
| final_time | 9:00–11:00 | No |

Notes on the `time` field: Classes with variable schedules across days use this format: `Mon 16:30–19:30; Tue 13:40–17:15; Thu 17:10–19:30`. The app parses this and shows only the relevant time slot for each day in the week view. Simple times like `9:00–10:50` are shown as-is on all days.

Notes on `days`: Accepts two formats. Either comma-separated three-letter abbreviations (`Mon, Tue, Wed, Thu, Fri`) or a single concatenated string of single-letter day codes (`MTWR` = Mon/Tue/Wed/Thu; `R` stands for Thursday to avoid collision with Tuesday). The `parseDays()` helper detects which format is in use.

Notes on `honorific`, `firstname`, `email`: Used in the "All Courses" view. When `honorific` and `firstname` are both present, the professor line reads "Prof. María Castro"; otherwise it falls back to the bare surname. When `email` is present, a tappable "Email Prof. Castro" button appears on the card.

Notes on `color`: Hex color code used for the left accent bar on class cards. The current scheme uses three colors to distinguish class groups:

- **GE classes:** Sky `#6CACE4` (ARTH 251, HIST 270, HUM 200, INTS 350, NUTR 210, NUTR 210 Lab, PE 193, SPAN 121, SPAN 251)
- **UD classes:** Pepperdine Blue `#00205B` (SPAN 350, SPAN 380, SPAN 440)
- **Combined:** Pepperdine Orange `#E35205` (HUM 295/296)

An earlier scheme used Ocean (`#0057B8`) for GE and Pep Blue (`#00205B`) for UD, but the two blues were hard to distinguish as thin border accents. Sky provides much stronger contrast against the dark navy. Full brand palette available in the `Branding_Guidelines` project knowledge file.

Notes on `start_date` and `end_date` (added 2026-04-28): Both `YYYY-MM-DD`, both optional. They bracket the regular meeting period for the class. Outside `[start_date, end_date]` (inclusive) the class is suppressed from the Today tab's activity card and the Schedule tab's Weekly Overview. The Class Schedule (Mon–Fri grid) and Courses (alphabetical list) sub-views always show the full catalog regardless. Either value blank means "no gate on that side." Implementation lives in `isClassActive(c, dateStr)` and `filterActiveClassesForDate(classes, dateStr)`. Set `end_date` to the last regular session, not the final exam date.

Notes on `final_date` and `final_time` (added 2026-04-28): `final_date` is `YYYY-MM-DD`; `final_time` is a free-form time-range string like `9:00–11:00`. Both optional and typically left blank at the start of the term, then filled in row-by-row as the registrar publishes the schedule. Effects: (a) the Today tab's `<TodayFinalsTile>` and the Schedule tab's `<FinalsCard>` switch from "TBD · {finals window}" to the concrete date+time for that row; (b) on the day itself, `getTodayItems` replaces the regular class meeting on Today's activity card with a "Final" entry (Pep Orange pill via `kind: "final"`); (c) `WeeklyOverviewView` does the same swap on the matching day card with a Pep Orange highlighted card. Implementation: `getStudentFinals`, `getFinalForDate`.

**Calendar**

| Column | Example | Required |
|--------|---------|----------|
| date | 2026-05-11 | Yes |
| title | UD Arrival Day | Yes |
| type | milestone | Yes |
| description | UD students arrive at EZE | Yes |
| end_date | 2026-05-13 | No |
| start_time | 10:00 | No |
| end_time | 13:00 | No |
| visibility | both | No |

The `type` field must be exactly one of: `program`, `academic`, `excursion`, `holiday`, `orientation`, `mundial`. Each type has a distinct color and icon in the app:

- **program**: Parchment-orange background, Pep Orange border, ★ (the most prominent treatment; flags program-office-curated entries like arrival days, asados, faculty visits)
- **academic**: Ice background, Ocean border, ◆
- **excursion**: Pale green background, forest green border, ▲
- **holiday**: Pale pink background, red border, ●
- **orientation**: Ice background, Sky border, ⬟
- **mundial**: Championship-gold background (`#FFF8E1`), amber border (`#F9A825`), ⚽ (added 2026-06-01 for World Cup games; the ⚽ soccer ball is prefixed to the event title — the only event type that renders its glyph inline, all others are distinguished by bg/border alone). **Argentina game-day treatment (added 2026-06-11):** when a `mundial` event is dated *today* and its title contains "Argentina", the Today tab additionally puts on an albiceleste skin — a celeste-forward greeting strip with a Sol de Mayo and drifting papelitos, plus a `<MundialGameTile>` hero with jersey stripes, a number-10 watermark, and a live kickoff countdown from `start_time`. Detection is `getArgentinaGameForDate` (a title-match); non-Argentina mundial games are unaffected. See the 2026-06-11 changelog row and the Today-tab feature notes.

The `milestone` type that previously carried the Pep Orange treatment was retired on 2026-05-11b; its visual was reassigned to `program` because the two had drifted into operational overlap (both surfaced Director-curated date entries and students never reliably distinguished them when filtering). Any legacy `milestone` rows still in the sheet fall through `EVENT_STYLES.academic` and render as Academic cards until the Director relabels them to `program`.

Date format must be `YYYY-MM-DD`. If using Google Sheets date cells, ensure they export in this format (plain text cells are safest).

The optional `end_date` column supports multi-day events. When present (and later than `date`), the event displays as a single card with a date range label (e.g., "May 22–25") and a day count (e.g., "3 days") rather than duplicating across multiple days. The `end_date` value is normalized to the first 10 characters to handle timestamp artifacts from CSV export.

The optional `start_time` and `end_time` columns (24-hour format, e.g. `10:00`, `13:00`) display a time range under the event title on the Weekly Overview and are used to sort events within a single day. Events without a `start_time` appear first for that day (treated as all-day or flexible items), followed by timed events in chronological order.

The optional `visibility` column controls where the event appears:

- `both` (default if blank) — appears in both the Semester Calendar and the Weekly Overview
- `week` — appears only in the Weekly Overview (e.g., routine weekly items like the Friday asado that would clutter the semester view)
- `semester` — appears only in the Semester Calendar (e.g., semester-level milestones that shouldn't appear on a given week)

**Birthdays**

| Column | Example | Required |
|--------|---------|----------|
| name | María García | Yes |
| date | 05-12 (or 5-12, or 2003-05-12) | Yes |
| role | student, staff, faculty | No |

This tab is optional. When present, the Today tab renders a celebratory birthday card on any day where one or more rows match today's MM-DD. The card sits between the announcement banner and the holiday card.

The `date` column is parsed via `parseBirthdayMD()` and accepts three formats: `MM-DD` (e.g., `05-12`), `M-D` (e.g., `5-12`), or full `YYYY-MM-DD` (e.g., `2003-05-12`). When a full date is provided, the year is intentionally stripped during parsing. **The app never displays or computes age.** This is a deliberate privacy choice — students share their birthday, not their age.

The `role` column accepts `student`, `staff`, `faculty` (or any free-form value) but is captured-but-unused in v1. Reserved for future filtering or visual differentiation.

**Privacy guidance:** Only add students who've affirmatively opted in. The card displays publicly to all app users. A simple consent checkbox during orientation works well — "Would you like the program to surface your birthday on the app?" Staff and faculty are typically lower-stakes since the audience is a closed cohort, but their preference still rules.

**UI tiers based on count:**
- **1 person:** "¡Feliz cumple, María!"
- **2 people:** "¡Feliz cumple a María y Carlos!"
- **3+ people:** "¡Feliz cumple!" header, with names listed in a single comma-joined line beneath

The card is Spanish-only by design — the names themselves carry the bicultural feeling, and the Spanish reads warmer without an English echo underneath.

**Feb 29 birthdays:** In non-leap years, anyone born on February 29 won't be matched. The program office can manually adjust their date row to `02-28` or `03-01` for that calendar year as preferred.

**Holidays**

| Column | Example | Required |
|--------|---------|----------|
| date | 2026-05-25 | Yes |
| name_es | Día de la Revolución de Mayo | Yes (or name_en) |
| name_en | May Revolution Day | Yes (or name_es) |
| cancels_classes | TRUE | Yes |
| observance_type | national | No |
| description_es | Aniversario de la Revolución de Mayo de 1810... | No |
| description_en | Anniversary of the May Revolution of 1810... | No |

This tab is optional but recommended. When present, it becomes the source of truth for which days suppress classes and what the Today holiday card displays. When absent, the app falls back to calendar events tagged `type: "holiday"` (legacy behavior; treats every holiday-typed event as class-cancelling).

The `cancels_classes` flag is parsed via `parseBoolean()` which accepts `TRUE`, `true`, `yes`, `y`, `1`, `x`, `✓`, `sí`, `si` as truthy and anything else as falsy. National feriados, Semana Santa, and días no laborables con fines turísticos should be `TRUE`; cultural observances like Día del Maestro, Día del Estudiante, Día de la Tradición, and Día Internacional de la Mujer should be `FALSE`.

The `observance_type` column is free-form but values like `national`, `religious`, `cultural`, and `provincial` are conventional. Reserved for future filtering work; the app currently doesn't read it.

The Today holiday card renders bilingual content: Spanish title and description primary, English in italic EB Garamond underneath. Class-cancelling holidays get a red feriado treatment (`#FCE4EC` bg, `#C62828` stripe, "Feriado / Holiday" label); cultural observances get a quieter Ocean treatment (`C.ice` bg, `C.ocean` stripe, "Día especial / Cultural day" label). Weekly Overview day cards inherit the same visual distinction in a more compact form.

**Announcements**

| Column | Example | Required |
|--------|---------|----------|
| message | Add/Drop deadline is Friday | Yes |
| type | info | No |
| start_date | 2026-05-15 | Yes |
| end_date | 2026-05-18 | Yes |
| link | https://example.com/form | No |

This tab is optional. The app fetches it with a try/catch wrapper, so the app will not break if the tab does not exist.

The `type` field accepts `info` or `urgent` (defaults to `info` if blank):
- **Info**: Ice Blue background, BAP Blue accent stripe, `<MegaphoneIcon>` glyph, "Aviso / Notice" label.
- **Urgent**: Parchment background, Pep Orange accent stripe, `<AlertIcon>` triangle glyph, "Importante / Important" label, plus a small Pep Orange pulsing dot pinned to the right of the label (`.bap-pulse-dot-orange`).

Announcements are filtered client-side: only rows where today falls within the `start_date`/`end_date` range (inclusive) are shown. Multiple active announcements stack vertically on the Today tab between the quick-stats row and today's activity card.

Per the 2026-04-26 redesign, announcements are not user-dismissible: they auto-clear once the `end_date` passes, so the program office controls the lifecycle entirely from the sheet. Each banner shows a bilingual DM Mono label, the message body in 14 px Roboto, an italic "Hasta el viernes" / "Hasta el 4 de mayo" Spanish gloss when the announcement runs ≤21 days (so students know when it'll auto-disappear), and a "Más info →" CTA pill in the accent color when `link` is set. The previous `icon` column is no longer read; the type-driven glyph replaces it.

**Health**

| Column | Example | Required |
|--------|---------|----------|
| name | Clínica Zabala (Swiss Medical) | Yes |
| type | Hospital/Clinic | No |
| address | Av. Cabildo 1295, Belgrano | No |
| phone | +54 11 5236-8500 | No |
| notes | 24hr emergency; Swiss Medical Group | No |
| link | https://www.swissmedical.com.ar/clinewsite/zabala/ | No |
| insurance | bcbs | No |
| category | facility | No |
| location_note | 24/7 Telehealth Therapy | No |

The `link` column is optional per row. If present, the app shows a tappable button. Link types auto-detected:

- `https://wa.me/...` → shows "WhatsApp" button
- `https://...instagram.com/...` → shows "Instagram" button
- Any other URL → shows "Visit website" button

WhatsApp link format: `https://wa.me/` followed by phone number with country code, no spaces or punctuation. Example: `+54 9 11 4419-7092` becomes `https://wa.me/5491144197092`.

The optional `insurance` column, when set to `bcbs` (case-insensitive), shows a small BCBS/GeoBlue logo next to the provider name to signal in-network status for students on the Pepperdine GeoBlue plan.

The optional `category` column overrides the automatic facility-vs-person detection. Valid values are `facility` or `person`. When a row is a facility (either by category or by type match against `hospital|clinic|clínica|sanatorio|laboratory|lab|pharmacy|farmacia|emergency|isos|imaging|diagnóstico`), the card gets an Ice-blue background to visually separate it from individual practitioners. Use `category: person` to force a named-facility row like "Dr. Smith Clinic" to render as a person, or `category: facility` to force a row whose type isn't in the pattern list.

The optional `location_note` column is for providers whose "location" isn't a real street address: telehealth services ("24/7 Telehealth Therapy"), home-visit practitioners ("Casa (on-site)"), or providers where the location line carries reference data ("Member Num: 11BCAS525378") instead of a physical address. Unlike `address`, it renders as plain italic gray text with no 📍 pin and no Google Maps link. If a row has both `address` and `location_note`, the address appears first (linked), then the note below as unlinked context.

**Churches**

| Column | Example |
|--------|---------|
| name | Saddleback Buenos Aires |
| denomination | Non-denom. |
| address | Mario Bravo 559 |
| location_note | Meets in rotating member homes |
| service | 11AM, 5PM, 7PM (Spanish & English) |
| notes | 35 mins by subte/bus |
| link | https://saddleback.com/visit/locations/buenos-aires |

Same `link` behavior as Health tab. The `location_note` column follows the same rules as on the Health tab: plain italic gray text, no pin, no map link; used for churches whose gathering location isn't a single fixed street address.

**Explore**

| Column | Example |
|--------|---------|
| name | MALBA |
| type | Museum |
| description | Premier Latin American art museum |
| address | Av. Figueroa Alcorta 3415, Palermo |
| location_note | Multiple locations citywide |
| hours | Thu–Mon 12–8pm |
| link | https://www.malba.org.ar |

Displayed as the third sub-tab under Local. Addresses are rendered as clickable Google Maps links (see `AddressLink` below). The `location_note` column follows the same rules as on Health and Churches: plain italic gray text, no pin, no map link; useful for entries like neighborhoods or city-wide attractions where a single street address would be misleading.

**Contacts**

| Column | Example |
|--------|---------|
| name | Buenos Aires Program |
| role | Program Office |
| phone | +5491151561793 |
| whatsapp | https://wa.me/5491151561793 |
| email | buenosaires@pepperdine.edu |
| address | 11 de Septiembre de 1888 955, CABA |
| maps | https://maps.app.goo.gl/HQt8A6ZQABrhL7rG7 |
| type | office |

The `type` column controls how the contact is grouped and styled:
- `office` — the Program Office card at the top (shows address, Call, Open in Maps, and Email buttons)
- `emergency` — amber-accented emergency card (shows Call and WhatsApp buttons)
- `staff` — individual staff cards (shows Call, WhatsApp, Email buttons; address is not rendered here)

The `maps` column is optional. When present on an `office` row, its URL is used for the address link; otherwise a Google Maps URL is auto-generated from the `address` field.

**Resources**

| Column | Example |
|--------|---------|
| name | U.S. Embassy Buenos Aires |
| detail | Av. Colombia 4300, Palermo |
| phone | +54 11 5777-4533 |
| url | https://ar.usembassy.gov/ |

Optional tab. Displayed in the Contacts view under the "Additional Resources" header, below the Local Emergency Numbers block. Each row renders as a card with a Call button (when `phone` is set) and a Website button (when `url` is set). The app wraps the fetch in a try/catch so a missing Resources tab will not break data loading.

**FAQ**

| Column | Example |
|--------|---------|
| title | Independent Travel |
| content | Students may travel independently on weekends… |
| link | https://example.com/handbook/travel-policy |

FAQ entries display as expandable accordion cards. If a `link` is present, a "View full details" button appears below the summary text. The button renders as `display: flex` with `width: fit-content` so it always starts on its own line. The sheet tab was previously called "Policies"; the columns and behavior are unchanged.

**Apps**

| Column | Example | Required |
|--------|---------|----------|
| name | Google Maps | Yes |
| category | Navigation | No |
| description | Maps, transit, and walking directions. | No |
| ios_url | https://apps.apple.com/app/google-maps/id585027354 | No |
| android_url | https://play.google.com/store/apps/details?id=com.google.android.apps.maps | No |
| web_url | https://dolarhoy.com | No |
| priority | essential | No |

Optional tab. **As of 2026-06-10j the Apps sub-view was removed from the Local tab UI (see "Removed / dormant features" below); this tab is no longer surfaced anywhere in the app.** The data plumbing is intact, though — the tab is still listed in `Code.gs`'s `TABS`, still fetched, and still parsed into `data.apps` by `normalizeData` — so the schema below remains accurate and the feature can be resurrected without a sheet or Apps Script change. The app wraps the fetch in a try/catch so a missing Apps tab will not break data loading.

Filter pills above the list filter by `category` (e.g., Navigation, Transportation, Food & Delivery, Finance, Comms). Pills only appear when there is more than one unique category.

Apps are sorted with `priority: essential` first, then alphabetically by name. Essential apps get a subtle Ice-blue card background and a small Pep Orange dot next to the name to visually cluster them.

Each card shows name, category badge, description, and up to three tappable buttons. The render logic: if `ios_url` is set, show a "📱 iOS" button; if `android_url` is set, show a "🤖 Android" button. If neither store URL is set but `web_url` is set, show a "🌐 Website" button as a fallback. `web_url` is ignored when a store URL is present.

The `priority` value is case-insensitive; only `essential` triggers the visual treatment. Any other value (e.g., `recommended`, or blank) renders as a regular card.

**Tips**

| Column | Example | Required |
|--------|---------|----------|
| text | *Dale* is yes, no, sure, ok, and "let's go," all at once. | Yes |
| category | phrases | No |

Optional tab. Powers the rotating tip card on the Loading screen (the only screen students see when they open the app for the first time, before any cache exists). The app wraps the fetch in a try/catch so a missing Tips tab will not break data loading.

The `text` column supports markdown-style asterisk emphasis: any text wrapped in single asterisks (e.g., `*subte*`, `*che*`, `*re bueno*`) renders as italic EB Garamond inline. This is parsed by the `renderTip()` helper using a simple split on `*`; there is no HTML evaluation, so the field is XSS-safe. Use this for Spanish words, place names, or any phrase that benefits from a small typographic emphasis. Plain text without asterisks renders as-is.

The `category` column is purely for the Director's organizational use (e.g., `transit`, `food`, `phrases`, `culture`, `city`). The app does not currently filter by category; it cycles randomly-then-sequentially through the full list, advancing every 4 seconds with a 320ms cross-fade.

Tips are kept short (rule of thumb: under 20 words) so they're readable in a single glance during the loading interval. When the Tips tab is empty or missing, the LoadingScreen falls back to a small built-in set of three tips so the first-ever load is never blank.

**Events**

| Column | Example | Required |
|--------|---------|----------|
| title | Lulu Sandri en Niceto | Yes |
| category | music | Yes |
| description | Argentine alt-folk; doors at 21:00 | Yes |
| start_date | 2026-04-26 | Yes |
| end_date | 2026-04-30 | No |
| time | 21:00 | No |
| venue | Niceto Club | No |
| neighborhood | Palermo | No |
| address | Av. Niceto Vega 5510 | No |
| link | https://nicetoclub.com/... | No |
| cost | $8.000 ARS | No |

Optional tab. Powers the "This Week" sub-tab in Local (the leftmost sub-tab) and the "Esta semana / This Week in BA" tile on Today. Updated weekly by the Director; no rebuild needed.

The `category` field must match one of: `music`, `theater`, `film`, `exhibit`, `dance`, `festival`, `food`, `talk`, `service`, or `other`. Anything else falls through to `other`. Each category carries a distinct color and an SVG glyph rendered inside a 44 px colored circle on the event card:

- **music**: Pep Orange, music-note glyph
- **theater**: Pep Blue, theater-mask glyph
- **film**: Mountain, film-reel glyph
- **exhibit**: Sky, picture-frame glyph
- **dance**: Pep Orange, tango-shoe glyph
- **festival**: Pep Orange, sparkle glyph
- **food**: Ocean, fork-and-plate glyph
- **talk**: Mountain, microphone glyph
- **service**: BAP Blue, hands-cradling-heart glyph (volunteering, service learning, giving back to the community)
- **other**: Stone, pin glyph

`start_date` and `end_date` use `YYYY-MM-DD`. `end_date` is optional and only used for multi-day runs (festivals, exhibitions); single-day events leave it blank. The app auto-hides events whose last day is before today, so old entries stay in the sheet without cluttering the app — useful for archiving past picks. Events are sorted chronologically (by `start_date`, then by `time` when same-day; untimed entries lead the day).

Within the sub-tab, events are split into two groups: "This week / Esta semana" (today through +7 days) and "Coming up / Próximamente" (anything later). Filter pills above the list let students narrow by category, and only appear when 2+ categories are present in the data. When no events are upcoming at all, the sub-tab shows a friendly "Nothing curated yet" empty state pointing to Explore BA for evergreen recommendations.

`link` accepts any URL (website, Instagram profile, ticket page). The app renders it as a tappable "Open Link →" button using the existing `<LinkButton>` helper. `address` is rendered through `<AddressLink>` and opens Google Maps. `cost` is a free-text field; conventions like "Free", "$8.000 ARS", "$15 USD", "Suggested $5.000" all render fine.

## Apps Script Endpoint

The app reads from two Apps Script Web Apps, each bound to its own spreadsheet for permission isolation: this section covers the **content endpoint** (class schedules, calendar, contacts, etc.); the parallel **Roster Auth Endpoint** (see next section) handles per-user identification. Splitting the two means the content script literally has no read access to the Roster, and the auth script literally has no read access to the content sheet, so a bug in either can't leak data from the other.

The content Apps Script Web App reads every tab the app needs from the content spreadsheet and returns one JSON blob keyed by tab name (each tab is an array of header-keyed row objects). As of 2026-05-03 it is also the cohort-level auth boundary: every request must carry a `?token=` query parameter matching the `COHORT_TOKEN` entry in Script Properties, or the script returns `{ error: "unauthorized" }` with no data.

**Deployed URL location.** Stored in `App.jsx` as the `APPS_SCRIPT_URL` constant in the CONFIGURATION section.

**Source.** The Apps Script lives bound to the spreadsheet (Extensions > Apps Script from the sheet's menu). The single source file is `Code.gs`, also kept in the repo root as the canonical reference. The repo copy and the script editor have to be kept in sync manually after any edit.

**Caching.** The script caches its JSON response for 1 hour via `CacheService.getScriptCache()` under the key `bap_app_data_v1`. Sheet edits therefore take up to 1 hour to appear via this endpoint. Append `?bust=1` to the Web App URL (alongside `&token=…`) when manually verifying an edit; that query param skips the cache for one request and re-reads the spreadsheet. The Today pull-to-refresh gesture also threads `?bust=1` through `fetchAllData({ token, bust: true })`. The cache key is intentionally versioned (`_v1`); bump the suffix in `Code.gs` if the script's output shape ever changes incompatibly.

**Manual cache reset.** Run the `clearCache()` function from the Apps Script editor to drop the cached value without bumping the key.

**Deploy procedure (first-time):**

1. Open the spreadsheet, then Extensions > Apps Script.
2. Paste in the contents of `Code.gs`. Save.
3. Project Settings (gear icon, left sidebar) > Script Properties > Add property: `COHORT_TOKEN` = the cohort passcode (e.g., `asado-summer-26`).
4. Run `testReadAllTabs()` once from the editor to authorize the spreadsheet read scope and confirm tab counts in the execution log.
5. Deploy > New deployment > Web app. Execute as: Me. Who has access: Anyone.
6. Copy the deployed URL into `App.jsx` as `APPS_SCRIPT_URL`. Commit, push, Vercel rebuilds.

**Re-deploy after script edits.** Apps Script Web Apps freeze at the version they were deployed at. Editing the script doesn't affect production until a new deployment is created (Deploy > Manage deployments > pencil icon > Version: New version). The URL stays the same across re-deployments of an existing deployment slot, which is what you want.

**Cohort token rotation.** Edit the `COHORT_TOKEN` Script Property and save. **No re-deploy is needed for token rotation** — Script Properties are read at request time, not at deploy time. The next student fetch will fail with AuthError, the app will clear the stale token from localStorage automatically, and the gate will re-prompt for the new code. Combine the rotation with a WhatsApp-group announcement of the new code so students know what to type.

**Failure mode.** When the script is reachable but rejects auth, the app returns to the passcode gate with a "Wrong code" message. When the script is unreachable (network error, deploy expired), `App.jsx` keeps cached content on screen and downgrades the status pill to "Saved version (offline)" — same behavior as before. There is no longer a fallback path; the gviz CSV approach was removed on 2026-05-03 because it was the only remaining auth-free door into the data.

## Roster Auth Endpoint

A second Apps Script Web App, bound to a separate "BAP App Roster" spreadsheet, handles per-user identification AND per-student data collection (prompts + responses, see "Prompts and Responses" below). Same shape as the content endpoint (Apps Script returning JSON over a Web App URL with `?token=…`), different concern (validates a CWID + birthday against the Roster tab, returns the matched user's curated row, serves prompt definitions, accepts submissions back).

**Deployed URL location.** Stored in `App.jsx` as the `AUTH_SCRIPT_URL` constant in the CONFIGURATION section, alongside `APPS_SCRIPT_URL`.

**Source.** `AuthCode.gs`, kept in the repo root as the canonical reference. Lives bound to the Roster spreadsheet (Extensions > Apps Script from the Roster sheet's menu). The repo copy and the script editor have to be kept in sync manually after any edit, same convention as `Code.gs`.

**Endpoint shapes:**

> **As of 2026-06-10d the four read actions below (`identify`, `prompts`, `admin_responses`, `admin_places`) are called by the app via POST `text/plain` (JSON body with the same `action`/`token`/`cwid`/`birthday` keys), so per-user credentials stay out of the URL/exec log. The server's `doPost` routes them to the same handlers; `doGet` still accepts them for backward compatibility with older cached builds. The GET shapes below remain accurate for the params each takes — only the verb changed. `places` (token-only, no PII) is still GET.**

```
GET|POST action=identify&token=<cohort>&cwid=<cwid>&birthday=<MM-DD>
  → { user: { cwid, first_name, last_name, preferred_name, pronouns,
              role, email, whatsapp, housing_assignment, tshirt_size,
              tshirt_fit, dietary_restrictions, food_allergies,
              program_status } }
  → { error: "unauthorized" }   // bad cohort token
  → { error: "no_match" }       // cwid not found, birthday mismatch, or program_status not active
  → { error: "bad_request" }    // missing/unparseable params

GET ?action=prompts&token=<cohort>&cwid=<cwid>&birthday=<MM-DD>
  → { prompts: [ {
        prompt_id, title_es, title_en, description_es, description_en,
        category, surface, start_date, end_date,
        fields: [ { field_id, field_order, label_es, label_en,
                    field_type, options[], option_labels_es[],
                    option_labels_en[], required, placeholder_es,
                    placeholder_en } ],
        responses: { field_id: storedValue, ... },   // pre-filled
        submitted_at: "2026-05-09T...Z"               // most recent
      } ] }
  → { error: "unauthorized" | "no_match" | "bad_request" }

POST  (Content-Type: text/plain;charset=utf-8, JSON body)
  { action: "submit", token, cwid, birthday,
    prompt_id, fields: { field_id: value, ... } }
  → { prompt: { ... }, ... }    // refreshed prompt after upsert
  → { error: "validation_failed", details: "missing_field:fid" | "bad_value:fid" }
  → { error: "not_found" | "audience_mismatch" | "prompt_inactive" | "lock_failed" }
  → { error: "unauthorized" | "no_match" | "bad_request" }

GET ?action=admin_responses&token=<cohort>&cwid=<cwid>&birthday=<MM-DD>
  → { prompts: [ {
        prompt_id, title_es, title_en, description_es, description_en,
        category, surface, audience, start_date, end_date, end_time,
        is_active,                            // computed server-side
        fields: [ ...same shape as ?action=prompts... ],
        expected_cwids: [ "123456789", ... ], // active-roster CWIDs
                                              // whose audience matches
        responses: [ { cwid, field_id, value, submitted_at }, ... ]
      } ],
      roster:  { "<cwid>": { preferred_name, first_name, last_name, role } } }
  → { error: "forbidden" }    // requester's role isn't staff
  → { error: "unauthorized" | "no_match" | "bad_request" }

POST  (Content-Type: text/plain;charset=utf-8, JSON body)   // student place submission
  { action: "submit_place", token, cwid, birthday,
    fields: { name, category, address, maps_url, why } }
  → { ok: true }                                  // landed as a pending/community row
  → { error: "validation_failed",
      details: "missing_field:name" | "bad_value:category" | "missing_location" }
  → { error: "lock_failed" }
  → { error: "unauthorized" | "no_match" | "bad_request" }

GET ?action=admin_places&token=<cohort>&cwid=<cwid>&birthday=<MM-DD>   // staff-only
  → { places: [ {
        place_id, name, category, why, address, lat, lng, maps_url,
        neighborhood, hours, link, status, submitted_by_cwid,
        submitted_by_name, show_credit, source, submitted_at, vetted_by
      }, ... ] }                                   // ALL statuses, for the vetting queue
  → { error: "forbidden" }    // requester's role isn't staff
  → { error: "unauthorized" | "no_match" | "bad_request" }

POST  (Content-Type: text/plain;charset=utf-8, JSON body)   // staff vet action
  { action: "vet_place", token, cwid, birthday,
    place_id, status: "approved" | "rejected", show_credit? }
  → { ok: true }                                  // flips status, sets show_credit + vetted_by
  → { error: "not_found" | "lock_failed" }
  → { error: "forbidden" }    // requester's role isn't staff
  → { error: "unauthorized" | "no_match" | "bad_request" }
```

The `birthday` field is intentionally omitted from the identify response — the student already knows it; echoing back is a small leak surface for nothing. Tier-C fields (medical notes, passport, emergency contacts) are deliberately not in the Roster sheet at all and not in `CURATED_FIELDS`; they belong to Pepperdine's existing intake systems, not to this app.

**POST shape rationale.** Submit is POST (not GET) because (a) write actions belong on POST, (b) multi-field payloads are awkward in URL params and can hit length limits, and (c) `text/plain` content-type avoids the CORS preflight that `application/json` would trigger — Apps Script Web Apps don't implement `doOptions`, so preflight requests fail. The script parses `e.postData.contents` as JSON internally.

**Cohort token shared with the content script.** The auth script reads `COHORT_TOKEN` from its own Script Properties; the value should match the content script's `COHORT_TOKEN`. Rotation is two 30-second copies — once per Script Property. **No re-deploy is needed for token rotation** (Script Properties are read at request time).

**Lookup logic.** `findRosterRow(cwid)` reads the Roster tab via `getDataRange().getValues()`, finds the row whose `cwid` column matches after `normalizeCwid()`, and returns the row as a header-keyed object. `normalizeCwid()` strips non-digits and leading zeros, so input variations (`"0123456789"`, `"123-456-789"`, `" 123456789 "`) all collapse to the same canonical form. CWIDs are conventionally 9-digit numerics with no leading zeros, but the lookup is lenient on input variation; `validateRoster()` flags non-conforming rows in the editor.

**Birthday matching.** `parseBirthdayMD()` mirrors `App.jsx`'s parser: accepts `MM-DD`, `M-D`, or full `YYYY-MM-DD`, returns canonical `MM-DD`. Both sides of the comparison are canonicalized before matching, so a sheet row entered as `2005-05-12` still matches a front-end submission of `05-12`.

**`program_status` gate.** If the matched row's `program_status` is anything other than `active` (case-insensitive), the script returns `no_match` instead of the user record. Blank is treated as `active`. This means a student who has `program_status: withdrawn` can't sign back in mid-cohort; the Director controls re-enabling them by editing the cell.

**No response caching at the script layer.** Identify happens roughly once per device per cohort, and prompts/submit need to reflect Director edits and student writes immediately, so neither uses `CacheService`. Direct sheet reads are negligible at cohort scale. (The front end has its own per-cwid 10-min `bap-prompts-cache` so repeat opens render instantly without re-fetching.)

**Editor helpers in `AuthCode.gs`:**

- `testReadRoster()` — run once after pasting the script in to trigger the OAuth scope prompt. Logs the row count and headers to the execution log.
- `validateRoster()` — flag duplicate CWIDs (raw and normalized), missing required fields, unrecognized roles (anything not in `student | staff | faculty`), malformed birthdays, malformed emails, CWIDs containing non-digit characters, CWIDs that aren't exactly 9 digits, and CWIDs with leading zeros. Run anytime you've meaningfully edited the Roster.
- `validatePrompts()` — flag duplicate `prompt_id` values in `Prompts`, fields referencing an unknown `prompt_id`, duplicate `(prompt_id, field_id)` pairs in `PromptFields`, missing or unrecognized `field_type` values, select fields with no options, mismatched `option_labels_es` / `option_labels_en` counts, malformed `start_date` / `end_date` / `end_time` / `event_date`, end-before-start windows, `event_date` before `start_date`, `event_date` before `end_date` (the submission cutoff would land after the event date — almost certainly a Director typo since the kitchen / vendor needs the tally before service), prompts with no fields defined, unrecognized `surface` values (anything other than `today` / `profile` / `both`), unparseable `lock_after_submit` booleans, and audience tokens that aren't `all` / a role / a CWID-shaped string. Run after any meaningful edit to the Prompts or PromptFields tabs.

**Deploy procedure (first-time):**

1. Open the Roster spreadsheet, then Extensions > Apps Script.
2. Paste in the contents of `AuthCode.gs`. Save.
3. Project Settings > Script Properties > Add property: `COHORT_TOKEN` = the same value used in the content script's properties.
4. Run `testReadRoster()` once from the editor to authorize the spreadsheet read scope.
5. Run `validateRoster()` to confirm the seeded data has no warnings.
6. Deploy > New deployment > Web app. Execute as: Me. Who has access: **Anyone** (not "Anyone within Pepperdine University" — that requires a `@pepperdine.edu` Google sign-in in the calling browser, which breaks for students using Safari without a signed-in Workspace session).
7. Copy the URL into `App.jsx` as `AUTH_SCRIPT_URL`. Commit, push, Vercel rebuilds.

**Re-deploy after script edits.** Same convention as the content script: Apps Script Web Apps freeze at the version they were deployed at. Editing `AuthCode.gs` doesn't affect production until a new deployment is created (Deploy > Manage deployments > pencil icon > Version: New version). The URL stays the same across re-deployments.

## Cohort Auth

The cohort passcode is the single credential gating the app. Same string serves three roles: (1) the API token the Apps Script validates on every request, (2) the localStorage entry the app reads to decide whether to render the gate or the main UI, and (3) the code the program office hands out at orientation in the WhatsApp group.

**How auth flows on a cold open.** The Apps Script reads the expected token from Script Properties at request time. The app reads the user's token from localStorage at mount; if it's missing, `<PasscodeGate>` renders in place of the main UI. The gate's submit handler fires a probe `fetchAllData({ token: candidate })` against the Apps Script. On success the resolved data is handed up via `onAuth(token, data)`, which saves the token to localStorage, primes `data`/`status`/the localStorage cache, and dismounts the gate; the post-auth render's data-fetch effect short-circuits via the `justAuthed` ref so the probe's payload isn't immediately re-fetched. On failure the gate distinguishes auth failure (bilingual "Código incorrecto / Wrong code", input clears, focus returns) from network failure ("Couldn't connect. Try again.") so a wrong code doesn't read as a flaky network and vice versa.

**How auth flows on background fetches.** Every `fetchAllData()` call threads the token from `cohortToken` state. If the script returns `{ error: "unauthorized" }` (e.g., the cohort code was rotated since the student last opened the app), the App's data-fetch effect catches the AuthError, clears the bad token from localStorage via `clearCohortToken()`, and sets `cohortToken` to empty — which triggers an early-return back to the gate on the next render. Same handling applies in `refreshAllData()` (the pull-to-refresh path).

**Storage shape:**

As of 2026-05-09b the token lives in a JSON envelope at the localStorage key `bap-cohort-token`:

```json
{ "token": "asado-summer-26", "savedAt": 1715000000000 }
```

`loadCohortToken()` checks `Date.now() - savedAt` against `AUTH_TTL_MS` (60 days) and treats older values as missing — so a student who hasn't opened the app for two months gets re-prompted at the cohort gate even if their browser hasn't evicted localStorage. Backward compatibility: the legacy plain-string format (anything stored before this commit) is still accepted as valid on load; the next save upgrades it to the envelope, after which the TTL kicks in. Profile and data cache live at separate keys (`bap-profile`, `bap-app-cache`), so wiping the cohort token doesn't disturb a student's enrolled-classes selections or their cached content.

**Helpers in `App.jsx`:**

- `class AuthError extends Error` — thrown by `fetchAllData()` when the script rejects the token. Lets the App-level catch distinguish auth failure from generic fetch failure without string-matching error messages.
- `loadCohortToken()` / `saveCohortToken(token)` / `clearCohortToken()` — three small wrappers around `localStorage.getItem/setItem/removeItem` for the `bap-cohort-token` key, each try/catch-wrapped for environments with disabled storage.
- `<PasscodeGate onAuth />` — bilingual full-screen gate component. EB Garamond title pair ("Buenos Aires Program" / "Programa de Buenos Aires"), DM Mono "Código de acceso / Access code" label above a single text input, "Continuar / Continue" button, italic helper line at the bottom ("¿No tenés el código? Pedíselo al equipo del programa"). Probes the Apps Script with the entered token; on success calls `onAuth(token, data)` with the resolved data. Differentiates wrong-code from network-error in the inline error treatment (Pep Orange–accented rejection panel for both, but distinct copy). Reuses the App's `SouthernCrossDecoration` and `LOGO_URI` so the visual identity matches the header. Auto-focuses the input on mount.

**What the gate does not do.** It doesn't gate by user identity (everyone in the cohort uses the same code). It doesn't sync across devices — each device gets its own gate experience until the code is entered there too. It doesn't survive a browser "Clear site data" action; in that case the student re-enters the code on next open, same as the first open. iOS Safari can evict localStorage from installed PWAs after ~7 days of non-use, so a long-quiet student may be re-prompted occasionally; this is iOS behavior, not a bug.

**Threat model.** The gate is calibrated to "keep the URL out of search engines and out of strangers' hands while the cohort is in session." It is not a defense against device theft (anyone with the unlocked phone can read localStorage and the cached app), against a student deliberately sharing the code outside the cohort (which would be an honor-code matter, not a technical one), or against a determined actor with network-monitoring tools (the token is sent in the URL query; on the wire it's TLS-encrypted, but it's visible to anyone who shares the device's network at the OS level). The data the app surfaces is mostly low-stakes (class schedules, public healthcare addresses, opt-in birthdays without years), so this calibration is appropriate; if the data ever grew more sensitive the calibration should be revisited.

## User Auth

The cohort passcode is layer 1 ("are you in this cohort?"); per-user identification via CWID + birthday is layer 2 ("which person in the cohort are you?"). Layer 1 is a security boundary; layer 2 is identification, not authentication — the cohort passcode is doing the security lifting, and the user gate is calibrated to "tell us which row in the Roster you are" so future features can be person-specific (RSVPs, t-shirt sizes, role-gated views, dinner menu choices, and so on).

**How auth flows on a cold open.** After `<PasscodeGate>` succeeds and the cohort token is stashed, `<App>` early-returns `<UserGate>` if `currentUser` is null. The gate's submit handler fires `identifyUser({ token: cohortToken, cwid, birthday })` against the Roster Auth Endpoint. On success the curated user record is handed up via `onAuth(user, birthday)`, which augments the record with the entered birthday and saves it to localStorage at `bap-user`, then dismounts the gate; the main UI renders immediately with content data already primed by the cohort gate's probe fetch. The birthday is included in the stored envelope so the prompts/submit endpoints can re-validate identity on every call without re-prompting (see "Prompts and Responses" below); without it, every prompts call would force a re-entry of CWID + birthday. On `NoMatchError` the gate shows a bilingual "We couldn't find you. Verify CWID and birthday" panel and the fields stay populated for editing. On `AuthError` (the cohort token was rotated mid-session, between the cohort gate succeeding and the user gate submitting), the gate calls `onCohortReset` which clears the cohort token; the next render bounces to `<PasscodeGate>` instead of showing a misleading wrong-credentials message in the user gate. On any other error the gate shows "Couldn't connect."

**How auth flows on background fetches.** No identify call happens on background fetches — once `currentUser` is in localStorage it persists until cleared (sign out, AuthError on a content fetch, or manual site-data clear). The data-fetch effect's `AuthError` handler now clears `currentUser` alongside the cohort token, so a rotated cohort code resets both gates in lockstep rather than letting a stale identity from a previous cohort silently persist across the rotation.

**Storage shape:**

As of 2026-05-09c the curated user record is wrapped in a JSON envelope at the localStorage key `bap-user` and now also carries the birthday the student typed at the user gate (so prompts/submit can re-validate without re-prompting):

```json
{
  "value": {
    "cwid": "123456789",
    "birthday": "05-12",
    "first_name": "María",
    "last_name": "García",
    "preferred_name": "Mari",
    "pronouns": "she/her",
    "role": "student",
    "email": "mgarcia@pepperdine.edu",
    "whatsapp": "+5491144197092",
    "housing_assignment": "Homestay – Recoleta (Familia López)",
    "tshirt_size": "M",
    "tshirt_fit": "women's",
    "dietary_restrictions": "vegetarian",
    "food_allergies": "",
    "program_status": "active"
  },
  "savedAt": 1715000000000
}
```

`loadUser()` checks `Date.now() - savedAt` against `AUTH_TTL_MS` (60 days, shared with the cohort token) and returns `null` for older values, so a quiet student gets re-prompted at the user gate. **Birthday is required on-device:** records without it (anything saved before 2026-05-09c, including the legacy flat format) are treated as missing on load, forcing a one-time re-prompt at the user gate; the next save includes the birthday and the record persists normally. The legacy flat format (no envelope) is also still accepted at load time when it carries both `cwid` and `birthday`. No version field on the stored user; the auth script's `CURATED_FIELDS` projection plus the birthday field is the source of truth for shape. If a field is later removed from `CURATED_FIELDS`, old localStorage entries still load fine — extra fields are ignored at read time.

**Threat-model note on storing birthday.** The original 2026-05-09 design intentionally omitted birthday from localStorage so that an attacker with cohort-token + device access still couldn't re-authenticate without separately knowing the student's birthday. The 2026-05-09c expansion to enable prompts/submit accepts a small weakening of that posture: birthday now lives alongside cwid in the envelope, so a determined attacker with localStorage access can act as the student against the auth script. The trade is justified by the prompts feature's value (no re-prompting per submit), and is consistent with the threat model already articulated for the user gate ("identification, not authentication; calibrated to low-stakes data within the cohort").

**Helpers in `App.jsx`:**

- `class NoMatchError extends Error` — thrown by `identifyUser()` when the auth script returns `{ error: "no_match" }`. Distinct from `AuthError` so the gate can show the right message for the right failure (wrong-credentials vs. stale-cohort-token).
- `loadUser()` / `saveUser(user)` / `clearUser()` — three small wrappers around `localStorage.getItem/setItem/removeItem` for the `bap-user` key, each try/catch-wrapped. `loadUser` returns `null` (not an empty object) on any malformed or missing entry — the App's gate logic uses `null` as the trigger to show `<UserGate>`.
- `identifyUser({ token, cwid, birthday })` — async function next to `fetchAllData`. Calls `AUTH_SCRIPT_URL`, parses the response, and throws `AuthError` / `NoMatchError` / generic `Error` so the gate can branch cleanly. CWID is sent as the digit-stripped form from the input; birthday is sent as `MM-DD`.
- `isStaffOrFaculty(user)` — returns `true` when `user.role` is `staff` or `faculty` (case-insensitive). Single source of truth for any role-gated UI surface added later. Returns `false` on null/missing user so preview mode (no SHEET_ID, no auth) reads as "student" for safety. As of 2026-05-17b this helper has no callers — every role-gated surface so far has wanted the stricter staff-only `isStaff()` gate. Kept for future surfaces that want to include faculty (e.g. a class roster reader limited to the professors who teach in BA).
- `isStaff(user)` — strict staff-only gate. Same shape as `isStaffOrFaculty` but excludes faculty. Drives the Director response dashboard's "Ver respuestas" button visibility, since visiting professors who teach in BA shouldn't read other students' RSVP / dietary / size submissions.
- `<UserGate cohortToken onAuth onCohortReset />` — bilingual full-screen gate component. Visual identity matches `<PasscodeGate>` (same gradient, same logo, same EB Garamond title-pair pattern, same DM Mono caption, same Pep Orange–accented error panel, same submit button style). Microline reads "Buenos Aires Program" instead of "Pepperdine University" to signal continuation from the cohort gate. Title pair: "¡Hola! / Hello!" (warmer than the cohort gate's institutional title). Caption: "Decinos quién sos / Tell us who you are." Two fields: a 9-digit CWID input (`inputMode="numeric"`, `maxLength={9}`, onChange strips non-digits so a paste like `"123-456-789"` cleans up automatically) and a birthday two-dropdown row (Spanish-primary month labels via `MONTH_OPTIONS`, day options re-cap when month changes via `daysInMonthMD()` — Feb=29 for leap-year support, Apr/Jun/Sep/Nov=30, others=31). Helper line at the bottom with a mailto link to `buenosaires@pepperdine.edu`. Auto-focuses the CWID input on mount. Reuses `SouthernCrossDecoration` and `LOGO_URI`.
- `<App>` state additions: `const [currentUser, setCurrentUser] = useState(() => loadUser())`. New callbacks: `handleUserAuth(user, birthday)` (augment user with birthday, stash, dismount gate); `handleCohortReset()` (clear cohort token, bounce to cohort gate); `handleSignOut()` (clear user AND prompts cache — see "Prompts and Responses" below — leaves cohort token AND profile intact, so signing back in restores everything). Three-state early-return: no SHEET_ID → preview; no cohort token → `<PasscodeGate>`; cohort token + no user → `<UserGate>`; both → main UI.

**ProfileModal integration.** The "First name" text input is gone (name comes from the roster, not user input). In its place is a read-only "Logged in as" card showing preferred-or-first name + last name, role (capitalized), email, and a confirmed "Cerrar sesión / Sign out" button below. Sign out clears `currentUser` only — leaves the cohort token AND the profile (`enrolledClasses`, `filterEnabled`) intact, so the next sign-in restores the student's settings without making them re-tick courses.

**Constants used by `<UserGate>`:**

- `MONTH_OPTIONS` — array of `{ value, es, en }` objects for the month dropdown; Spanish primary, English secondary in the option labels.
- `daysInMonthMD(monthStr)` — pure function returning days available in the day dropdown for a given month. Returns 31 when month is unset so the day dropdown is fully populated until a month is chosen.
- `SELECT_CHEVRON` — inline SVG data URI used as the right-side affordance on the month/day selects (since we strip native appearance to match the CWID input's visual styling).

**What the gate does not do.** It doesn't authenticate the user against an institutional identity provider (no Google sign-in, no Pepperdine SSO; that's a Phase-3-roadmap option held until the data grows sensitive enough to warrant it). It doesn't sync identity across devices — each device gets its own gate experience. It doesn't survive a "Clear site data" action; in that case the student re-enters CWID + birthday on next open. The cohort gate continues to do the actual security work; this gate just records who's using the app.

**Threat model.** Per-user identification, not authentication. Within a cohort, a student could enter another student's CWID + birthday and get into the app as that person. For the use cases the gate enables (RSVPs, dinner menus, t-shirt sizes, role-based UI), this is calibrated correctly: low-stakes data, closed cohort, honor-code expectations. If the app ever exposes anything individually sensitive (grades, medical info, financial details), this calibration should be revisited and Google sign-in via `@pepperdine.edu` becomes the right next step.

## Prompts and Responses

A generalized per-student data-collection primitive: the Director defines a prompt in a sheet, the app surfaces it to the right students, students submit answers, responses land back in the sheet. Once this primitive exists, every future use case (t-shirt sizes, meal RSVPs, activity sign-ups, evaluation surveys, weekly check-ins) is just a row in the `Prompts` tab — no code change.

**Architecture.** Two new tabs in the existing Roster spreadsheet (extends the auth permission boundary that already protects PII; avoids spinning up a third spreadsheet/script):

- **`Prompts` tab** — one row per logical question. Schema:

  | Column | Notes |
  |---|---|
  | `prompt_id` | Stable string. Director-controlled; used as the join key. |
  | `title_es`, `title_en` | Bilingual headline shown on the Today card / ProfileModal row and as the BottomSheet title. |
  | `description_es`, `description_en` | Optional context shown above the fields. |
  | `audience` | `all` (or blank) → everyone; `student` / `staff` / `faculty` (or comma-list) → role-gated; or a comma-list of CWIDs for narrow targeting. Mix freely. Case-insensitive. |
  | `start_date`, `end_date` | Optional `YYYY-MM-DD` window. Both blank → always active (used by evergreen profile prompts). Inclusive on both ends. |
  | `end_time` | Optional `HH:mm` (24-hour). Tightens the close on `end_date` from end-of-day to a specific time of day — useful for "RSVP closes at 8 PM" style cutoffs. Blank keeps the end-of-day default. Only meaningful when `end_date` is also set. |
  | `category` | `profile` / `meal` / `activity` / `feedback` (free-form; reserved for future grouping). |
  | `surface` | `today` (default) / `profile` / `both`. Drives where the prompt renders in the app. |
  | `lock_after_submit` | Optional. TRUE/FALSE; default FALSE. When TRUE, once the student submits, the form switches to read-only and a second submit is rejected with `already_submitted`. Use for "submit-once" prompts where the answer becomes load-bearing on something external (t-shirt size goes to ordering; an excursion sign-up where the headcount drives logistics). Leave FALSE for prompts where the student should be able to revise up to the cutoff (most meal RSVPs). |
  | `event_date` | Optional `YYYY-MM-DD`. Marks the date a prompt should resurface on Today in read-only mode, even after `end_date` has passed. Use for meal RSVPs and other event-tied prompts where the student wants to see their selection on the day of the event. On `event_date`, the prompt is suppressed from the regular `<PromptCard>` "Pendientes" tile and instead renders inline beneath the matching event row inside the activity card as a small "Tu selección · {title}" caption block. Profile-surface prompts ignore `event_date` and just remain on Profile in read-only state post-cutoff automatically (so t-shirt size lives there permanently, no `event_date` needed). Validator flags `event_date` before `end_date` (the cutoff would be after the event — almost certainly a Director typo). |

- **`PromptFields` tab** — one row per input box, joined to `Prompts` by `prompt_id`. Schema:

  | Column | Notes |
  |---|---|
  | `prompt_id` | Foreign key to `Prompts`. |
  | `field_id` | Stable string within the prompt (e.g. `appetizer`, `main`, `dessert`, `comments`, `size`). |
  | `field_order` | Integer; controls render order top-to-bottom. |
  | `label_es`, `label_en` | Bilingual field label. |
  | `field_type` | One of: `short_text` / `long_text` / `single_select` / `multi_select` / `number` / `boolean`. |
  | `options` | Semicolon-delimited for select types: `XS;S;M;L;XL;2XL`. |
  | `option_labels_es`, `option_labels_en` | Optional bilingual labels parallel to `options`. |
  | `required` | TRUE/FALSE — per field, not per prompt. Lets a meal RSVP require selections but leave the comments box optional. |
  | `placeholder_es`, `placeholder_en` | Optional hint text shown inside the input. |

  A single-field prompt is just the degenerate case (one `PromptFields` row). A meal RSVP with appetizer/main/dessert/comments is one `Prompts` row + four `PromptFields` rows.

- **`Responses` tab** — one row per `(cwid, prompt_id, field_id)`. Auto-created by the auth script on first submit, so a fresh deploy doesn't need a manual setup step. Schema:

  | Column |
  |---|
  | `submitted_at` |
  | `cwid` |
  | `prompt_id` |
  | `field_id` |
  | `value` |

  Upserts are keyed on the `(cwid, prompt_id, field_id)` triple. A submission updates `value` + `submitted_at` for existing rows and appends new rows for never-answered fields. multi_select values are stored semicolon-joined; booleans as `TRUE` / `FALSE` (matching `parseBoolean` convention used elsewhere in the project).

**How prompts flow on a cold open.** Once both gates are clear, the App's prompts effect fires `fetchPrompts({ token, cwid, birthday })` against the Roster Auth Endpoint. The lazy-init in `useState` already populated `prompts` from the per-cwid cache (`bap-prompts-cache`, 10-min TTL) so repeat opens render the previous list instantly; the background fetch refreshes silently. The script does the audience filter and active-window check on its side, and pre-fills any existing responses for this user, so the front end mostly just renders.

**How submit flows.** When the student opens a prompt and taps Submit, `<PromptForm>` calls `onSubmit({ promptId, fields })` → App's `handleSubmitPrompt` → `submitResponse({ token, cwid, birthday, prompt_id, fields })` → POST text/plain JSON body to the auth script. The script re-validates identity, runs each field through `validateSubmissionValues` against its declared type and required flag, upserts responses under a `LockService` script lock, and returns the refreshed prompt object. App splices it into local state and rewrites the per-cwid cache so both surfaces re-render in sync; PromptForm closes.

**Surfaces.** Two places in the app render prompts:

- **Today tab** — `<PromptCard>` renders between the activity card and `<TodayFinalsTile>`. Lists prompts whose `surface` is `today` or `both`. Pep Orange left stripe + "Pendientes / For you" header. Each row shows the prompt title with a small Pep Orange dot if any required field is unanswered (Parchment background, "Completar →" / "Responder →" CTA), or the calmer Ice background and "Editar →" CTA once the student has saved a complete response.
- **ProfileModal** — `<PromptProfileSection>` renders right after the "Logged in as" card. Lists prompts whose `surface` is `profile` or `both`. Each row shows a comma-joined preview of saved values for at-a-glance recall, or italic "Sin respuesta / No answer yet" when nothing has been submitted yet. Used for evergreen prompts the student should be able to revisit anytime (t-shirt size, dietary preferences, contact corrections).

Tapping a row from either surface bubbles up to App's `selectedPrompt` state, which renders `<PromptForm>` as a slide-up `<BottomSheet>` over either surface. The form sheet is rendered at App level (rather than inside Today or ProfileModal) so it stacks cleanly above either and shares one state — closing the sheet returns to whichever surface opened it.

**Cache layer (`bap-prompts-cache`).** Separate from the content-data cache (`bap-app-cache`) and the user record (`bap-user`). Keyed by `cwid` so a sign-out / sign-in on the same device doesn't surface the previous user's pending forms in the brief window before `fetchPrompts` repopulates state. 10-min TTL is intentional: prompts are Director-edited in Sheets and need to propagate fast — the auth script intentionally doesn't cache its responses, and the front-end TTL trades a bit of API chatter for snappier visibility into Director edits. Cleared by sign-out, by both AuthError handlers (data-fetch and refresh), and by the prompts-fetch effect when it sees `AuthError` / `NoMatchError`.

**Helpers in `App.jsx`:**

- `class SubmitError extends Error` — thrown by `submitResponse()` for non-auth failures (validation_failed, prompt_inactive, audience_mismatch, lock_failed, etc.). Carries `.code` and `.details` so `<PromptForm>` can branch on the error type to show a useful inline message. Distinct from `AuthError` so the gate flow isn't triggered on a normal validation failure.
- `fetchPrompts({ token, cwid, birthday })` — async function next to `identifyUser`. Calls the auth script's `?action=prompts` endpoint and returns the array. Throws `AuthError` / `NoMatchError` / generic `Error` so the App's effect can branch cleanly.
- `submitResponse({ token, cwid, birthday, prompt_id, fields })` — async function. POSTs text/plain JSON to avoid CORS preflight against Apps Script. Throws `AuthError` / `NoMatchError` / `SubmitError` / generic `Error`. Returns the refreshed prompt on success.
- `loadPromptsCache(cwid)` / `savePromptsCache(cwid, prompts)` / `clearPromptsCache()` — wrappers around `localStorage.getItem/setItem/removeItem` for the `bap-prompts-cache` key, each try/catch-wrapped. `loadPromptsCache` returns `null` when the entry is missing, malformed, expired, or keyed to a different `cwid`.
- `filterPromptsBySurface(prompts, target)` — returns prompts whose `surface` matches `target` (or `both`). Used by `<PromptCard>` (target `today`) and `<PromptProfileSection>` (target `profile`).
- `isPromptPending(prompt)` — true when at least one required field has no stored response. Drives the orange "needs answering" treatment vs. the calmer "edit anytime" affordance.
- `hasAnyResponse(prompt)` — true when at least one stored value on this prompt is non-empty. Used by the post-cutoff visibility rules (a locked profile-surface prompt stays visible only if the student actually answered; a meal prompt re-surfaces on its `event_date` only if there's a selection to show).
- `isPromptReadonly(prompt)` — null-safe wrapper around the wire `prompt.readonly` flag set by the server. Returns false for older cached prompt objects missing the field so the legacy editable behavior stays intact across the deploy.
- `getPromptDisplayState(prompt, todayStr)` — single source of truth for how each surface should render a prompt. Returns one of: `editable` (current behavior), `locked_readonly` (calmer Ice+Ocean treatment, lock glyph, no submit button), `inline_event` (suppressed from `<PromptCard>` and rendered inline beneath today's matching event in the activity card; reserved for `readonly + has stored response + event_date === todayStr`), or `hidden` (locked-but-never-answered prompts that would have nothing to show).
- `formatFieldValueForDisplay(field, rawValue)` — resolves a stored field value back to its human-readable display label. For selects it walks `option_labels_es` / `option_labels_en` so a stored option key (`M`, `appetizer_2`) renders as the bilingual label the student originally saw; for multi-selects it returns the values comma-joined; for booleans it returns `Sí / Yes` or `No`; other types pass through as strings. Used by the readonly `<PromptFieldInput>` branch and by inline-meal caption blocks.
- `formatPromptCutoff(prompt, now)` — returns `{ es, en }` strings for the subtle "closes at…" caption rendered beneath a prompt's title when `end_time` is set; returns `null` otherwise. Three proximity tiers: "Cierra hoy a las 20:00" (today), "Cierra mañana a las 20:00" (tomorrow), "Cierra el 15 de mayo a las 20:00" (further out). Past the cutoff, returns `Cerrado / Closed` so a stale cached entry doesn't claim the form is still open in the brief window before the next prompts fetch drops it.
- `initFormFromPrompt(prompt)` — builds the local form-state object from a prompt's stored responses, normalizing each field by type (multi_select → array, boolean → bool, others → string). Ensures controlled inputs stay defined.
- `formatPromptSavedAt(iso)` — bilingual relative-time label for the form footer ("hace 5 min / 5m ago"), falls back to a date label after 24h.

**Components in `App.jsx`:**

- `<PromptFieldInput field value onChange readonly />` — renders one field per `field_type`. Selects render as tappable cards with ✓ (multi) / • (single) and bilingual EB Garamond labels (vs. a native `<select>`, which is cramped on mobile and doesn't support bilingual options well). Boolean is a Sí/Yes vs. No pill pair. Numbers use `inputMode="decimal"` with digit-only sanitization. short_text/long_text are styled inputs with optional placeholder. Required fields show a small Pep Orange dot beside the label. Unknown types render a defensive placeholder rather than crashing the form. **Readonly branch** (when `readonly` is truthy): every field type collapses to a static Ice-filled readout displaying the saved value (resolved through `formatFieldValueForDisplay`), with italic stone `Sin respuesta / No answer` when an optional field is empty. The required-field orange dot is suppressed in readonly mode (it would be a contradictory affordance).
- `<PromptForm open onClose prompt onSubmit />` — bottom-sheet wrapper using the existing `<BottomSheet>` component. Sticky-ref pattern (`lastPromptRef`) preserves the prompt object during the BottomSheet's 260ms close animation so the form doesn't flash an empty fallback during exit. `useEffect` deps include only `promptKey` (the `prompt_id`) so a background prompts refresh while the form is open won't wipe the student's in-progress edits. Reads `isPromptReadonly(prompt)` on every render: when true, a small Ice-filled banner with a `<LockGlyph>` glyph and DM Mono caption ("Cerrado · No editable / Closed · View only") sits above the description, every field receives `readonly={true}`, and the Pep Blue submit button is replaced with a Fog-bordered "Listo / Done" close button. Maps `SubmitError.code` to bilingual inline error panels (Pep Orange–striped Parchment background): `validation_failed` → "Falta completar: ${label} / Missing: ${label}" or "Valor inválido: ${label} / Invalid value: ${label}"; `prompt_inactive` → "Este formulario ya cerró"; `already_submitted` → "Ya enviaste este formulario. / You already submitted this form."; `audience_mismatch` → "No tenés acceso"; `lock_failed` → "La planilla está ocupada. Reintentá"; AuthError / NoMatchError → corresponding "Sesión expirada" / "Tu usuario ya no es reconocido" copy. Submit button label shifts: "Enviar / Submit" first time, "Actualizar / Update" once a prior submission exists. Footer shows the relative-time label of the most recent save in either mode.
- `<PromptCard prompts onOpenPrompt todayStr />` — Today tile listing prompts with `surface=today/both`. Renders `null` when no such prompts exist (so the Today layout stays clean for students whose Director hasn't queued anything). Filters out `inline_event` and `hidden` display states (those render elsewhere or not at all). Editable rows keep the existing Parchment/Pep-Orange-or-Ice/Ocean treatment; `locked_readonly` rows render with Ice background + Ocean stripe + "Ver tu selección →" CTA + 12 px lock glyph beside the title (the orange-dot Parchment treatment is reserved for pending-required prompts only). Each row is a `<button>` that calls `onOpenPrompt(p)` to open the (possibly readonly) form sheet for that prompt.
- `<PromptProfileSection prompts onOpenPrompt />` — list block inside `<ProfileModal>` rendering prompts with `surface=profile/both`. Drops never-answered locked prompts (otherwise the gear modal would nag about content that has nothing to show). Editable rows render with the white background and existing pending/answered treatment; saved-but-locked rows render with Ice background + Ocean stripe + lock glyph + "Ver →" CTA. Same row shape as `<PromptCard>` with a comma-joined preview of saved values per row.

**Inline-event display on Today** (separate from `<PromptCard>`): a locked meal prompt whose `event_date === todayStr` and which has a stored response renders *inside* the activity card as a small Ice-filled caption block ("Tu selección · {title}" with a 11 px lock glyph in the DM Mono caption row, comma-joined responses in 13 px Roboto beneath, Ocean left stripe). The block is tappable and opens the read-only `<PromptForm>` for a fuller view. Three render branches in `<TodayView>` handle the interplay with the existing activity-card logic: (1) `items.length > 0` — meal blocks append at the bottom of the items list; (2) `items.length === 0` AND `hasInlineMeals` — a minimal Agenda card renders with just the meal blocks (the "¡Día libre!" empty state is suppressed because a planned meal contradicts a "nothing scheduled" read); (3) `items.length === 0` AND class-cancelling holiday AND no inline meals — `activityCard = null` (legacy behavior preserved; the holiday card alone is enough). Branch 2 also wins over the class-cancelling-holiday branch when both apply — a feriado with a planned program meal still shows the locked selection.

**`<App>` state additions:**

- `prompts` (lazy-init from `loadPromptsCache(currentUser?.cwid)`) — the array of prompt objects.
- `selectedPrompt` — the prompt currently being edited in the form sheet; `null` when the sheet is closed. Set via `handleOpenPrompt(p)`, cleared via `handleClosePrompt()`.
- `handleSubmitPrompt({ promptId, fields })` — async callback passed to `<PromptForm>`. On success, splices the refreshed prompt into local state and rewrites the cache. On `AuthError` / `NoMatchError`, clears the relevant credentials and lets the gate flow take over. Re-throws the error so the form can branch on `SubmitError.code` for inline copy.
- Background prompts effect — runs whenever `cohortToken`, `currentUser.cwid`, or `currentUser.birthday` changes. Best-effort: network failures are logged and swallowed, leaving whatever's in state (cache or empty).

**What the prompts feature does not do.** It doesn't sync state across devices — each device hits the auth script on open, so prompts and responses are consistent within a few seconds, but two devices won't push to each other in real time. It doesn't support file uploads (no image, no PDF). It doesn't support cross-prompt logic (e.g. "show field X only if field Y = 'yes'") — every field renders unconditionally. It doesn't support per-field `placeholder` values that vary by language separately from `placeholder_es`/`placeholder_en` (it picks Spanish-first, falls back to English if Spanish is blank). And it doesn't write to the content spreadsheet — submissions live entirely on the Roster side, by design (the content script literally has no read access to the Roster, the auth script literally has no read access to the content sheet).

**Threat model.** Submissions are authenticated by the same cohort-token + cwid + birthday triple the rest of the user-auth surface uses. The auth script re-validates identity on every submit (looking the row up in the Roster), so a stolen cohort token alone can't write submissions. Within the cohort, a student who knows another student's CWID + birthday could submit on behalf of them — same calibration as the user gate. For the data this primitive collects (sizes, meal preferences, RSVPs), this is the right calibration; if a future prompt collects something individually sensitive (grades, financials, anything FERPA-touching), the cohort+CWID+birthday triple becomes the wrong gate and the system should move to Google sign-in.

## Director Response Dashboard

A role-gated surface inside the app for reading every prompt's submissions across the cohort. Shipped 2026-05-17b on top of the prompts/responses primitive, closing the loop on Director-side data collection: the Director can now see who's responded, who hasn't, and what they said, without leaving the app for the Responses spreadsheet tab.

**Access.** A "Ver respuestas / View responses" button inside `<ProfileModal>`, visible only when `isStaff(currentUser)` returns true. **Strictly staff-only** — faculty are intentionally excluded; visiting professors who teach in BA shouldn't read other students' RSVP / dietary / size submissions, so the gate is `role === "staff"` rather than the more permissive `isStaffOrFaculty` helper. Tapping the button closes the gear modal and opens `<DirectorResponsesView>` over the rest of the app. The server enforces the same role check on every request (role !== "staff" → `forbidden`), so hiding the button on the client is just UX — a non-staff user manually hitting the URL gets `{ error: "forbidden" }`.

**Endpoint.** New `?action=admin_responses` on the auth script. See the Roster Auth Endpoint shape table above for the exact request/response. Returns every prompt with its full field list, the expected-audience CWIDs (computed server-side by walking the active roster against the prompt's `audience` expression), every response row, an `is_active` flag (computed server-side from `start_date` / `end_date` / `end_time`), and a curated roster lookup keyed by CWID for rendering submitter names. All prompts — active and expired — so the Director can look at historical submissions inside the app.

**Two internal screens.** `<DirectorResponsesView>` is a single overlay that swaps between two views based on internal `selectedPromptId` state:

1. **Prompt list.** Active prompts first, then closed, descending by `end_date` within each band so the freshest live data leads. Each row shows the bilingual title, an Active/Closed pill, the response count (`12 / 18`), and the prompt's window dates. The count flips from Pep Blue to green (#1E8E3E) when `responded >= expected`.
2. **Drill-in detail.** Header carries a back chevron. Body shows the bilingual description, an audience + window + counts summary card, a per-field section for each field, a "Falta responder / Awaiting response" section listing non-responders, and an optional "Fuera de la audiencia / Outside audience" section when there are responders whose CWID isn't in the current expected audience (happens when the Director narrows audience after some students have already submitted — the data is surfaced rather than dropped).

**Per-field sections.** For `single_select` and `multi_select` fields, a horizontal tally bar (Ocean fill on Ice background) renders one row per declared option in order, with `"other"` values that don't match any declared option appended after. Other field types skip the tally and just list responses. Below the tally is a row per responder showing their value for THIS field; partial submissions (a responder who skipped this field on a multi-field prompt) are filtered out of the list. Multi-select values render as comma-joined for readability (`Empanadas, Provoleta` instead of the stored `Empanadas;Provoleta`).

**Name rendering.** `directorResponderName(rosterEntry, cwid)` returns `"Preferred-or-first Last-initial."` to match the rest of the app's first-names-only default. Falls back through preferred → first → last → cwid if a roster field is missing. Full names are available in the Roster spreadsheet for any deeper drill-down.

**Helpers in `App.jsx`:**

- `class ForbiddenError extends Error` — thrown by `fetchAdminResponses()` when the auth script returns `{ error: "forbidden" }`. Distinct from `AuthError` (cohort token bad) and `NoMatchError` (CWID/birthday wrong) so the caller can keep the dashboard open with an inline error instead of bouncing the user through the gates.
- `fetchAdminResponses({ token, cwid, birthday })` — async function next to `fetchPrompts`. Calls the auth script's `?action=admin_responses` endpoint. Throws `AuthError` / `NoMatchError` / `ForbiddenError` / generic `Error`.
- `sortDirectorPrompts(prompts)` — active-first then descending by `end_date`. Pure function.
- `groupResponsesByCwid(responses)` — flattens the response row array into a `{ cwid: { field_id: value } }` map for O(1) per-cwid lookup.
- `respondedCwidSet(responses)` — set of CWIDs that have at least one stored value for this prompt. Drives both the "X of Y responded" count and the responder/non-responder split.
- `tallySelectField(field, responses)` — counts per declared option in field-defined order, with extras appended. Multi-select values split on semicolons.
- `directorPromptTitle` / `directorPromptTitleAlt` / `directorPromptDescription` / `directorFieldLabel` / `directorResponderName` — small bilingual rendering helpers with sensible fallbacks.

**Components in `App.jsx`:**

- `<DirectorResponsesView open onClose loading error payload onRefresh />` — the overlay. Holds the `selectedPromptId` state internally; resets to `null` when `open` flips false→true so the dashboard always re-opens to the list. Renders a loading pill and an error panel at the top of the body, both pinned above whatever cached content is on screen so the Director still sees yesterday's data while a refresh is in flight.
- `<DirectorPromptList prompts roster onSelect loading error />` — the list view. One `<button>` per prompt with the title pair, status pill, response count, and window dates.
- `<DirectorPromptDetail prompt roster />` — the drill-in. Renders the header summary, then maps over the prompt's fields to render `<DirectorFieldSection>` for each, then the non-responder / outside-audience sections at the bottom.
- `<DirectorFieldSection field responses roster responseByCwid respondedCwids />` — one field's full breakdown: label + type badge, tally bars (if select), individual responses.

**App-level state and handlers:**

- `directorOpen` / `directorPayload` / `directorLoading` / `directorError` — four pieces of state. No localStorage cache: the dashboard is opened a handful of times per week, and staleness is more confusing than a 1-second loading state on each open.
- `loadDirectorResponses` — async callback. Fetches the payload, sets state. AuthError → clears cohort token + user + prompts cache, closes the dashboard, lets the gate flow take over. NoMatchError → clears user, closes the dashboard. ForbiddenError → keeps the dashboard open with a bilingual error. Other errors → keeps the dashboard open with a generic bilingual error.
- `handleOpenDirector` — closes `<ProfileModal>` first (so the dashboard isn't stacked under it) then calls `loadDirectorResponses`.
- `handleCloseDirector` — closes the dashboard and clears the error so re-opening starts clean.

**What the Director dashboard does not do (v1).**

It doesn't export to CSV — for that, copy the `Responses` tab in the Roster spreadsheet. It doesn't filter or sort the response list within a field — entries appear in the order their CWIDs were added to the prompt's expected audience. It doesn't show submission timestamps in the per-responder rows (they're in the response payload but suppressed to keep the row compact; the per-prompt freshest `submitted_at` could surface in v2 if useful). It doesn't compute per-option percentages, just raw counts on the tally bars. It doesn't notify the Director when new submissions arrive — refresh is manual via the Actualizar button.

**Threat model.** The Director already has full Roster access via the spreadsheet, so this view doesn't add a new leak surface from the Director's perspective. The new surface is: a staff roster row can now read all submissions from the front-end app. The cohort-token + CWID + birthday triple already gates the auth endpoint; the strict role === "staff" check adds a second gate. A non-staff row (student or faculty) that somehow obtains a staff CWID + birthday could read all submissions — same calibration as the user gate (within-cohort credential theft is an honor-code issue, not a technical one). If the prompts feature ever collects FERPA-touching data, this dashboard becomes part of what needs an SSO-grade auth upgrade alongside the rest of the user-auth surface.

## Local Cache

The app uses a stale-while-revalidate pattern backed by `localStorage` so repeat opens render content instantly instead of showing a loading screen.

**How it works:**

1. On mount, `useState` reads from `localStorage` synchronously during the first render. If a cached payload is found, it's used as the initial `data` state and status starts as `refreshing`.
2. Independent of cache, a background fetch to the Google Sheet kicks off via `useEffect`.
3. When the fetch resolves, fresh data replaces the cached data on screen and the cache is rewritten with a new timestamp; status becomes `live`.
4. If the fetch fails and cached data was already on screen, it stays on screen; status downgrades to `cached` ("Saved version (offline)"). If no cache existed and the fetch fails, the app falls back to hardcoded `DEFAULT_DATA`; status becomes `fallback`.

**Storage shape:**

```json
{
  "version": 2,
  "data": { /* full normalized data object */ },
  "timestamp": 1745001234567
}
```

Stored under the localStorage key `bap-app-cache`.

**CACHE_VERSION:**

The constant `CACHE_VERSION` in `App.jsx` is the cache key version. Whenever the data shape changes (renamed columns the app reads, new fields, removed fields, new sheet tabs the app consumes), this number must be incremented. On the next open, the app sees the version mismatch and ignores the old cache; the next successful fetch then writes a fresh cache at the new version. This prevents students from seeing broken renders against stale data shapes.

**Helpers in `App.jsx`:**

- `loadCache()` — reads and parses the cached payload. Returns `null` if absent, malformed, or version-mismatched.
- `saveCache(data)` — writes the data with the current version and timestamp. Wrapped in try/catch so quota errors or disabled storage are silently ignored.

**What the cache does not do:**

As of 2026-04-26, the cache layer has a service-worker companion (see "PWA" section below) that precaches the JS bundle, fonts, icons, and HTML. With the SW installed, the app does provide true offline mode for the shell + cached content. The localStorage cache continues to handle the *content* layer (sheet data, weather, dólar); the SW handles the *shell* layer. Together they mean a student on the subte or in a dead zone gets the full app, including layout and content from the last successful fetch.

The cache itself does not eliminate the underlying network fetch entirely; it just makes the second-and-onward opens feel instant by serving cached data while the network round trip happens in the background. The fetch itself was sped up substantially by the consolidated Apps Script endpoint (see above), which collapses 15 round trips into one and serves most opens from the script's own 1-hour CacheService cache.

## Student Profile

The app supports per-student personalization via a small profile stored locally in the browser. The profile is optional: students who never open Settings see exactly the same app every other student does. Director use is unaffected; the filter is off by default and only takes effect when explicitly enabled.

The profile is distinct from `currentUser` (the User Auth section above). The roster row identified at sign-in is the authoritative source for *who the student is* (first name, role, email, t-shirt size, etc.); the profile holds *what the student has chosen on this device* (which courses they're enrolled in, whether the filter is on). They live at separate localStorage keys (`bap-profile` vs. `bap-user`) so each can be cleared independently — sign out wipes identity but preserves course selections, while a profile reset clears selections but leaves identity untouched.

**What's customizable:**

- **Enrolled courses.** A multi-select list of every course currently in the Classes tab. Each row shows the course code and title with the same color accent used elsewhere in the app.
- **Show only my classes (toggle).** When on, the Today tab's "Today's Activity" card and the Schedule tab's Class Schedule and Courses sub-tabs filter to only the ticked courses. The Weekly Overview is unaffected because it shows calendar events, not classes; calendar events are never filtered by profile.
- **Persistent announcement dismissals.** The × on an announcement now sticks across sessions instead of returning every time the app reloads. Tracked by a stable `djb2` hash of the message text, so editing an announcement in the sheet effectively resurfaces it as new.

The "First name" field that used to live here was removed in v2: the Today greeting now reads from `currentUser.preferred_name || currentUser.first_name` (the authoritative roster identity) instead of a student-typed string. Existing v1 profiles are migrated by `loadProfile()` rather than nuked — `enrolledClasses` and `filterEnabled` are salvaged so a student who already personalized doesn't lose course selections on the deploy that introduces the user gate.

**Access:** A small gear icon in the top-right of the header opens the Settings modal. The modal is full-screen within the 480px column. At the top is a read-only "Logged in as" card (preferred-or-first name + last name, role, email, plus a confirmed "Cerrar sesión / Sign out" button) sourced from `currentUser`; below it are the toggle, course checklist, and "Reset profile" button. Changes save immediately to localStorage on every interaction; the "Done" button just closes the modal. Sign out clears `currentUser` only — leaves the cohort token AND the rest of the profile intact, so signing back in restores everything. As of 2026-06-10 both the "Reset profile" and "Sign out" confirmations use the in-app `<ConfirmDialog>` (a centered, on-brand card) instead of native `window.confirm`, and the modal itself has `role="dialog"` + the shared `useDialogA11y` focus-trap/Escape/focus-return treatment.

**Filter affordance:** When the filter is active, an inline pill at the top of the Schedule sub-tabs (Class Schedule and Courses) reads "My classes only — Showing N of M. Tap to change." Tapping it reopens the Settings modal. The pill does not appear on the Weekly Overview (calendar-only) or on the Today tab (where the filter is visible implicitly through the activity card).

**Auto-enable on first selection:** As of 2026-04-26, ticking the first class in the profile editor's course checklist automatically flips `filterEnabled` from false to true. This means a student doesn't have to remember to also toggle "Show only my classes" — selecting their courses is intent enough. Subsequent toggling of `filterEnabled` (off and back on) respects the user's deliberate choice; the auto-enable only fires on the empty-to-non-empty transition.

**Class display gating on Today and Weekly Overview:** Also as of 2026-04-26, classes only render on the Today tab activity card and inside the Calendar tab's Weekly Overview day cards when `shouldFilterClasses(profile)` is true. Without personalization, the Today activity card shows only events (or the "¡Día libre!" empty state); Weekly Overview day cards show only events. The Schedule tab's Mon–Fri Class Schedule grid is intentionally exempt — it's the unfiltered browseable view of the program's classes, and a student should be able to see all classes there regardless of personalization.

**Holiday-aware suppression:** On any day with a calendar event of `type: "holiday"`, classes are suppressed on both Today and Weekly Overview, even when the student has personalized. On Today this triggers a small holiday card above the activity card (parchment treatment, `#C62828` accent stripe, bilingual "Feriado / Holiday" DM Mono label, the event title in italic EB Garamond, and the event description in Roboto); the activity card's "¡Día libre!" empty state is suppressed because the holiday card already explains the open day.

**Storage shape:**

```json
{
  "version": 3,
  "enrolledClasses": ["SPAN 350", "HUM 295"],
  "filterEnabled": false,
  "dismissedAnnouncements": [],
  "seenAnnouncements": ["g45ysh", "a1b2c3"]
}
```

Stored under the localStorage key `bap-profile`, separate from `bap-app-cache` and from `bap-user`. This separation is intentional: bumping `CACHE_VERSION` (which happens when the Google Sheet data shape changes) wipes the data cache but leaves the profile intact, so students don't lose their settings every time the schema evolves; signing out clears `bap-user` but leaves the profile alone, so re-signing-in restores course selections without making the student re-tick anything.

The `dismissedAnnouncements` array is a legacy field kept in the schema for backwards compatibility with previously stored profiles. As of the 2026-04-26 announcement-banner redesign, announcements are no longer user-dismissible (the program office controls lifecycle via `end_date`), so this field is neither read nor written anymore. It will simply remain at `[]` for new profiles and stay frozen at whatever value existing profiles had.

The `seenAnnouncements` array (added 2026-06-10b, v3) holds the `announcementKey()` djb2 hashes of announcements the student has already seen on the Today tab. It drives the unread dot on the Today bottom-nav tab: an active announcement whose key isn't in this list lights the dot until the student opens Today, at which point the mark-seen effect rewrites the list to exactly the currently-active keys (so it self-prunes as old announcements expire). Editing an announcement's message or shifting its dates changes its key, resurfacing it as new — matching the old `dismissedAnnouncements` convention.

The `name` field that lived in v1 was removed in v2 — see the "Logged in as" card discussion above.

**PROFILE_VERSION:**

The constant `PROFILE_VERSION` in `App.jsx` is the profile-shape version (currently 3). Bump it only when the profile shape itself changes (new field, renamed field, removed field). On next open, the version mismatch causes `loadProfile()` to return `EMPTY_PROFILE` for any version it doesn't recognize, and the next save writes a fresh profile at the new version. The v1 → v2 and v2 → v3 transitions are special-cased: rather than nuking, `loadProfile()` salvages `enrolledClasses` and `filterEnabled` from old profiles so course selections survive the deploy (v2 → v3 added `seenAnnouncements`, defaulted to `[]` on migration so the cue doesn't fire for already-seen announcements). Future migrations should follow the same pattern when there's value in preserving carryover state.

**Helpers in `App.jsx`:**

- `loadProfile()` — reads and parses the profile from `localStorage`, normalizing array fields and applying defaults. Returns a fresh `EMPTY_PROFILE` clone if absent, malformed, or version-mismatched.
- `saveProfile(profile)` — writes the profile with the current `PROFILE_VERSION`. Wrapped in try/catch so quota errors or disabled storage are silently ignored.
- `shouldFilterClasses(profile)` — returns `true` only when the profile has `filterEnabled: true` AND at least one entry in `enrolledClasses`. The two-condition gate means the filter never accidentally hides everything for a student who turned it on before ticking any courses. As of 2026-04-26 this helper is also the gate for whether classes appear on Today and Weekly Overview at all (not just whether they're filtered) — a student without personalization sees no classes in those views.
- `filterClassesByProfile(classes, profile)` — returns `classes` unchanged when `shouldFilterClasses` is false, otherwise returns the subset whose `code` is in `enrolledClasses`. Used by `getTodayItems()`, `WeeklyOverviewView`, and `ClassScheduleView`.

**What the profile does not do:**

It does not sync across devices. A student who installs the app on both their phone and laptop will have two independent profiles. Cross-device sync would require a backend, which this project doesn't have. It also does not survive a "Clear site data" action in browser settings; in that case the student re-enters the profile the next time they open the app.

## Brand Identity

The app follows the BAP Brand Identity Guide (see separate `Branding_Guidelines` project knowledge file for the full guide). Key tokens are defined in the `C` object in `App.jsx`:

| Token | Hex | Usage |
|-------|-----|-------|
| bapBlue | #64B5F6 | Primary BAP color; "Pepperdine University" label, active tab dot |
| pepBlue | #00205B | Header gradient, headlines, nav active state |
| pepOrange | #E35205 | Sparingly; milestone event accents, urgent announcement accent |
| ocean | #0057B8 | Links, badges, header gradient end, info announcement accent |
| sky | #6CACE4 | Supporting accent |
| fog | #B9D9EB | Borders, dividers |
| mountain | #425563 | Body text, secondary content |
| stone | #7A99AC | Tertiary text, labels, inactive nav |
| pepBlack | #1D252D | Primary text |
| ice | #E3F2FD | Light backgrounds, pill buttons, info announcement background |
| parchment | #F5F3F0 | Main page background, urgent announcement background |

**Typography:**

- Headlines: EB Garamond (Bold 700)
- Body: Roboto (Regular 400)
- Labels, metadata, times: DM Mono (Regular 400)

All loaded from Google Fonts via a `<link>` in `index.html`.

**Logo:** The BAP circular logo is served as a static asset at `/logo.png` (160×160 PNG with a transparent background and a circular alpha mask, ~47KB on disk, 2× retina for the 80px CSS display). The `LOGO_URI` constant in `App.jsx` is just the path string `"/logo.png"`; the three header `<img>` sites reference it unchanged. Prior to 2026-05-16 the same image lived as a ~63KB base64 string inlined directly in `App.jsx`, which added that weight to the JS bundle the phone had to parse before first paint; moving it to a static asset both shrinks the bundle and lets the SW precache the image alongside the rest of the shell. PWA home screen icons use a separate version with a solid Pep Blue (`#00205B`) background.

Source images are available in the project files:
- `LOGO_Buenos_Aires_Program_BAP_App__Transparent_Background.png` (for in-app header)
- `LOGO_Buenos_Aires_Program_BAP_App__PepBlue_Background.png` (for PWA/home screen icons)

## Deployment

**Platform:** Vercel (free tier)
**Repo:** GitHub (the user manages this manually)
**Auto-deploy:** Yes; any commit to the GitHub repo triggers a Vercel rebuild (typically ~60 seconds)

**To update the app code:**

1. Download the updated `App.jsx` from Claude
2. Go to the GitHub repo > `src` > `App.jsx`
3. Click the pencil icon to edit
4. Select all, delete, paste new content
5. Click "Commit changes"
6. Vercel auto-deploys

**To update content (classes, calendar, announcements, health, churches, apps, FAQ):**
Just edit the Google Sheet. The app fetches fresh data on each page load; cached students see new content the next time their background fetch succeeds.

## PWA (Add to Home Screen + Offline Mode)

The app includes a `manifest.json`, `apple-touch-icon.png`, and a Workbox-based service worker so students can install it to their phone's home screen and run it offline. It appears as a standalone app with the BAP logo as the icon, the Pep Blue theme color, and "Buenos Aires Program" as the app name.

Students do this by opening the URL in their browser and tapping:

- **iOS Safari:** Share button > "Add to Home Screen"
- **Android Chrome:** Three-dot menu > "Add to Home Screen" or "Install app"

Note: iOS aggressively caches `apple-touch-icon.png`. Students who already have the app on their home screen may need to remove and re-add it to pick up a new icon.

**Service worker (offline mode):**

The service worker is generated at build time by `vite-plugin-pwa` (configured in `vite.config.js`). It precaches the app shell on first install (JS bundle, CSS, fonts, icons, HTML, manifest) so subsequent opens launch with zero network: useful on the subte where there's no signal, in a hostel with flaky Wi-Fi, on flights, or for students rationing a US data plan.

Two layers of caching now stack:

1. **Service worker precache.** The shell. Once the SW installs, every JS chunk, every font file, every icon, and the HTML itself live in the browser's CacheStorage and load from disk on every open.
2. **localStorage data cache (`bap-app-cache`).** The content. Class schedules, calendar events, contacts, and so on. Stale-while-revalidate, refreshed in the background on every successful fetch.

Together they mean a student opening the app on the subte with no signal sees their full app: shell, fonts, icons, schedule, calendar, contacts, the works. The Today tile's live data (weather, dólar, current temperature) falls back to whatever was last seen, since those calls require network and are not cached at the SW layer.

**Why those calls are not in the SW cache.** The Apps Script endpoint, Open-Meteo, and dolarapi.com all already have their own purpose-built localStorage caches in `App.jsx` (`bap-app-cache` for sheet data, `bap-today-cache` for weather and dólar, both with explicit TTLs and version keys). Adding a Workbox runtime cache on top of those would create two parallel cache layers with different staleness semantics, producing surprising bugs (e.g., the SW serving a 6-hour-old dólar to a function that thinks it just got fresh data). The localStorage caches are the source of truth for those endpoints; the SW handles only the shell.

**What the SW does cache at runtime:**

- **Google Fonts CSS** (`fonts.googleapis.com`): `StaleWhileRevalidate` with a one-year max age. The CSS file is small, occasionally updated, and gets quietly refreshed in the background.
- **Google Fonts WOFF2 files** (`fonts.gstatic.com`): `CacheFirst` with a one-year max age. Font files are content-addressed and effectively immutable; first download, lifetime cache.

**Update lifecycle:**

The plugin runs with `registerType: 'autoUpdate'`, plus Workbox's `clientsClaim: true` and `skipWaiting: true`. When a new build is deployed, the SW detects the update on the next open, the new version takes over immediately, and the student sees the new build with no in-app prompt. There is no "tap to refresh" banner; updates are silent and seamless. This matches the rest of the app's UX philosophy: students never have to think about it.

**On-resume update check (added 2026-06-11b).** The browser only re-checks the SW *script* for a new version on a real navigation, but an installed Android PWA resumed from the background usually just *restores* the existing page — so a freshly deployed build could sit undetected for a long time (the field report was a 24h-cold open showing stale content until a manual double hard-close). To close that gap, an app-level `useEffect` at the top of `<App>` forces an update check — `navigator.serviceWorker.getRegistration().then(reg => reg.update())` — on every `visibilitychange → visible` (app regains focus), plus an hourly `setInterval` backstop while open and once on mount. This only triggers the *check* more often; the SW config above is unchanged, and once a new worker is found `skipWaiting`/`clientsClaim` activate it as before. It is deliberately **detect-only**: there is no `controllerchange`-driven page reload, so a session is never interrupted mid-task — the activated build shows on the student's next cold launch rather than live in the resumed session. The effect early-returns when `navigator.serviceWorker` is unavailable (preview / unsupported). Note that each device must pick up the 2026-06-11b build itself once before this on-resume checking applies; from the next deploy onward those devices self-check on resume. A `controllerchange`-driven silent auto-reload (fresh content on the *same* reopen) was considered and held as a small future addition.

iOS quirk: on home-screen-installed PWAs, iOS WebKit can take a second cold open before a fresh SW activates. This is iOS behavior, not a bug; the second open always picks it up. iOS also separately evicts installed-PWA service workers after ~7 days of non-use; the on-resume check above helps but can't override that OS-level eviction.

**Build output:**

`vite-plugin-pwa` adds two files to `dist/` at build time: `sw.js` (the service worker itself) and `workbox-*.js` (the Workbox runtime, hashed filename). Both are served by Vercel from the site root. Vercel's default cache headers are appropriate for both; no `vercel.json` overrides needed in the common case. If updates ever seem to propagate slowly after a deploy, the suspect is usually browser-side SW caching, which can be cleared with a hard reload (Cmd-Shift-R / Ctrl-Shift-R) or by closing and reopening the home-screen app entirely.

**Verifying offline mode:**

In Chrome DevTools, Application > Service Workers shows the registered SW for `baprogram.vercel.app`. Application > Cache Storage shows the precache (named with the build hash) and the two `bap-google-fonts-*` runtime caches. Toggle DevTools > Network > Offline, refresh the page, and the app should load fully from the precache.

## App Features

**Today tab (default):**

The leftmost tab and the default view on every app open. A daily-snapshot dashboard built to make the app a daily-open habit instead of a reference manual. Top to bottom:

- **Greeting strip.** Time-of-day gradient (BAP Blue → Sky → Ocean → Pep Blue across the day, deep navy at night), Spanish greeting (`Buen día` / `Buenas tardes` / `Buenas noches`) in 26 px EB Garamond, italic Spanish date below (e.g., "Lunes, 28 de abril"). A slow-rotating sun glyph fills the top-right during day hours; a crescent moon (`<MoonIcon>`) takes its place from 19:00 to 06:00. Sun rotation is `bap-sun-spin` (80 s linear infinite); suppressed under `prefers-reduced-motion`.
- **Quick-stats row.** Two equal tiles, both tappable as of 2026-04-27. **Weather** (Open-Meteo): a small `<WeatherIcon>` keyed off `weather_code` and `is_day`, current temperature in Fahrenheit (DM Mono, 22 px) as the headline, today's high/low in Fahrenheit on a small mono line below (e.g., `↑ 78°  ↓ 65°`), and a one-line bilingual dress hint ("Un sweater / A sweater"). When the next 48 hours of hourly data show something disruptive (thunderstorm, heavy rain, freezing temps, strong wind, or notable rain at high probability), a single-line bilingual alert renders below the dress hint on a Pep Orange–striped Parchment background: Spanish in 11.5 px Roboto medium weight, a small slash separator in stone gray, then English in italic EB Garamond — same pattern as the dress hint right above it. Underlying values stay in Celsius so the dress-hint thresholds keep working; conversion to Fahrenheit happens at display time via `cToF()`. Tapping the tile opens `<WeatherSheet>` (see below). **Dólar** (dolarapi.com): Blue `compra` rate as `$1.250` in DM Mono as the headline (the rate a casa pays you per dollar — what students actually transact at when cashing USD), with `MEP $1.220` and `Oficial $1.000` shown as two small DM Mono lines below. Tapping the tile opens `<DolarSheet>`, the bidirectional currency calculator. Both tiles fall back to an em-dash placeholder when the fetch fails; the secondary lines fall back to `MEP —` / `Oficial —` independently when one of them is unavailable. Empty-state placeholder tiles render as plain divs (no false tap affordance); populated tiles render as `<button>` elements with the existing `bap-press` scale-on-tap feedback.
- **Weather sheet.** Slide-up bottom sheet (`<BottomSheet>`) opened by tapping the weather tile. Two stacked sections. Top: a horizontal "Próximas 12 horas / Next 12 hours" strip, each card showing time label ("Ahora" for the current hour, then "14h", "15h", etc.), `<WeatherIcon>` with day/night derived from the hour-of-day in the ISO timestamp, Fahrenheit temp, and rain probability when ≥40 %. Sourced from the existing 48-hour hourly slice already in the weather object — no extra fetch. Bottom: a 7-day "Próximos 7 días / Next 7 days" list, one row per day with weekday label (Spanish primary, English italic gloss; today shows "Hoy / Today"), `<WeatherIcon>` (always day-side), bilingual condition descriptor from `getWeatherLabel(code)`, and high/low temps. Rain probability (`☂ 60 %`) and max wind gusts (`≈ 45 km/h`) appear as compact DM Mono captions only when meaningful (rain prob ≥25 %, gust ≥30 km/h) so quiet days stay clean. Dress hint is intentionally kept on the Today tile only, not duplicated here. The dolar tile and weather tile are independent; opening one doesn't affect the other.
- **Currency calculator sheet.** Slide-up bottom sheet (`<BottomSheet>`) opened by tapping the dólar tile. A direction toggle at the top (Pesos → Dólares / Dólares → Pesos) defaults to ARS → USD because students are usually computing what a peso price means in dollars. A large input field accepts decimal input (commas auto-normalized to dots for Argentine convention); a row of quick-pick chips below the input fills common amounts (1.000, 5.000, 10.000, 50.000 ARS in ARS-source mode; 5, 20, 50, 100 USD in USD-source mode). The headline result is computed at the Blue compra rate. Below, an "All rates / Comparación" strip shows the same input converted at all four rates: Blue compra (highlighted on Ice background as the primary), Blue venta, MEP, and Oficial; each row displays the rate itself in small DM Mono (`1 US$ = $1.250`) and the converted amount aligned right. A small bilingual footnote at the bottom of the sheet explains the spread: `compra` is what a cueva pays you per dollar; `venta` is what they charge when you're the buyer.
- **Pull-to-refresh.** Standard mobile gesture: pull down from the top of the Today tab to force-refresh all live data in parallel — weather (bypassing the 30-min TTL on `bap-today-cache`), dólar (same), the consolidated sheet data (bypassing the Apps Script's 1-hour `CacheService` entry via `?bust=1`), and (as of 2026-06-10l) **Places** (`refreshPlaces()` → the auth-script `?action=places` endpoint, which has no server cache, rewriting `bap-places-cache`). The Director can pull down on Today after editing the spreadsheet — including a row on the Roster `Places` tab — and see the change immediately, instead of having to hit the Apps Script URL with `?bust=1` in a separate browser tab. Before 2026-06-10l, Places was *not* part of this gesture (it had only a mount-only fetch + the 10-min cache), so a place edit could not be picked up in-app without a full reload; `refreshAllData` now fires the content and Places fetches together and awaits both, so the refresh indicator covers both. Touch handlers live on the `<TodayView>` root and walk up to the closest scrollable ancestor on first touch; the gesture only activates when that container is at `scrollTop === 0`. Raw touch delta is dampened by 0.55 for a weighted feel; visual pull is capped at 110 px; trigger threshold is 70 px on release. Indicator is a 28 px circular spinner (Fog ring with a Pep Blue top segment when triggered or refreshing) sitting in a slot above the greeting strip; the whole content block translates down with the pull and snaps back via a 280 ms cubic-bezier transition. The gesture is suppressed when either bottom sheet (Weather or Dólar) is open. `overscroll-behavior-y: contain` on the App-level content scroll container prevents the browser's own pull-to-refresh from firing alongside ours. Other tabs don't carry the gesture; the Today tab serves as the canonical "give me the freshest everything" entry point.
- **Active announcements.** The existing `<AnnouncementBanner>` renders here when an active announcement exists; otherwise nothing renders. Moved from Weekly Overview to Today.
- **Today's activity card.** Combines today's classes (filtered to today's day-of-week from `data.classes`) and today's calendar events (filtered via `eventOverlaps()` on `data.calendarEvents`, excluding semester-only). Sorted untimed-first, then by minutes-since-midnight. A "Próximo en X min" pill with the green pulse dot sits in the top-right of the card and auto-updates every minute (`setInterval` in `<TodayView>`). The next upcoming item is bolded and its time tinted Ocean; items already in the past dim to 0.55 opacity. Class location renders beneath each title.
- **Empty state.** When today has no items, the activity card flips to a "¡Día libre!" treatment: a 48 px mate gourd with two faint steam wisps animating up (`bap-steam`, 3.5 s ease-in-out infinite, with a 1.7 s delay on the second wisp), the line "Nada en agenda hoy. Date una vuelta; Buenos Aires te espera," and an "Explorar BA →" button that jumps to the Local tab. The button uses the `onJumpToTab` callback wired from the App component.
- **Esta semana / This Week in BA tile.** Surfaces the next 1-2 upcoming events from the Events sheet (today through +7 days). Each row shows a 30 px colored category circle with the category glyph, the title, and a date+time+neighborhood line. A "+N more this week" line appears at the bottom when more events are queued. The tile renders nothing when no events are upcoming, so weeks without curated content stay clean. Tapping the tile jumps to Local > This Week. Component: `<EventsTodayTile>`.
- **Tip card.** Rotates through entries from the Tips sheet (or the built-in `FALLBACK_TIPS` when the sheet is empty), 15-second rotation with a 320 ms cross-fade. Header reads "¿Sabías que…?" in DM Mono uppercase. Body uses `renderTip()` so `*italic*` markdown segments still render in EB Garamond italic.

**Live data sources used by Today:**

- Weather: `https://api.open-meteo.com/v1/forecast?latitude=-34.6037&longitude=-58.3816&current=temperature_2m,weather_code,is_day,wind_speed_10m&daily=temperature_2m_max,temperature_2m_min,weather_code,precipitation_probability_max,wind_gusts_10m_max&hourly=temperature_2m,weather_code,precipitation,precipitation_probability,wind_gusts_10m&forecast_days=7&timezone=America/Argentina/Buenos_Aires` — free, no API key, CORS-enabled. The `daily=` block now returns 7 days of high/low temps, daytime weather codes, max precipitation probability, and max wind gusts (each as a 7-element array, today first); these power the 7-day list in `<WeatherSheet>`. The `hourly=` block returns 48 hours from the current hour onward and powers both the impending-weather alert and the 12-hour strip in `<WeatherSheet>`.
- Dólar Blue: `https://dolarapi.com/v1/dolares/blue` — free, no API key, CORS-enabled.
- Dólar MEP (bolsa): `https://dolarapi.com/v1/dolares/bolsa` — same provider, same shape; secondary line on the dólar tile.
- Dólar Oficial: `https://dolarapi.com/v1/dolares/oficial` — same provider, same shape; tertiary line on the dólar tile, below MEP.

Both responses are cached in `localStorage` under the key `bap-today-cache` with a 30-minute TTL (`TODAY_CACHE_TTL = 30 * 60 * 1000`). The cache is keyed independently from the main sheet cache (`bap-app-cache`); failures in either fetch are silent and the previous cached value remains on screen until the next attempt. When the cache shape evolves (new fields added to weather or dólar), old cached values render gracefully — missing fields just don't show — and self-refresh on the existing 30-minute TTL, so most students see the new data within half an hour without any forced invalidation. As of 2026-04-27, `<TodayView>`'s effect also runs an explicit completeness check on the cached weather (`c.weather.daily && Array.isArray(c.weather.daily.time)`) and force-fetches when the daily block is absent, so the 7-day modal is never empty on first open after a deploy.

**Schedule tab:**

- Weekly Overview: scrollable week-by-week view of the semester. Each day card shows that day's calendar events (filtered via `eventOverlaps()`), the student's enrolled classes for that day-of-week (when personalization is on), and any finals scheduled for that date, all interleaved chronologically by start time. As of 2026-05-14 the three were merged into a single `dayItems` list sorted untimed-first then by minutes-since-midnight (matching `getTodayItems` on Today), so a 9 AM class now appears above a 3 PM excursion instead of always sitting below all events. Each entry tags its `kind` so the render keeps the kind-specific card styling — events keep their `EVENT_STYLES` type-driven bg/border, finals keep the Pep Orange "Final" pill, classes keep the thinner secondary row with the Fog left border. Classes are suppressed on any day with a holiday event that cancels classes. Empty days render the `<EmptyDay>` mate-gourd card. **Navigation is bounded** (as of 2026-05-11) to a tight personal window: one week prior, the current week (the default landing), and up to two weeks ahead. The chevron buttons render disabled (40 % opacity, stone color, `not-allowed` cursor) at the bounds and the click handler is a no-op so a stuck tap can't push past `MIN_WEEK_OFFSET` (-1) / `MAX_WEEK_OFFSET` (+2); `setWeekOffset` calls also wrap in `Math.max` / `Math.min` as a belt-and-suspenders clamp. The window is calibrated to "what's relevant to a student right now" — they don't need to browse the entire semester from this view (the Semester Calendar already does that), and capping the range keeps the navigation feeling intentional rather than open-ended. Today's day card is visually highlighted (Ice-tinted background, BAP Blue border, Pep Blue date circle, inline "TODAY" badge next to the weekday name) so students can spot it while scanning the week, but the previous "↓ TODAY" scroll-to-today button was removed on 2026-04-26 — the dedicated Today tab is now the canonical entry point for today-of-day context. **Auto-scrolls to today on open** (as of 2026-05-17): a `todayRef` attached to today's day card plus a `useEffect` keyed on `weekOffset` scrolls the card into view on mount and again whenever the student taps "← Back to This Week"; navigating to a neighboring week via the chevrons does NOT pull the student back to today because the effect gates on `weekOffset === 0`. The `scrollIntoView` call is wrapped in `requestAnimationFrame` so iOS Safari doesn't miss the target when the parent scroll container is still being sized. The `<TodayHero>` card that previously anchored this view was removed when the Today tab landed.
- Class Schedule (Week View): Shows classes grouped by day (Mon–Fri), sorted by start time for that specific day. Variable-schedule classes show only the relevant time slot per day.
- Courses (All Courses): Shows every course as a full card with complete schedule info. Professor names render as "Prof. María Castro" when `honorific` and `firstname` are both present in the sheet; otherwise the bare surname is used. When `email` is set, each card shows a tappable "Email Prof. Castro" button.

**Calendar tab:**

- Semester events grouped by month, filterable by type (milestone, academic, excursion, holiday, program, orientation).
- Color-coded event cards with date and day-of-week.
- Multi-day events display as a single card with a date range (e.g., "May 22–25") and day count (e.g., "3 days").
- The optional `visibility` column on each event controls where it appears: `both` (default) shows in both views, `week` limits to the Weekly Overview, and `semester` limits to the Semester Calendar.
- A thin BAP Blue divider with a centered DM Mono "Hoy · Today" pill renders before the first not-past event in the chronologically-sorted list, and every past event dims to opacity 0.55. "Not-past" is `(end_date || date) >= todayStr` so an ongoing multi-day event reads as current rather than past, and the divider lands before it rather than after the last fully-past event. The marker is driven by a `firstNotPastKey` `useMemo` in `CalendarView` keyed on `grouped + todayStr`; when no events are upcoming under the active filter, the key is `null` and no divider renders.
- **Auto-scrolls to the "Hoy · Today" divider on open** (as of 2026-05-17): a `todayDividerRef` attached to the divider element plus a `useEffect` keyed on `firstNotPastKey` scrolls it into view on mount and again whenever the active filter pill changes — a filtered list has its own first-not-past event, so re-anchoring keeps the upcoming-events part of the list in view rather than leaving the student looking at past events from a different filter. When `firstNotPastKey` is `null` (every event in the active filter is past) the effect no-ops and the student lands at the natural top of the list. The `scrollIntoView` call is wrapped in `requestAnimationFrame` so iOS Safari doesn't miss the target when the parent scroll container is still being sized.
- **Add-to-calendar (.ics) per event** (as of 2026-06-13): each event card carries a compact "Agendar" pill (`<AddToCalendarButton>`) that downloads a single-VEVENT `.ics` for that event — timed when `start_time` is set, otherwise an all-day event spanning `date`..`end_date`. See the add-to-calendar helper set in Helper Functions for the time model.

**Announcements:**

- Data-driven banner rendered on the Today tab between the quick-stats row and today's activity card.
- Two visual tiers: info (BAP Blue accent, `<MegaphoneIcon>` glyph) and urgent (Pep Orange accent, `<AlertIcon>` triangle glyph plus a small Pep Orange pulsing dot).
- Bilingual DM Mono label across both tiers: "Aviso / Notice" or "Importante / Important".
- Soft accent gradient stripe on the left edge replaces the hard left border of the previous design.
- Italic Spanish "Hasta el viernes" / "Hasta el 4 de mayo" gloss appears at the bottom when the announcement runs ≤21 days, so students see when it'll auto-disappear; suppressed when the end date is further out (the gloss would just be noise).
- Optional "Más info →" CTA pill in the accent color when `link` is set.
- Automatically activates and deactivates based on the `start_date`/`end_date` range in the Google Sheet.
- Not user-dismissible (per the 2026-04-26 redesign): the program office controls the banner's lifecycle entirely from the sheet, by editing the date range or removing the row.
- Renders nothing when no announcements are active.

**Local tab:**

- Opens to a **category hub** (added 2026-06-08): large, stacked, full-width buttons — **as of 2026-06-10j** four of them, in this order — This Week, **Places**, Healthcare, Churches (the fifth, **Apps**, was removed; see "Removed / dormant features"). Each has a glyph in a tinted Ice container, an EB Garamond English label + Roboto Spanish gloss, a 4 px left accent stripe in the category's color, and a → chevron. The hub previews no listings; tapping a button drills into that category's listing, and a "Volver / Back" header (DM Mono pill + the EB Garamond section title) returns to the hub. The hub replaced the previous two-row pill selector. It's driven by the module-scope `LOCAL_SECTIONS` config array (`{ key, en, es, Icon, accent }`) — single source of truth. Glyphs: `SparkleIcon` (This Week), `ObeliscoIcon` (Places), and the inline-SVGs `HealthCrossIcon` / `ChurchIcon` (`AppGridIcon` is no longer used by the hub but is retained — the Places category-picker grid's "All" tile still uses it).
- The **Places** sub-view (was "Explore BA"; renamed and reworked as **Places Stage 1**, 2026-06-08c) renders an approved, student-contributable BA directory. See the dedicated changelog row and the **Places** section of the operations guide for the full schema and the submit→vet→publish pipeline. Stage 1 ships the read path (categorized cards from the auth-side `?action=places` endpoint, ♥ saves, the reused "Cerca tuyo / Near you" distance sort); Stage 2 (2026-06-09c) adds the "+ Suggest a place" FAB → `<PlaceSubmitForm>` (a fixed four-field BottomSheet that POSTs `submit_place`, landing a `pending`/`community` row) → confirmation toast, plus a staff-only `<DirectorPlacesView>` vetting dashboard (reached by a second "Revisar lugares / Review places" button in Settings) that reads `?action=admin_places` and Approves/Rejects via `vet_place` with a per-place "show submitter's name" credit toggle. **Navigation (2026-06-09):** tapping Places opens a **two-column category-picker grid** (glyph tiles: All first, then all twelve categories sightseeing-first via `PLACE_CATEGORY_ORDER`, plus a ♥ Saved tile once the student has saved something); tapping a tile drills into that category's listing, and the App-level header reads **"Places"** (gloss suppressed) while in the section. The grid shows all twelve categories regardless of present data (the earlier present-gated filter-pill row is retired). A **search box on the hub grid** (via `<SearchInput>`, added 2026-06-10b) replaces the grid with a flat list of matches across all categories (name / why / neighborhood / category label) while the query is non-empty; the **♥ Saved tile shows its count** when > 0, and each `<PlaceCard>` carries a **share button** (Web Share API + clipboard fallback) beside the ♥ save toggle.
- Default landing is the hub. Deep-links from the Today tab open straight into a listing instead of the hub: the "Esta semana / This Week" events tile lands on the This Week listing, and the empty-state "Explorar BA →" button lands on the Explore BA listing. Implemented via `<LocalView>`'s `initialSub` prop, threaded from `<App>` through a `jumpToTab(tabKey, sub)` wrapper (passed to `<TodayView>` as `onJumpToTab`) and `localInitialSub` state; a normal Local-nav tap clears the flag so it defaults back to the hub.
- This Week sub-tab pulls from the Events sheet (see Tab Structure above). Past events auto-hide, remaining events are sorted chronologically and split into two groups with bilingual headers: "This week / Esta semana" (today through +7 days) and "Coming up / Próximamente." Filter pills above the list narrow by category and only appear when 2+ categories are present in the upcoming pool. Empty state: a friendly "Nothing curated yet" card pointing students to Explore BA for evergreen recommendations.
- Each event card has a 44 px colored category circle (color and glyph from `EVENT_CATEGORIES`), the title in EB Garamond, a date+time pill on the right, an optional description, venue + neighborhood line, address (tappable, opens Google Maps), an optional cost pill, and an external link button when `link` is set. The same pill style and address treatments are reused from elsewhere in the app for visual consistency.
- Filter pills above each sub-tab: This Week filters by event `category`, Healthcare filters by `type`, Churches by `denomination`, Explore BA by `type`. Pills only appear when there is more than one unique value to filter on. (The Apps sub-tab and its `category` filter were removed 2026-06-10j.)
- "Cerca / Near" distance sort (added 2026-06-07; toggle relabeled from "Cerca tuyo / Near you" to the more compact "📍 Cerca / Near" on 2026-06-10e) on the Explore BA, Healthcare, and Churches sub-tabs (and on Places listings): a toggle (shown only once a sub-tab has rows with `lat`/`lng` coordinates) re-sorts the list nearest-first using the phone's location and shows a small distance caption per located card. One permission prompt is shared across the tabs; toggling off restores the default order; declining the permission shows an inline note and keeps the default order. No map, on-device only, offline-safe. On a Places listing the pill sits on one line beside the List/Map toggle. **As of 2026-06-10m the three Places controls are icon-only** — a bulleted-list glyph (List), a folded-map glyph (Map), and a navigation-arrow glyph (Near) — each with a bilingual `aria-label`/`title`; the Health/Churches/Explore "Cerca / Near" pill keeps its text (a Places-only `nearMeIconButton` was added alongside the shared text `nearMeButton`).
- Health sub-tab distinguishes facilities (hospitals, clinics, pharmacies, labs, ISOS, imaging centers) from individual practitioners by rendering facility cards with an Ice-blue background. The distinction is auto-detected from `type` and can be overridden by the `category` column.
- When a Health provider has `insurance: bcbs`, a small BCBS/GeoBlue logo appears next to the provider name.
- Each card shows name, type/denomination/category, address, phone/hours, notes.
- Addresses are tappable and open Google Maps (see `AddressLink` below).
- Tappable link buttons (website, WhatsApp, Instagram) when a link is provided in the spreadsheet.

*(The Apps sub-tab's essentials-first sort and the "📱 iOS / 🤖 Android / 🌐 Website" store buttons were removed 2026-06-10j along with the sub-view; see "Removed / dormant features.")*

**Contacts tab:**

- Order (as of 2026-06-10k): **Emergency card first** (pinned to the top as the most time-critical), then the **Buenos Aires Program** office card, then Staff cards, Local Emergency Numbers (107/911/100), and optional Additional Resources.
- Office and staff cards surface Call, WhatsApp, Email, and Maps actions when the corresponding fields are populated. As of 2026-06-10k these are **icon-only glyph buttons** (no text labels) via `<ActionBtn>` — a telephone glyph to call, the WhatsApp mark, an envelope to email, a map pin for Maps — each carrying a bilingual `aria-label`+`title`. Two buttons keep their value visible beside the glyph because the data itself is worth showing: the **Emergency phone number** and the **office email address**.

**FAQ tab:**

- Expandable accordion cards (formerly the Policies tab).
- Optional "View full details" link per entry.
- A search box at the top (via `<SearchInput>`, added 2026-06-10b) filters entries on title + content, with a bilingual result count and empty state.
- Page title: "Frequently Asked Questions".

**Status indicator:**
The header shows a small badge indicating data source and freshness. The badge is rendered in the synced (blue) visual state for both `live` and `refreshing`; the muted (white-translucent) state covers everything else. In the synced state, a small mint-green dot appears beside the label and softly pulses (`@keyframes bap-pulse` in the injected stylesheet) to signal liveness; the dot disappears in non-synced states. Pulse animation is suppressed under `prefers-reduced-motion: reduce`.

**Personality Moves (cross-cutting):**

The app's chrome carries a small set of intentional personality moments layered on top of the institutional palette. Each move is reversible and respects accessibility constraints.

- **Editorial section titles.** Every main view (except Today, where the greeting strip is the page anchor) opens with a `<SectionTitle>` pairing an English headline in EB Garamond at 28 px with a small Spanish gloss in DM Mono uppercase below it (e.g., "Program Schedule" + "CRONOGRAMA"). Pulled from the `TAB_TITLES` constant.
- **Pulsing synced dot.** A 6×6 mint-green dot sits before the "Synced" label and softly throbs while live data is on screen. Implemented as the `.bap-pulse-dot` class via the injected `bap-personality-styles` block. Reused on the Today activity card's "Próximo" countdown pill.
- **Loading screen with rotating tips.** First-paint loading view (`<LoadingScreen>`) shows a spinning ring, a "Cargando…" label, and a card cycling through Argentine-flavored tips from the Tips sheet. New tip every 4 seconds with a 320 ms cross-fade. Falls back to a built-in 3-tip array when no Tips sheet exists.
- **Whimsical empty days.** In the Weekly Overview, any day with no events renders the `<EmptyDay>` card: a small mate-gourd SVG, "¡Día libre!" in italic EB Garamond, and "Nada en agenda hoy." beneath. The Today tab carries the same motif at a larger scale with steaming animation.
- **Press feedback on actions.** Buttons that drive real interactions (filter pills, bottom-nav tabs, FAQ accordion headers, class cards, Today's quick-stats tiles, Today's "Explorar BA" button) carry the `.bap-press` class, which scales them to 0.97× on tap.
- **Header constellation.** The header's faint top-right circle is replaced with `<SouthernCrossDecoration>`: five BAP Blue dots arranged in the Southern Cross pattern with thin connecting lines, set at 55 % opacity. Decorative; sits behind the logo + content row.
- **Animated bottom nav with per-tab color identity.** Each tab in the `TABS` array has its own `color` property: Today = BAP Blue, Schedule = Pep Blue, Calendar = Ocean, Local = Sky, FAQ = Mountain, Contacts = Pep Orange. The active tab's icon and label adopt that color and the icon lifts 3 px (`.bap-nav-icon.lifted`). A 44 × 4 px pill (`.bap-nav-pill`) slides between tabs via CSS transform, adopting the active tab's color. Pill positioning is computed in a `useEffect` that runs on tab change and on window resize, measuring the active button via a ref-keyed map (`navBtnRefs.current`). Local shifted from BAP Blue to Sky when Today claimed BAP Blue as its marquee color.
- **Dot-grid pattern fill on the Apps section header.** *(Dormant as of 2026-06-10j — the Apps sub-view was removed, so this surface no longer renders. The `.bap-dot-pattern` CSS remains available.)* A two-radial-gradient pattern (`.bap-dot-pattern`) at very low opacity behind the Apps section header gave the surface texture without affecting contrast on the type above it; can be reused on other section headers if texture becomes a recurring motif.
- **Apps grouped by transport vs daily life.** *(Dormant as of 2026-06-10j — removed with the Apps sub-view; the `<SectionDivider>` component and the `ColectivoIcon`/`PalmIcon` glyphs are retained.)* When the Apps filter was set to "All", apps split into "Getting around / Transporte" (matching `/navigation|transport|transit/i` on the category) and "Daily life / Día a día" (everything else), each preceded by a `<SectionDivider>` with a glyph (colectivo and palm respectively). When a single category filter was selected, the dividers disappeared and apps rendered in a flat list.

The CSS for these moves lives in a single `<style id="bap-personality-styles">` element injected once via `useEffect` in the App component. Idempotent (bails if already present); no new dependencies. All animations honor `prefers-reduced-motion: reduce`.

As of 2026-06-10 the labels are **Spanish-only** (the pill is narrow 10 px DM Mono; terse Spanish fits it and matches the app's Spanish-first posture), wrapped in `lang="es"` with `role="status"` + `aria-live="polite"` so the offline transition is announced with correct pronunciation.

| Status | Label | Meaning |
|--------|-------|---------|
| `live` | Sincronizado | Fresh data just loaded successfully |
| `refreshing` | Actualizando… | Cached data is on screen; background fetch in flight |
| `loading` | Cargando… | First-ever open, no cache yet, fetch in flight |
| `cached` | Guardado (offline) | Fetch failed but cached data is still available |
| `fallback` | Sin conexión | Fetch failed and no cache; hardcoded defaults shown |
| `default` | Vista previa | No SHEET_ID configured |

## Helper Functions

Key utility functions in `App.jsx`:

- **`loadCache()`** — synchronously reads and parses the localStorage payload at key `bap-app-cache`. Returns `{ data, timestamp }` if the cache is present and the version matches `CACHE_VERSION`; returns `null` otherwise (missing, malformed, or stale version). Called during `useState` lazy initialization to make first paint use cached content.
- **`saveCache(data)`** — writes the full normalized data object to localStorage with the current `CACHE_VERSION` and timestamp. Wrapped in try/catch so quota-exceeded errors or disabled storage are silently ignored.
- **`parseDays(raw)`** — normalizes the `days` field from either comma-separated abbreviations (`"Mon, Tue, Thu"`) or a concatenated single-letter string (`"MTR"`) into an array of three-letter day names. Uses `DAY_LETTER_MAP = { M: "Mon", T: "Tue", W: "Wed", R: "Thu", F: "Fri" }`.
- **`toMinutes(timeStr)`** — parses a time string (`"8:00"`, `"08:00"`, `"8:00:00"`, `"8:00 AM"`, `"20:30"`) into minutes-since-midnight. Returns `null` for empty or unparseable input. Used by the Weekly Overview to sort events chronologically within a day; a direct `localeCompare` on raw time strings would incorrectly put `9:00` after `10:00`.
- **`eventOverlaps(event, rangeStart, rangeEnd)`** — returns `true` if an event's date range overlaps with a given window. Used by the Weekly Overview to catch multi-day events that span into the visible week.
- **`dateRangeLabel(startDate, endDate)`** — produces a compact label like "May 22–25" (same month) or "May 30 – Jun 2" (cross-month).
- **`countDays(startDate, endDate)`** — returns the number of days in a range, inclusive.
- **Add-to-calendar (.ics) helper set** (added 2026-06-13) — generates and downloads a standards-compliant single-VEVENT `.ics` for Calendar-tab events and assigned finals. `buildEventIcs({ uid, title, description, location, allDay, startDateStr, endDateStr, startMin, endMin })` assembles the `VCALENDAR`/`VEVENT` string (CRLF line endings, RFC5545 text escaping via `icsEscape`). Time model: Buenos Aires is a constant GMT-3 (no DST), so timed events convert local→UTC via `icsUtcFromBAMinutes(dateStr, minutes)` (`Date.UTC(y, m-1, d, hh+3, mm)`, which also absorbs hour/day rollover) and emit `DTSTART:…Z` / `DTEND:…Z`; all-day events emit `DTSTART;VALUE=DATE` with an **exclusive** `DTEND` computed by `nextDayStr` (last day + 1). Supporting helpers: `pad2`, `icsEscape`, `icsUtcStamp` (a `Date` → `YYYYMMDDTHHMMSSZ`), `parseYMD`, `icsDate` (strip dashes), `slugifyForFilename`, and `downloadIcs(filename, ics)` (Blob → `URL.createObjectURL` → synthetic `<a download>` click → revoke). Two mappers wrap `buildEventIcs`: `downloadCalendarEventIcs(e)` (timed when `e.start_time` is set, end = `e.end_time` or +1 h; else all-day spanning `e.date`..`e.end_date`) and `downloadFinalIcs(f)` (title `Final · {code} {title}`, `f.location`; parses `f.final_time` by splitting on en-dash/hyphen through `toMinutes`, end = second part or +1 h; blank/unparseable → all-day on `f.final_date`). Stable per-item `UID` via the existing `djb2` hash. No new dependency — pure browser APIs. *Open caveat:* iOS Safari's `.ics` download/handoff into the Calendar app on an installed PWA wants a real-device check.
- **`<CalendarPlusIcon size color />`** — small inline-SVG calendar-with-a-plus glyph (added 2026-06-13) used inside `<AddToCalendarButton>`.
- **`<AddToCalendarButton onClick />`** — the compact "Agendar" pill (DM Mono, Ocean accent, `bap-press`, `aria-label`/`title` "Agendar / Add to calendar", `stopPropagation` on click). Rendered right-aligned inside each `<CalendarView>` event card and each assigned `<FinalsCard>` row; its `onClick` fires the matching `download…Ics` mapper.
- **`EVENT_CATEGORIES`** — constant config mapping each Events category key (`music`, `theater`, `film`, `exhibit`, `dance`, `festival`, `food`, `talk`, `other`) to `{ label, es, color, Icon }`. The `color` is a hex from the BAP palette and the `Icon` is a category glyph component that accepts a `color` prop. Single source of truth for category visuals; adding a new category here also surfaces it as a filter pill automatically.
- **`getEventCategory(key)`** — safe lookup against `EVENT_CATEGORIES`, falling back to the `other` entry when the sheet contains a category Claude doesn't recognize. Prevents crashes when the Director introduces tentative new categories.
- **`filterUpcomingEvents(events, todayStr)`** — filters out past events. Honors `end_date` for multi-day runs; single-day events are kept on their date.
- **`sortEventsChronological(events)`** — sorts by `start_date` then by `time` within the same day. Untimed events lead the day so all-day rows come first.
- **`getThisWeekEvents(data)`** — returns events whose date range intersects today through today+7 days, already sorted chronologically. Used by both `<EventsTodayTile>` (preview on Today) and `<EventsView>` (group split in Local).
- **`eventDateLabel(event)`** — compact date label for an event card. Single-day events render as `"Mon, May 4"`; multi-day runs reuse `dateRangeLabel()` for `"May 22–25"` or `"May 30 – Jun 2"`.
- **`LOCAL_SECTIONS`** — module-scope config array of `{ key, en, es, Icon, accent }` for the Local-tab category-hub buttons. **As of 2026-06-10j four entries** (This Week, Places, Healthcare, Churches); the fifth, Apps, was removed (a commented-out placeholder marks where its entry was, so resurrection is a one-line re-add — see "Removed / dormant features"). Single source of truth for the hub's order, labels, glyphs, and accent colors. Icon components are hoisted function declarations, so referencing them above their definitions is fine.
- **`<LocalView data initialSub />`** — the Local tab. When `sub` is `null` it renders the category hub (one stacked button per `LOCAL_SECTIONS` entry); when `sub` is set it renders a "Volver / Back" header (`setSub(null)` returns to the hub) above that category's listing. `initialSub` (default `null` → hub) lets Today's deep-links land directly on a listing; `<App>` supplies it via `localInitialSub` state set by the `jumpToTab(tabKey, sub)` wrapper and cleared on a normal Local-nav tap. A separate `resetSignal` prop (a counter bumped by `<App>` only when the Local nav tab is tapped while already on Local) drives a `useEffect` that collapses `sub` + `placesFilter` back to the hub, so re-tapping Local is a reliable "back to the top" gesture even from deep inside a Places category. **As of 2026-06-10i** that effect compares the `resetSignal` value against the last-seen value (a `lastResetSignal` ref) rather than a boolean "skip first run" guard, so mount runs are no-ops (a Today deep-link survives) and it stays correct under React StrictMode's double-effect-invoke. All listing sub-views (`<EventsView>`, the health/churches/places cards, the "Cerca / Near" sort) are unchanged by the hub refactor.
- **`<HealthCrossIcon>` / `<ChurchIcon>` / `<AppGridIcon>`** — three inline-SVG glyphs (64×64 viewBox, `size` + `color` props) added 2026-06-08 for the Healthcare, Churches, and Apps hub buttons. Join the existing glyph library alongside `<ObeliscoIcon>` (Explore BA) and `<SparkleIcon>` (This Week).
- **`<EventsView events activeFilter onFilterChange categoriesPresent />`** — the "This Week" sub-tab content under Local. Filters out past events, applies the active category filter, and splits the remaining list into `This week` (today to +7d) and `Coming up` (later) groups. Renders a "Nothing curated yet" empty state when no events are upcoming.
- **`<EventCard event />`** — the per-event card. Colored category circle, title, date pill, description, venue + neighborhood, address (via `<AddressLink>`), cost pill, and external link button (via `<LinkButton>`). All optional fields render only when present.
- **`<EventsTodayTile data onJumpToTab />`** — the Today dashboard preview tile for upcoming events. Shows the next 1-2 events with a 30 px category circle and a date+time+neighborhood line. Renders `null` when no events are upcoming this week, so the Today tab stays clean on quiet weeks. Tapping the tile calls `onJumpToTab("local", "events")`, which routes to the Local tab and deep-links straight into the "This Week" listing (via `<LocalView>`'s `initialSub` prop) rather than the category hub.
- **`isFacility(provider)`** — returns `true` when a health provider is a facility rather than an individual. Honors the `category` column if set; otherwise matches the `type` field against `hospital|clinic|clínica|sanatorio|laboratory|lab|pharmacy|farmacia|emergency|isos|imaging|diagnóstico`. Facility cards get an Ice-blue background.
- **`parseCoord(v)` / `haversineKm(lat1,lng1,lat2,lng2)` / `formatDistance(km)` / `getUserLocation()` / `sortByDistance(list,userLoc)`** — the "Cerca tuyo / Near you" distance-sort helper set (added 2026-06-07; powers Explore BA, Healthcare, and Churches on the Local tab). `parseCoord` normalizes a sheet cell to a float or `null` (used by `normalizeData` for the optional `lat`/`lng` columns). `haversineKm` is the great-circle distance in km. `formatDistance` renders a language-neutral `~600 m` / `~1,4 km` label (Spanish decimal comma, sub-1km rounded to nearest 10 m). `getUserLocation` wraps `navigator.geolocation.getCurrentPosition` in a Promise (low accuracy, 8 s timeout, 5-min cached fix) and rejects with a typed `{reason: "denied" | "unavailable"}`. `sortByDistance` returns a stable nearest-first copy with coordless rows sunk to the bottom in original order. All consumed by `<LocalView>`'s shared `nearMe`/`userLoc`/`geoStatus` state, the `handleNearMe` toggle handler, the `nearMeControl` element (the "Cerca / Near" button + permission note, used by Healthcare / Churches / Explore when `anyCoords(list)`), and the per-row `distanceCaption(row)` closure. As of 2026-06-10e `nearMeControl` is composed from two smaller pieces — `nearMeButton` (the pill) and `nearMeNote` (the inline denied/unavailable note) — so a Places listing can drop `nearMeButton` into its Lista/Mapa toggle row (all three pills on one line) and render `nearMeNote` below. No map, no dependency; geolocation requires a secure context (`localhost` or production HTTPS).
- **Link / action glyphs** (added 2026-06-10k) — `<PhoneGlyph>`, `<WhatsAppGlyph>`, `<EnvelopeGlyph>`, `<MapPinGlyph>`, `<GlobeGlyph>`, `<InstagramGlyph>`: small inline-SVG icons (each `{ size, color }`) used to label contact + link buttons **without words**. They replaced the previous emoji (`📞 💬 ✉ 📍 📷 →`). Any button rendering one of these icon-only must carry an `aria-label` + `title` (the convention every caller follows). `WhatsAppGlyph` is the green WhatsApp mark; `InstagramGlyph` the camera outline; the rest are Feather-style line/fill icons.
- **`<ActionBtn href Glyph label value variant />`** — the Contacts/Resources action pill. Renders `Glyph` (one of the link glyphs, colored to the variant) icon-first with a ≥40 px tap target and `aria-label`+`title` set from the bilingual `label` (the button is otherwise wordless). Optional `value` shows visible text beside the glyph — used only where the data is worth surfacing (the Emergency phone number, the office email). `variant` (`phone`/`whatsapp`/`email`/`maps`/`emergency`) drives the bg/color/border. Scheme-guards `href` through `safeExternalUrl`.
- **`<LinkButton url />`** — the external-link pill used across the Local sub-views (events, places, health, churches). As of 2026-06-10k it is **icon-only**: it sniffs the URL and renders `GlobeGlyph` (generic website), `WhatsAppGlyph` (`wa.me`), `InstagramGlyph` (`instagram.com`), or `PhoneGlyph` (`tel:`), with the descriptive text in `aria-label`+`title`. Pep-Orange pill styling; the WhatsApp/Instagram marks render in that same orange (uniform Local link-pill color) rather than their brand colors.
- **`<AddressLink address mapsUrl />`** — renders an address as a tappable link prefixed with the `<MapPinGlyph>` SVG pin (was a 📍 emoji before 2026-06-10k). When `mapsUrl` is provided (e.g., from the `maps` column in the Contacts tab) it is used as the `href`; otherwise the address is URL-encoded into `https://www.google.com/maps/search/?api=1&query=...`. On mobile this opens the native Google Maps app when installed; on desktop it opens maps.google.com. Used in HealthView, ChurchesView, ExploreView/PlaceCard, and the Office card of ContactsView.
- **`<LocationNote note />`** — renders non-address location info as plain italic gray text with no pin and no link. Used for the `location_note` column in Health, Churches, and Explore. Companion to `AddressLink`; both may appear on the same card, with the address linked first and the note below as unlinked context.
- **`renderTip(text)`** — small inline parser that renders a tip string with markdown-style `*italic*` segments. Splits the input on asterisks and alternates between regular `<span>` text and italic EB Garamond `<em>` elements. Safe by construction (no `dangerouslySetInnerHTML`, no HTML evaluation). Used by `LoadingScreen` to render tips from the Tips sheet.
- **`<MateGourdIcon size />`** — inline SVG of a mate gourd with bombilla. Six paths, BAP Blue body, mountain-color details, soft white highlight stroke. Used as the visual anchor of the empty-day card in the Weekly Overview. Inherits brand color tokens directly so a future palette tweak propagates automatically.
- **`<EmptyDay />`** — the whimsical empty-state card shown in each empty day row of the Weekly Overview. Pairs a small mate gourd with the headline "¡Día libre!" and the subtitle "Nada en agenda hoy." One uniform treatment for all empty days; no special-casing of "today."
- **`<SectionTitle tabKey />`** — the bilingual section heading at the top of every main view. Renders the English title in EB Garamond at 28 px, with a small Argentine-Spanish gloss below in DM Mono uppercase (e.g., "Program Schedule" + "CRONOGRAMA"). Pulls strings from the `TAB_TITLES` constant; adding a new tab requires both a `TABS` entry and a `TAB_TITLES` entry.
- **`<LoadingScreen tips />`** — the first-paint loading view shown only when there is no cache and no SHEET_ID is missing. A spinning ring, "Cargando…", and a card showing one rotating tip every 4 seconds with a 320 ms cross-fade. Reads from `data.tips`; falls back to a built-in 3-tip array (`FALLBACK_TIPS`) when the Tips sheet is empty or missing. Animations honor `prefers-reduced-motion`.
- **`TAB_TITLES`** — constant object mapping each tab key to `{ en, es }` strings used by `SectionTitle`. Single source of truth for the bilingual headlines.
- **`<TodayView data onJumpToTab profile />`** — the Today tab itself. Greeting strip, weather + dólar quick-stats row, active announcements, today's activity card with a live "Próximo en X min" countdown, and a rotating tip card. Drives a one-minute clock tick (`setInterval`) so the countdown stays accurate without keeping the user's tab open re-rendering everything. Receives `setTab` as `onJumpToTab` so the empty-state "Explorar BA →" button can route to the Local tab. Reads weather and dólar from a separate localStorage cache (`bap-today-cache`) with a 30-minute TTL.
- **`getGreeting(hour)` / `getGreetingGradient(hour)` / `getDressHint(temp, code)` / `formatPesos(n)` / `formatCountdown(targetMin, nowMin)`** — small pure functions used by `TodayView` for the greeting copy, time-of-day gradient, bilingual dress hint based on temperature and weather code, peso formatting (`$1.250` with Spanish-locale separators), and the human-readable countdown to the next item.
- **`fetchWeather()` / `fetchDolar()` / `cToF(c)`** — async fetches against Open-Meteo (Buenos Aires lat/lon hardcoded as `BA_LAT = -34.6037` and `BA_LON = -58.3816`) and dolarapi.com. No API keys; both endpoints are CORS-enabled. `fetchWeather()` pulls `current=temperature_2m,weather_code,is_day,wind_speed_10m`, a 7-day `daily=temperature_2m_max,temperature_2m_min,weather_code,precipitation_probability_max,wind_gusts_10m_max` block, and a 48-hour `hourly=` block (temp, weather_code, precipitation, precip prob, wind gusts), and returns `{ temp, code, isDay, tempMax, tempMin, hourly: { time, temp, code, precip, precipProb, windGust }, daily: { time, tempMax, tempMin, code, precipProbMax, windGustMax }, ts }` with all temps in Celsius. The `hourly` block is sliced to start at the current hour (`startIdx` computed via ISO-prefix comparison) so the next-12-hour and weather-alert paths both see "now-aware" data. `fetchDolar()` pulls Blue (`/v1/dolares/blue`), MEP (`/v1/dolares/bolsa`), and Oficial (`/v1/dolares/oficial`) in parallel via `Promise.allSettled` and returns `{ venta, compra, mep, oficial, ts }`; only throws when Blue itself fails, so a failed MEP or Oficial call still leaves the others on screen. `cToF(c)` is a tiny pure helper that rounds `(c × 9/5) + 32`; used at display time so the underlying Celsius values are preserved for the bilingual dress hint thresholds.
- **`loadTodayCache()` / `saveTodayCache(payload)`** — read and write the `bap-today-cache` localStorage entry. Stores `{ weather: { temp, code, isDay, tempMax, tempMin, hourly, daily, ts }, dolar: { venta, compra, mep, oficial, ts } }`. `TODAY_CACHE_TTL = 30 * 60 * 1000` is checked per-entry, so weather and dólar refresh independently if one falls stale before the other. Wrapped in try/catch so quota errors are silently ignored. Old cached values missing newer fields (e.g., `tempMax`, `tempMin`, `mep`, `oficial`) render gracefully and self-refresh within half an hour; no version bump is needed when fields are added. The 2026-04-27 `daily` addition is also guarded by an explicit completeness check in `<TodayView>`'s effect so the 7-day modal works immediately after a deploy without waiting for the TTL to lapse.
- **`getWeatherLabel(code)` / `formatHourLabel(iso, isFirst)` / `getShortDayLabel(iso, today)` / `formatUsd(n)`** — small helpers used by `<WeatherSheet>` and `<DolarSheet>`. `getWeatherLabel` maps a WMO code to a `{ es, en }` short condition descriptor that mirrors the state grouping in `<WeatherIcon>` so icon and label always agree. `formatHourLabel` returns `"Ahora"` for the first slot in the hourly strip and `"14h"`/`"15h"` for subsequent hours (Argentine 24-hour convention). `getShortDayLabel` returns a `{ es, en }` weekday label from an Open-Meteo daily ISO string, with `Hoy / Today` for the current day. `formatUsd` renders dollar amounts with two decimals under $100 and rounded thousands-separated whole dollars otherwise.
- **`prefersReducedMotion()` / `useReducedMotion()`** — reduced-motion gate (added 2026-06-13). `prefersReducedMotion()` reads `matchMedia('(prefers-reduced-motion: reduce)').matches`; `useReducedMotion()` is the hook form, returning that boolean and subscribing to the media-query's `change` event (with the legacy `addListener` fallback) so it updates live. It exists because the CSS `@media (prefers-reduced-motion: reduce)` block can't reach **inline** `transition:` styles — components that animate via inline styles call the hook and set `transition: reduced ? "none" : "<value>"`. Consumed by `<BottomSheet>` (slide + backdrop fade), `<FaqView>` (accordion chevron), `<TodayFinalsTile>` and `<FinalsCard>` (collapsible chevrons), and `<ProfileModal>` (settings toggle thumb). The class-based animations were already gated in the CSS block; this closes the inline-style remainder (Tier 4 #14).
- **`relLuminance(hex)` / `discGlyphColor(fillHex)`** — category-glyph contrast rule (added 2026-06-13b, Tier 4 #13). `relLuminance` is the WCAG relative luminance of a `#rrggbb` hex. `discGlyphColor` returns the ink color for a glyph rendered on a category disc: `C.white` when white-on-fill ≥ 3.5:1 (dark fills — `pepOrange`/`ocean`/`mountain`/`pepBlue`), else `C.pepBlue` (navy) for light fills (`bapBlue`/`sky`/`stone`), so the glyph always clears the WCAG 1.4.11 non-text 3:1 bar (navy-on-light lands at 5.1–7.0:1). The disc *fill* stays the category's brand color; only the glyph ink flips. Applied at all five category-disc render sites (`<PlaceCard>`, `<PlaceSubmitForm>`, the `<LocalView>` category-picker grid, `<EventCard>`, `<EventsTodayTile>`) and threaded to the Places map pins via a `_glyphInk` field on the enriched map-place objects (`PlacesMap.jsx` reads `p._glyphInk || "#fff"`). Companion change (same ship): category caption *text* moved from the category color to neutral `C.mountain` (clears the 4.5:1 text bar) at `<EventCard>`, `<PlaceCard>`, and `<DirectorPlacesView>`, since the colored disc already carries the category cue.
- **`useDialogA11y(ref, { open, onClose })`** — shared dialog-accessibility hook (added 2026-06-10) used by every overlay surface so focus management lives in one place instead of four copies. On open it stashes the element that had focus, moves focus into the dialog (`ref` points at the content container, made focusable via `tabIndex={-1}`), traps Tab/Shift+Tab inside it, and on close restores focus to the trigger. A module-level **`__dialogStack`** tracks nesting order so a single global `keydown` listener closes only the *topmost* dialog on Escape — without it, a `<ConfirmDialog>` stacked over `<ProfileModal>` would close both at once. Consumed by `<BottomSheet>`, `<ProfileModal>`, `<ConfirmDialog>`, `<DirectorResponsesView>`, and `<DirectorPlacesView>`. Companion helpers: `getDialogFocusable(node)` (visible-focusables query) and the `DIALOG_FOCUSABLE_SELECTOR` constant.
- **`<ConfirmDialog open onConfirm onCancel titleEs titleEn bodyEs bodyEn confirmEs confirmEn />`** — centered confirm card (added 2026-06-10) replacing native `window.confirm` for destructive actions. Portals to `<body>`, `role="dialog"` + `aria-modal`, bilingual ES-primary title/body (`lang="es"`), a Pep-Orange destructive confirm button and a neutral cancel. Uses `useDialogA11y` for the focus-trap/Escape/focus-return baseline (focus defaults to the first focusable, i.e. Cancel — the safe choice on a destructive prompt). `zIndex: 300` so it floats above `<ProfileModal>` (200). Currently wired to ProfileModal's profile-reset and sign-out flows via a `pendingConfirm` state.
- **`<BottomSheet open onClose titleEs titleEn children />`** — generic slide-up bottom-sheet modal used by `<WeatherSheet>` and `<DolarSheet>`. Mounts the backdrop and sheet on `open=true`, animates in with a backdrop fade and a 280 ms `cubic-bezier(0.4, 0, 0.2, 1)` translateY, locks `document.body.style.overflow` while open to prevent iOS scroll-bounce behind the backdrop (ref-counted so stacked sheets restore in order), and unmounts after the slide-down completes. `role="dialog"` + `aria-modal`; as of 2026-06-10 it also uses `useDialogA11y` for focus-trap + Escape-to-close + focus-return. Backdrop tap closes; a × button in the header (Ice background, Fog border) also closes. Header pairs an EB Garamond English headline with a DM Mono Spanish gloss in the same pattern as `<SectionTitle>`. Body scrolls internally with `WebkitOverflowScrolling: "touch"`. Distinct from `<ProfileModal>`, which is full-screen and styled as a dedicated settings page; `<BottomSheet>` is a lighter content-detail sheet.
- **`<WeatherSheet open onClose weather />`** — detail view for the Today weather tile. Renders a 12-hour horizontal strip from `weather.hourly` (each card: time label, day/night-aware `<WeatherIcon>`, Fahrenheit temp, optional rain prob ≥40 %) and a 7-day daily list from `weather.daily` (each row: weekday label, daytime `<WeatherIcon>`, bilingual condition label from `getWeatherLabel`, optional rain prob ≥25 % and wind gust ≥20 mph shown as compact DM Mono captions, and high/low temps). Wind gusts are converted from Open-Meteo's km/h to mph at display time via `kmhToMph(kmh)` (the underlying value stays in km/h in the cached weather payload, so no version bump is needed). Renders a placeholder line when the daily block hasn't loaded yet. No additional fetch — the existing weather object carries everything.
- **`kmhToMph(kmh)`** — pure converter, returns `Math.round(kmh * 0.621371)` or `null` for non-numeric input. Used only for display; the cached weather payload keeps the raw km/h values from Open-Meteo so we can change the threshold or units later without rerouting fetches.
- **`isWeatherStale(weather)` / `WEATHER_STALE_MS`** — staleness gate for the Today weather tile. `WEATHER_STALE_MS = 6 * 60 * 60 * 1000` (six hours). Returns `true` when the cached weather payload's `ts` is older than that. When stale, the weather tile dims and a tap fires `refetchWeatherForStaleTap` (a foreground re-fetch) instead of opening `<WeatherSheet>` directly; on success the tile ungrays and the modal opens, on failure it stays grayed.
- **`isDolarStale(dolar)` / `DOLAR_STALE_MS`** — same pattern for the Today dólar tile, at the tighter `DOLAR_STALE_MS = 3 * 60 * 60 * 1000` (three hours) threshold because Blue/MEP/Oficial can drift 5–10 % over a single trading day, making a stale rate more misleading at the calculator than a stale weather card. When stale, the dólar tile dims and a tap fires `refetchDolarForStaleTap` (a foreground re-fetch) instead of opening `<DolarSheet>`; on success the tile ungrays and the modal opens, on failure it stays grayed. `dolarRefetching` state in `<TodayView>` is tracked separately from `weatherRefetching` so a refetch on one tile doesn't dim the unrelated one.
- **`parseArsAmount(costStr)`** — pulls an Argentine-peso integer out of a free-text cost string. Used by `<EventCard>` to append a USD parenthetical to event prices (e.g. `$8.000 ARS · ~$6 USD`). Skips strings that already cite USD/US$/U$S so amounts already in dollars aren't double-converted. Honors the Argentine `.` thousands convention: `$8.000` → 8000. Rejects sub-10-peso amounts so a comma-decimal misparse like `$5,50` (which the regex truncates to `$5`) doesn't render a meaningless `~$0.00 USD` annotation. Returns `null` when no parse is possible.
- **Tile dim/offline/stale states** — both Today tiles (weather and dólar) accept a `dimmed` parameter on `statTile()` that lowers opacity to 55 % and switches the background from white to Ice. Triggers feed `dimmed` per tile: `isRefreshing` (during pull-to-refresh) and `!isOnline` apply to both tiles symmetrically; the weather tile additionally dims on `isWeatherStale(weather)` or `weatherRefetching`; the dólar tile additionally dims on `isDolarStale(dolar)` or `dolarRefetching`. While `dimmed` is true, taps that would normally open the sheet are suppressed. The exception is the stale-tile case (weather or dólar): `dimmed` is true but `onClick` is wired to the matching `refetch…ForStaleTap` handler, so the tile reads as "waiting" rather than "interactive" but still does something useful when tapped — on success the tile ungrays and the corresponding sheet opens; on failure it stays dimmed. `isOnline` is initialized from `navigator.onLine !== false` (lenient default for environments where the property is unavailable) and updated via `useEffect`-attached `online` / `offline` listeners that clean up on unmount.
- **`isClassActive(c, dateStr)` / `filterActiveClassesForDate(classes, dateStr)`** — class date-gating helpers (added 2026-04-28). `isClassActive` returns `true` when `dateStr` is within `[c.start_date, c.end_date]` (inclusive); blank bounds are treated as "no gate on that side." `filterActiveClassesForDate` is a thin `Array.filter` wrapper. Used by the Today tab's activity card and the Schedule tab's Weekly Overview to suppress class meetings outside the regular meeting window. Class Schedule (Mon–Fri grid) and Courses (alphabetical list) bypass these helpers because they're catalogs.
- **`getStudentFinals(data, profile)` / `getFinalForDate(data, profile, dateStr)` / `shouldShowFinalsUI(data, profile, today)` / `daysUntil(targetDateStr, today)` / `formatFinalDate(dateStr)` / `formatFinalsWindow(startStr, endStr)`** — the finals helper set (added 2026-04-28). `getStudentFinals` returns the personalized class list with `final_date` and `final_time` projected onto each row, sorted by `final_date+final_time` (assigned first, TBD last by `code`). `getFinalForDate` filters that list to a single date for the Today-tab class-replacement logic. `shouldShowFinalsUI` is the gate that controls whether `<TodayFinalsTile>` and `<FinalsCard>` render: true when the student has personalized AND either we're within 14 days of `data.finals_window_start` OR at least one enrolled class has a `final_date` populated. `daysUntil` computes day offsets in BA-local terms via `T12:00:00` anchoring. `formatFinalDate` produces a `"Sat, Aug 29"` label for assigned dates. `formatFinalsWindow` renders the program-wide window as either `"Aug 29"` (single day) or `"Aug 29–31"` / `"Jul 30 – Aug 1"` (multi-day, same-month or cross-month).
- **`<TodayFinalsTile data profile now onJumpToTab />`** — Today-dashboard tile that mirrors `<EventsTodayTile>`'s shape but with a Pep Blue accent for academic context. Returns `null` unless `shouldShowFinalsUI(data, profile, now)`. Shows the first 3 enrolled-class finals as colored-stripe rows with `code · title` and either `formatFinalDate · final_time` or `TBD · {finals window}`. Tap routes to the Schedule tab via `onJumpToTab("schedule")`, where the fuller `<FinalsCard>` is the primary anchor. Inserted in `<TodayView>`'s render between `activityCard` and `<EventsTodayTile>`.
- **`<FinalsCard data profile today />`** — Pinned at the top of the Schedule tab, above all three sub-pills (Weekly Overview, Class Schedule, Courses). Returns `null` unless `shouldShowFinalsUI`. Same data as `<TodayFinalsTile>` but laid out as a stationary, more-detailed reference: each row shows `code · title`, the assigned `final_date · final_time` or a TBD pill, plus the `location` underneath when present. Each row carries a 4 px left stripe in the class's `color` so it visually pairs with the Class Schedule cards below. Renders the program-wide finals window as a small Ocean pill in the top-right of the header. As of 2026-06-13, each row with an **assigned** `final_date` also carries an "Agendar" pill (`<AddToCalendarButton>`) that downloads a single-VEVENT `.ics` for that exam (timed when `final_time` parses, else all-day); TBD rows omit it (no date to add).
- **`getTodayItems(data, profile)`** — combines today's classes, today's finals, and today's calendar events. Class meetings are gated by (a) `shouldFilterClasses(profile)`, (b) the holiday-cancels-classes check from `findHolidayContext()`, (c) `isClassActive(c, todayStr)` so out-of-range courses are dropped, and (d) `todaysFinals.length === 0` so a final-exam day replaces the regular meeting entirely. Today's finals come from `getFinalForDate(data, profile, todayStr)` and are emitted as `kind: "final"` items, which the activity card renders with a small Pep Orange "Final" pill prefixed to the title. All three lists are merged and sorted untimed-first then by `sortMin`.
- **`<DolarSheet open onClose dolar />`** — currency calculator for the Today dólar tile. Direction toggle (default ARS → USD), sanitized decimal input (commas auto-normalized to dots), quick-pick chips (1.000/5.000/10.000/50.000 ARS or 5/20/50/100 USD depending on direction), primary result computed at Blue compra, comparison strip showing the same input converted at all four rates (compra highlighted on Ice as the primary), and a small bilingual footnote explaining the compra/venta spread. Pure client-side math; no fetch. Reuses `formatPesos` for ARS output and the new `formatUsd` for USD output.
- **`formatSpanishDate(d)`** — formats a `Date` as Argentine Spanish: capitalized weekday, numeric day, lowercase month, "de" connector. Example: "Lunes, 28 de abril". Driven by the `SPANISH_WEEKDAYS` and `SPANISH_MONTHS` constants.
- **`<SunIcon />`, `<MoonIcon />`, `<WeatherIcon code isDay />`, `<ColectivoIcon />`, `<ObeliscoIcon />`, `<TangoShoeIcon color />`, `<PalmIcon />`, `<RioWaveIcon />`, `<MusicNoteIcon color />`, `<TheaterMaskIcon color />`, `<FilmReelIcon color />`, `<PictureFrameIcon color />`, `<SparkleIcon color />`, `<ForkPlateIcon color />`, `<MicrophoneIcon color />`, `<PinIcon color />`, `<HandsHeartIcon color />`** — inline SVG glyphs in the BAP palette, alongside the existing `<MateGourdIcon />`. All accept a `size` prop (default 36 px for the standalone glyphs, 30 px for `WeatherIcon`). Glyphs used in Events also accept a `color` prop so the rendered shape matches the category accent. `<WeatherIcon>` picks one of six states (clear, partly cloudy, cloudy, rain, snow, thunder) keyed off the WMO weather code, with day/night variants for the first two states. Currently in active use: sun and moon (Today greeting strip), weather glyph (Today quick-stats), mate (Weekly Overview empty days and Today empty state), palm (the Places `outdoors` category), río wave (Today tip card watermark), and the ten event-category glyphs (music note, theater mask, film reel, picture frame, tango shoe, sparkle, fork-and-plate, microphone, hands cradling heart, pin) on the This Week sub-tab and the Today events tile. Defined-but-unused: obelisco, and (as of 2026-06-10j) `colectivo` — formerly the Apps "Getting around" divider glyph, now dormant with the removed Apps view; both available as a one-line drop for a future view.
- **`<SouthernCrossDecoration />`** — the header decoration that replaces the previous faint circle. Five BAP Blue dots in the Southern Cross arrangement at 55 % opacity, with thin 35 %-opacity connecting lines. Absolutely positioned in the top-right of the header gradient. Decorative only; sits behind the logo + content row.
- **`<SectionDivider icon en es />`** — bilingual section header used inside grouped lists. A 36 × 36 Ice-fill rounded container holding a glyph, paired with an English serif headline and a Spanish DM Mono gloss. *(As of 2026-06-10j it has no live callers — it formerly broke the Apps view into "Getting around / Transporte" and "Daily life / Día a día" groups; retained for a future Apps resurrection or any other grouped list.)*

## Troubleshooting

**App shows default/placeholder data instead of Google Sheet data:**

1. Check that the SHEET_ID is correctly pasted between the quotes in `App.jsx`
2. Verify the sheet is published to the web (File > Share > Publish to web)
3. Verify the sheet is shared publicly (Share > Anyone with the link > Viewer)
4. Check that tab names match exactly: Settings, Classes, Calendar, Announcements, Health, Churches, Explore, FAQ, Contacts, Resources, Apps, Tips
5. Check that column headers in row 1 are lowercase and match the expected names

**App shows stale data after a sheet update:**

The localStorage cache shows whatever was last successfully fetched. On next open the app displays cached data instantly while a background fetch refreshes it; that fetch only succeeds on a working network. Possible causes:

1. The student's network is blocking or slow on the docs.google.com fetch. The status pill will read "Saved version (offline)". Cached data persists until the next successful fetch.
2. The data shape changed (new column, renamed field) but `CACHE_VERSION` was not incremented. Old caches keep loading the old shape. Fix: bump `CACHE_VERSION` and redeploy.
3. The student has an unusually old cache on their device. Force a cold cache by bumping `CACHE_VERSION`, or have the student clear site data via their browser settings (iOS Safari: Settings > Safari > Advanced > Website Data; Android Chrome: site info > Cookies and site data).

**Build fails on Vercel:**
Check the build logs for syntax errors. The most common issue is the SHEET_ID being pasted outside the quotation marks rather than between them.

**Classes not sorting correctly:**
The time field must start with a parseable time (e.g., `9:00` or `Mon 14:00–17:15`). Times like "TBD" sort to the bottom.

## Technical Notes

- Canva logo exports come through as JPEGs even when the filename says `.png`. If re-exporting from Canva in the future, verify the actual format with `file` or check the file header. True PNGs start with `iVBORw0KGgo` in base64; JPEGs start with `/9j/`.
- The transparent banner logo was created by applying a circular PIL alpha mask to the Canva export (margin of 8px at 500px scale). If the logo design changes, this mask approach can be reused.
- The `AnnouncementBanner` component is self-contained and receives announcements as a prop. It could be reused in other views (e.g., the Semester Calendar) by passing the same `data.announcements` array.
- If more than two or three announcements are active simultaneously, consider limiting the render to the most recent ones to avoid visual heaviness.
- The font `<link>` is duplicated (once in `index.html`, once injected via `useEffect` in `App.jsx`). The browser dedupes identical link tags so this is harmless; the `index.html` location ensures fonts start downloading before React mounts, the `useEffect` is a safety net in case `index.html` is ever out of sync with the bundle.

## Roadmap

### Review backlog (added 2026-06-09, prioritized)

A full-app review (security, correctness, UX, accessibility, PWA, performance) run on 2026-06-09 produced the backlog below. Items are in recommended order of attack; tiers group by urgency and by what kind of change ships them. Strike items out or move them to the changelog as they land.

**Status:** Tiers 1, 2, 3, and 5 shipped in full (Tier 2 incl. the lower-priority #5a/#5b, shipped 2026-06-10d); Tier 4 fully shipped (#12 iOS safe-area insets landed 2026-06-13c, closing the last open item — #13 category-disc contrast fix shipped 2026-06-13b, #14 inline-`transition` reduced-motion gap closed 2026-06-13); Tier 6 shipped 4-of-5 (#25 add-to-calendar shipped 2026-06-13; only #27 dark mode remains). The consolidated **"Still outstanding"** list below is the live to-do; the per-tier detail further down is kept as shipped history (each item marked ✅ with its ship date) and as the source of context for the outstanding items. As of 2026-06-13c.

#### Still outstanding (carried forward from the 2026-06-09 review)

Everything from Tiers 1–6 that was *not* completed or was only partially done, pulled up here so it's actionable without scanning the shipped tiers. Each links back to its origin item for full context.

**Wayfinding (front-end)**

- **[from Tier 5 #20] Fuller ProfileModal-body bilingual pass.** The status pill, aria-labels, and the Today greeting/date were bilingualized (or made Spanish-only) in Tier 5, but the body copy inside `<ProfileModal>` still has English-leaning strings that could get the bilingual treatment. Low value, moderate churn.

**Features (Tier 6 — not yet built)**

- **[Tier 6 #27] Dark mode.** The parchment-on-blue palette is fixed; relevant for late-night subte use. **Identity-touching → per `CLAUDE.md` it needs a design conversation + explicit Director sign-off on a palette direction before any code, and ships on a branch, never straight to `main`.** Belongs with the "bolder color confidence — phase 2" conversation under *Future ideas → Personality / UX moves*.

---

**Tier 1 — trivial config fixes, ship immediately** — ✅ shipped 2026-06-09

1. **✅ Re-enable geolocation in production.** `vercel.json` sends `Permissions-Policy: … geolocation=(), …` on every response, which blocks the Geolocation API for *all* origins, including our own. The "Cerca tuyo / Near you" distance sort (shipped 2026-06-07) has therefore never worked on baprogram.vercel.app; every student silently hits the graceful "unavailable" fallback, which is why nobody noticed. One-word fix: `geolocation=()` → `geolocation=(self)`.
2. **✅ Re-enable pinch-zoom.** The viewport meta in `index.html` carried `maximum-scale=1.0, user-scalable=no`. Android Chrome honors it, so a student couldn't zoom an address or the 10 px DM Mono labels; it was also a hard WCAG 1.4.4 failure. Both directives removed → viewport is now just `width=device-width, initial-scale=1.0`. **Note:** `viewport-fit=cover` was added/removed several times chasing the Tier 4 #12 edge-to-edge look (09h added it + padding, 09i tuned, 09j reverted the lot) — it is currently **OFF**; the viewport is just `width=device-width, initial-scale=1.0`. See Tier 4 #12 for why the edge-to-edge attempt was shelved.

**Tier 2 — security hardening, one `AuthCode.gs` re-deploy** — ✅ **fully shipped.** #3 (formula-injection guard) + #4 (identify throttle): code 2026-06-09, re-deploy confirmed 2026-06-09 — live. #5a/#5b (GET→POST + vet audit log): code 2026-06-10d, **needs its own `AuthCode.gs` re-deploy + a coordinated front-end push** (the front-end POST commit must not reach production before the re-deploy, or identify/prompts break for the gap).

3. **✅ Neutralize spreadsheet formula injection.** Added a `sheetSafe(v)` helper (prefixes `'` to any value starting with `=` `+` `-` `@` tab or CR) applied at every user-input write sink: `appendPlaceRow` (all place values), `upsertResponses` (the student response value — `field_id` is server-controlled, confirmed via `validateSubmissionValues` iterating declared fields only), and `handleVetPlace` (`vetted_by`, for a uniform write policy). Original finding below.

   *Original:* **Neutralize spreadsheet formula injection** — the one review finding that exceeds the documented threat model. Student-submitted text (place `name` / `why` / `address` / `maps_url`, and any `short_text` / `long_text` prompt response) is written into Roster-spreadsheet cells via `setValue`/`setValues` with no escaping. A value starting with `=` becomes a live formula that evaluates in the **Director's** authenticated session the moment the Roster spreadsheet is opened — and that file holds every student's CWID, birthday, email, housing, and allergy data, so `=HYPERLINK`-style exfiltration of adjacent PII is a realistic outcome. The in-app dashboards are safe (React escapes on render); the documented "open the spreadsheet" workflow is exactly where this fires. Fix: a small `sheetSafe(v)` helper that prefixes `'` to any user-sourced value starting with `=`, `+`, `-`, `@`, tab, or CR, applied in `appendPlaceRow`, `upsertResponses`, and `handleVetPlace`.
4. **✅ Throttle `identify`.** Added a per-CWID failed-attempt counter in `CacheService` to `verifyUserIdentity` (the single choke point for all identity checks): 10 failures per 10-minute window (`IDENTIFY_MAX_FAILS` / `IDENTIFY_FAIL_WINDOW_SEC`) → short-circuit to `no_match` without reading the sheet; a successful identify clears the counter; a 700 ms `Utilities.sleep` on every failure slows blind enumeration even before the cap. Per-CWID (not global) so one attacker can't lock the whole cohort out. Original finding below.

   *Original:* **Throttle `identify`.** The cohort token is shared with the whole cohort, so it isn't a secret within it — and with a token in hand, identify is an unthrottled oracle: a classmate's birthday brute-forces in at most 366 tries, and a guessed *staff* identity unlocks `admin_responses` (every student's dietary/allergy/size submissions). The docs call within-cohort impersonation an honor-code matter, which holds for someone who already knows a friend's birthday, but scriptable enumeration is a step beyond that. Fix: a failed-attempt counter in `CacheService` (e.g. 10 failed identifies per 10 minutes per CWID → return `no_match` without checking), or at minimum `Utilities.sleep(700)` on the failure path. Identify runs roughly once per device per cohort, so there is no UX cost.
5. **✅ Shipped 2026-06-10d** (own re-deploy, see changelog row). **(a)** `identify` / `prompts` / `admin_responses` / `admin_places` moved from GET query strings to POST `text/plain` so token + CWID + birthday no longer land in the Apps Script execution log; `doPost` routes them to the same handlers, `doGet` kept for backward compat with old cached builds, `places` (token-only) stays GET. **(b)** `handleVetPlace` now reads the current status first (deliberate transition, not a blind write), no-ops idempotently on no change, and writes an **append-only `PlacesVetLog`** row (`prev_status → new_status`, auto-created tab) for every vet action. `rejected → approved` stays allowed (the dashboard's "flip to undo" relies on it) but is now audited in the log rather than silently overwriting `vetted_by`. *Original finding:* (a) credentials in GET query strings land in Apps Script execution logs; the POST paths already keep credentials in the body — move the GETs to POST `text/plain` to match. (b) `handleVetPlace` did an unconditional in-place status write with no state machine and overwrote `vetted_by` (no history); add an append-only vetting-log tab and guard the `rejected` → `approved` transition.

**Tier 3 — `App.jsx` bug-fix pass (one batch, `BUILD_VERSION` bump)** — ✅ shipped 2026-06-09k (all of items 6–11; see changelog row). Front-end-only, no `CACHE_VERSION` bump, no Apps Script change.

6. ✅ **Boolean prompt fields render a pre-selected "No."** `initFormFromPrompt` initializes a never-answered boolean to `false`, the "No" pill lights up before the student has chosen anything, and Submit records a decline they never made. For the documented use case (a "¿Vas?" excursion sign-up driving a bus headcount), skipped answers register as declines — a data-quality bug. Initialize unanswered booleans to empty, render neither pill selected, and let required-field validation catch the blank.
7. ✅ **`<PromptForm>` resurrects abandoned edits.** The reset effect keys only on `prompt_id`, and the sticky `lastPromptRef` keeps that constant while the sheet is closed — so open → edit → close without submitting → reopen shows the unsaved edits as though they were saved. Re-initialize from stored responses on the open transition (include `open` in the effect), which preserves the close-animation behavior the sticky ref exists for.
8. ✅ **Finals UI never turns off.** `shouldShowFinalsUI` uses `d <= 14` with no lower bound, and `daysUntil` goes negative for past dates, so the FinalsCard and Today tile persist indefinitely after the finals window passes. Invisible for Summer (the term ends), wrong for Fall/Spring. Bound the gate against `finals_window_end`.
9. ✅ **`parseDays` mangles weekends.** A `days` cell of `Sat` misses the named-day regex (Mon–Fri only), falls into the single-letter path, and the `t` maps to Tuesday — the class renders on Tuesdays. `Sun` silently parses to nothing. A comma-less typo like `Mon Wed` silently drops the class from every view. Add Sat/Sun support (Sunday needs its own letter, e.g. `U`), split the named-day branch on commas *or* whitespace, and decide whether `DAYS_ORDER` / the Mon–Fri Class Schedule grid should grow weekend columns or whether weekend meetings belong on the Calendar instead. **Shipped:** added `S`=Sat / `U`=Sun to `DAY_LETTER_MAP` (and `DAY_ABBREV`), made the named-day branch match Sat/Sun and split on commas-or-whitespace with canonical-abbrev normalization. **Decision (Director):** `DAYS_ORDER` and the Mon–Fri Class Schedule grid stay Mon–Fri — weekend classes now surface correctly on Today + Weekly Overview (which iterate real day-of-week), and grid columns can be added later if a cohort needs them.
10. ✅ **Error boundary around the lazy map chunk.** `PlacesMap-*.js` is excluded from the SW precache by design, so after a deploy, a student who still has the old build open and taps "Mapa" for the first time 404s on the old hashed chunk — and the uncaught lazy rejection white-screens the entire app (Suspense does not catch promise rejections). Wrap the lazy mount in a small ErrorBoundary with a bilingual "Necesitás conexión / Couldn't load the map — reload" fallback.
11. ✅ **Smaller fixes to batch into the same pass:** `<DirectorPlacesView>`'s `creditMap` is never cleared on refresh or close, so a sheet-side `show_credit` edit can be silently overwritten by a later in-app Approve (clear it when `open` flips or a fresh payload lands). `formatPromptCutoff` compares times lexicographically, so a non-zero-padded `end_time` of `9:00` reads as still-open at 10:00 (normalize both sides through `toMinutes`). PlacesMap markers close over stale place objects after a background `fetchPlaces` refresh (look the place up by `place_id` at click time instead). The Today mount effect and pull-to-refresh can race on `bap-today-cache` writes and resurrect a stale entry (re-read the cache inside each `.then` rather than from a captured snapshot). A second `PlaceToast` within 4 s is cut off by the first toast's timer (keep a timer ref). The empty-state stat tile carries `bap-press` without being tappable. `<BottomSheet>`'s body-scroll lock restores out of order if two sheets ever stack (ref-count the lock) — latent today, will bite the first programmatic sheet-over-sheet.

**Tier 4 — phone polish pass (the bar is "works well on a student's phone")** — ✅ items 13–17 shipped 2026-06-09l; #12 shipped 2026-06-13c — Tier 4 now fully shipped. (See the 2026-06-09l and 2026-06-13c changelog rows for details.)

12. ✅ **Shipped 2026-06-13c.** **iOS safe-area insets.** The goal: with `black-translucent` status bar and no `viewport-fit=cover`, the installed PWA on a notched iPhone leaves the header/home-indicator zones to iOS auto-insetting (which works fine — the app shipped that way for months); the *enhancement* would be `viewport-fit=cover` for a true edge-to-edge gradient behind the status bar, with `env(safe-area-inset-*)` padding consumed at every fixed/full-screen edge. **What went wrong:** the 09h attempt used additive `calc(base + env(...))` (double-counted → too loose at top, dead strip at bottom); 09i switched to non-additive `max(base, env(...))`, fixed the stranded nav pill, and reduced the nav reservation — but on a real Dynamic-Island iPhone it *still* read too loose at the top and left a white patch under the nav, so 09j reverted the whole thing to the flat known-good layout. **If reattempted:** (a) it needs genuine iterative on-device testing — env() values (e.g. ~59 px top, ~34 px bottom on Dynamic-Island phones) can't be eyeballed in a desktop preview, and `max(base, inset)` is the correct non-additive pattern; (b) the top is the hard part — the status-bar time (left) and battery (right) live in the corners, so pulling content tighter than the full top inset risks colliding with them, while the full inset reads as "too loose" to the eye → the real fix is probably to keep the header content at the full top inset but make the *gradient* bleed up behind the status bar so the blue area doesn't look empty, rather than fighting the content position; (c) the bottom wants a reservation *smaller* than the full home-indicator inset (the Director found the full 34 px too padded), with the `.bap-nav-pill` CSS `bottom` tracking whatever the nav's `padding-bottom` becomes; (d) the FAB (~7668), `<PlaceToast>` (~7088), content `padding-bottom` (~10477), the three overlay headers/footers, and the `<BottomSheet>` body all move together. **Bottom line:** cosmetic payoff (gradient behind the status bar), real iteration cost — low priority. Leave the flat layout unless someone's actively iterating on the device. **Resolution:** shipped via a temporary in-app `env()` readout to get the real device insets (top 59 / bottom 34), then a decoupled design — gradient bleeds the full top inset, content/gear offset off `--safe-top`, bottom reserves *less* than the full inset via `--bap-nav-pad-bottom` with the nav pill tracking it. See the 2026-06-13c changelog row.
13. ✅ **Shipped 2026-06-09l.** **Stone (`#7A99AC`) fails contrast as text.** Roughly 3.0:1 on white, 2.7:1 on parchment, 2.6:1 on ice — all below the 4.5:1 AA threshold at the 10–13 px sizes it's used at, and it's the app's default tertiary text color (filter pills, inactive nav labels, captions, hours). Keep stone for borders; darken text uses toward Mountain (`#425563`, 7.75:1, passes comfortably). Sky-as-text (category captions) has the same problem.
14. ✅ **Shipped 2026-06-09l** (the two infinite animations) **+ 2026-06-13** (the inline-`transition` gap, via the new `useReducedMotion` hook — now fully closed). **Close the `prefers-reduced-motion` gaps.** The reduce block covers pulse dots, spinner, tip fade, press, nav pill, and toast — but not the 80 s spinning sun (this doc previously claimed it was suppressed, which is wrong until this item lands) or `.bap-steam` (the mate steam loop). Both are infinite animations, exactly the category the preference targets. Inline transitions (BottomSheet slide, FAQ chevron, toggle thumb) also bypass the gate.
15. ✅ **Shipped 2026-06-09l** (icon-512 302→37 KB; PlacesMap chunk 68→45.5 KB gz via createRoot instead of react-dom/server). **Shrink heavyweight assets.** `public/icon-512.png` is ~302 KB and SW-precached on every first install; recompress to ~60 KB (pngquant/oxipng). The `PlacesMap-*` chunk is ~224 KB raw / 68 KB gz — far above the size estimates in this doc — because `react-dom/server` is bundled just to stringify a dozen pin glyphs via `renderToStaticMarkup`; pre-rendering the glyphs as static SVG strings (like `CAP_SVG` already is) drops ~40 KB gz from the first map open.
16. ✅ **Shipped 2026-06-09l.** **Manifest gaps.** Icons lack `"purpose": "maskable"`, so Android adaptive launchers letterbox the logo in a white circle; add a maskable 512 variant plus `"id": "/"` (and optionally `orientation` / `lang`).
17. ✅ **Shipped 2026-06-09l.** **Touch targets.** `<FilterPill>` is ~22 px tall (3 px vertical padding at 11 px font) and is the primary filter UI on five surfaces; the ♥ save toggle is ~23 px next to other tappables. Pad both toward the 44 px guideline.

**Tier 5 — UX consistency and accessibility** — ✅ shipped 2026-06-10 (all of items 18–22; see the changelog row). Front-end-only, no `CACHE_VERSION` bump, no Apps Script change.

18. ✅ **Client-side required-field validation in `<PromptForm>`.** `handleSubmit` now rejects a blank required field instantly (type-aware empties; same bilingual copy as the server path, which stays the backstop), matching `<PlaceSubmitForm>`.
19. ✅ **A non-touch refresh affordance on Today.** A real circular refresh button in the greeting strip runs the existing `triggerRefresh` path (`aria-label`, `aria-busy`, `bap-spin`); the touch gesture is unchanged.
20. ✅ **Bilingualize the wayfinding outliers** *(scope per Director's call).* Status pill is now **Spanish-only** (`lang="es"`; terse Spanish fits the narrow pill better than a bilingual string would) + `aria-live`. English-only aria-labels swept to the bilingual convention. `lang="es"` on the cleanly-Spanish greeting + date; root stays `<html lang="en">` and interleaved bilingual strings intentionally left untagged. *Remaining (not done):* a fuller ProfileModal-body bilingual pass.
21. ✅ **Modal and nav semantics.** Shared `useDialogA11y` (focus-trap + Escape + focus-return, topmost-only via `__dialogStack`) on `<BottomSheet>`, `<ProfileModal>`, `<ConfirmDialog>`, and both Director overlays (latter two + ProfileModal also gained `role="dialog"`/`aria-modal`). `aria-pressed` on `<FilterPill>`, `aria-expanded` on the FAQ accordion, `<nav>` landmark + `aria-current` on the active tab, `aria-live` on the status pill, and the three `role="button"` divs converted to real `<button>`s.
22. ✅ **Replace `window.confirm` for sign-out / profile reset** with a centered, on-brand `<ConfirmDialog>` (Pep-Orange destructive button, dialog-a11y baseline). Focus defaults to Cancel; stacks correctly over ProfileModal.

**Tier 6 — feature ideas surfaced by the review (fold into Future ideas as they mature)** — items 23/24/26 ✅ shipped 2026-06-10b (see changelog row); 25 and 27 remain.

23. ✅ **Shipped 2026-06-10b.** **Search across FAQ and Places.** A reusable `<SearchInput>` drives FAQ search (title + content) and a Places-hub search (name/why/neighborhood/category across all categories, replacing the grid with a flat result list while typing).
24. ✅ **Shipped 2026-06-10b.** **Unread state for announcements.** A Pep-Orange dot on the Today nav tab when an active announcement hasn't been seen, cleared when Today is viewed; new `seenAnnouncements` profile field → `PROFILE_VERSION` 3.
25. ✅ **Shipped 2026-06-13.** **Add-to-calendar (.ics) export** for calendar events and finals — a compact "Agendar" pill on each `<CalendarView>` event and each assigned `<FinalsCard>` row downloads a single-VEVENT `.ics` (BA-anchored UTC times for timed events, `VALUE=DATE` with an exclusive `DTEND` for all-day; finals `final_time` ranges parsed via `toMinutes`). No new dependency. *Open caveat:* iOS Safari's download/handoff into the Calendar app on an installed PWA still wants a real-device check.
26. ✅ **Shipped 2026-06-10b.** **Saved places: count and share.** The ♥ Saved hub tile shows its count; `<PlaceCard>` gained a share button (Web Share API + clipboard fallback). *Note: still no cross-device sync (out of scope — local-only by design).*
27. **Dark mode.** The parchment-on-blue palette is fixed; relevant for late-night subte use. Identity-touching, so it belongs with the "bolder color confidence" conversation below.

### Future ideas (no particular order)

- **Places (student-contributable BA recommendations).** *Designed 2026-06-02; not yet built.* Evolves the existing Explore BA sub-tab into a categorized, student-contributable directory of Buenos Aires spots (cafés, restaurants, parks, gyms, climbing walls, study spots, etc.). Today's curated Explore BA rows become the seed data. The novel part is a **submit → queue → vet → publish** pipeline: a student suggests a place, it lands as `pending`, a staff member approves it in-app, and it then renders for everyone.
  - **Placement:** Local tab, **replacing the Explore BA pill** (renamed "Places"). No bottom-nav or sub-tab-row redesign. Adds a category filter-pill row (reusing the `EVENT_CATEGORIES` pattern), a per-card category glyph+color, a ♥ save toggle, and a Google-Maps-deep-linking address. A **"+ Suggest a place"** entry point — exact location still to be decided (top-of-list button vs. floating action button); wants a deliberate placement pass before building.
  - **Map / spatial features — phased.** *Direction set 2026-06-07.* Two distinct things were conflated under "map" and are now separated, because the high-value half needs no map at all:
    - **v1 (tap-to-Maps, shipped behavior):** tappable pins open the native Google Maps app (the existing `<AddressLink>` behavior) — **no new dependency**, preserving bundle size and the offline-on-the-subte promise. Google does the geocoding from the free-text address, the user's own Maps app does routing/transit (better than anything we'd embed). This stays the baseline and the offline fallback regardless of what's added on top.
    - **Phase 1 — "Cerca tuyo / Near you" distance sort (no map). ✅ Shipped 2026-06-07** on the existing Local tab (Explore BA, Healthcare, Churches) ahead of the full Places build — see the changelog entry for the implementation. The original design rationale, preserved: add `lat` / `lng` columns to the Places schema and fill them at edit time (paste from a Google Maps right-click → "copy coordinates"; one-time per place). With coordinates in the data, use the browser **Geolocation API + a Haversine distance helper** to offer a "Near you" sort and a small distance badge per card ("~600 m"). This delivers most of the spatial value students actually want — *what's closest* — with **no new dependency, no map tiles, and no offline regression** (geolocation degrades gracefully to the default A–Z / category sort when the student declines the permission or is offline). It also seeds the coordinate data a real map would later need, so none of the work is throwaway. *Note:* avoid client-side geocoding of the address (Nominatim is rate-limited, its ToS discourages app use, and it needs network) — store coordinates in the sheet instead. Privacy: geolocation is read on-device only for the sort, never sent anywhere; gate it behind an explicit "Near you" tap so the permission prompt is intentional.
    - **Phase 2 — embedded interactive map. ✅ Shipped 2026-06-09g (Leaflet, online-only).** Now that Places has shipped both earlier stages (read path + student submission + vetting), the coordinate seed from Phase 1 is in place, so the map landed. What shipped (matching the recommendation below almost exactly):
      - **Library — Leaflet.** Chosen over the alternatives evaluated below. Leaflet core is ~42 KB gzipped JS + a small CSS file, it renders **raster** tiles (no WebGL, so it stays smooth on the older/cheaper phones in a cohort, unlike MapLibre's GPU-dependent vector rendering), and its API is small enough to wire a pinned map in well under a day. The bundle cost is neutralized by **lazy-loading** (see below), so it never touches first paint or the offline shell. *Rejected:* **MapLibre GL** (~200 KB, GPU-dependent, smoother pan but overkill for a few dozen pins); **Mapbox / Google Maps JS** (paid/keyed, best polish, not worth a billing relationship for this scale).
      - **Tiles — CARTO Positron raster, no key (recommended), Stadia as the keyed fallback.** OpenStreetMap's public tile server is still off the table (its usage policy disallows app traffic). CARTO's `light_all` (Positron) raster basemap is a clean, light-gray style that sits naturally against the app's Ice/Fog/white palette and requires no API key at cohort volume (a few dozen students, well inside fair use); attribution to CARTO + OSM is required and easy to add. If CARTO ever rate-limits or wants registration, **Stadia Maps** (free tier, API key + domain allowlist, similar clean styles) is the drop-in fallback. This keeps the "no backend, no secret to manage" ethos intact for the default path. *Verify CARTO's no-key raster endpoint and current attribution wording at build time.*
      - **Lazy-load / code-split — the key move.** Leaflet and its CSS load via a dynamic `import()` fired only when the student first taps the map toggle, so the main bundle, first paint, and the precached offline shell are all unchanged. A student who never opens the map never downloads it. This is what makes the dependency acceptable against the "works well on a student's phone" bar.
      - **UX placement.** A "🗺 Mapa / Map" ↔ "Lista / List" toggle at the top of a Places **category listing** (sitting beside the existing "Cerca tuyo / Near you" control), switching the current filtered+saved set between the card list and a full-view map. Render the map as its own view that replaces the list, **not** inside a `<BottomSheet>` — that sidesteps the Leaflet-drag-vs-sheet-drag gesture conflict entirely. Pins are custom `divIcon` markers in the place's `PLACE_CATEGORIES` color (reusing the same color/glyph machinery the cards already use); tapping a pin opens a small popup (name, category, one-line `why`) with a button that opens the existing place card / "Open in Maps" deep link. Center on CABA by default, or on the student's location when "Near you" permission was already granted. Coordinates come straight from the `lat`/`lng` already seeded in Phase 1; places without coordinates simply don't get a pin (the list stays their home).
      - **Offline — unchanged promise.** The map is an **online-only enhancement**. Tiles require network and SW-caching a useful zoom range across all of CABA is many MB, which would fight the offline-on-the-subte promise, so tiles are deliberately **not** added to the service-worker precache. When offline (or on a failed tile load), the toggle is disabled with a brief "Necesitás conexión / Needs connection" note and the **list view remains the canonical offline experience**.
      - **What changed from the plan when it shipped:** (a) **glyph pins** — each place marker is a category-colored disc with the category's own glyph rendered to a static SVG (via `react-dom/server`'s `renderToStaticMarkup`, which sits entirely inside the lazy chunk; accepted ~23 KB gz cost there for the legibility, since the chunk is online-only and browser-cached after first open); (b) **Casa Holden** (the Pepperdine BA campus) is hardcoded as a `CAMPUS_ANCHOR` constant and rendered on *every* Places map with a distinct Pep-Blue, Pep-Orange-ringed mortarboard pin + a permanent label, so students always have home base for reference (coords `-34.5687288,-58.4416504`, resolved from the program-office Maps link); (c) because the campus always anchors the map, the Lista/Mapa toggle is offered on *every* listing, not only ones with located rows; (d) tapping a place pin opens the real `<PlaceCard>` (with a working ♥ save) in a `<BottomSheet>`, rather than a Leaflet popup. The lazy chunk is also **excluded from the SW precache** via `globIgnores: ["**/PlacesMap-*"]` in `vite.config.js` (online-only; no reason to ship it to the offline shell). Clustering was skipped (cohort volume is dozens of pins max); `leaflet.markercluster` can be added later if a category ever gets crowded. No `CACHE_VERSION` bump (data shape untouched; the map only reads existing `lat`/`lng`), no Apps Script change, no sheet-schema change. **Still worth a real-device check:** the iOS-PWA gesture interplay (Leaflet touch/drag vs. the page scroll container and bottom-nav) on an installed home-screen PWA.
  - **Categories (proposed, editable):** Café · Restaurant · Bar/Nightlife · Park/Outdoors · Fitness/Gym · Climbing · Shopping · Culture/Museum · Study spot · Services. Each gets a glyph + BAP-palette color, same machinery as `EVENT_CATEGORIES`.
  - **Saves/favorites:** personal, stored in `localStorage` (like the profile), no server aggregation, no cross-device sync. A "Saved / Guardados" filter pill shows just the student's saved places. (Community upvotes/ratings were considered and held — they'd need server-side counting and invite gaming.)
  - **Submission (lightweight):** a `<BottomSheet>` form with a **fixed** schema — name, category, Google Maps link or address, one-line "why" — **not** the Director-defined prompts machinery. On submit the student gets a **confirmation toast only** ("¡Gracias! Enviado para revisión / Thanks — sent for review"); there's no post-submit status tracking or "my submissions" list in v1.
  - **Vetting (in-app Director dashboard):** the existing Director dashboard gains a **Places queue** mode (a near-1:1 reuse of the `admin_responses` + `<DirectorResponsesView>` staff-gated pattern). Each pending card shows the submission + submitter with **Approve / Reject** and a **"show submitter's name" toggle** (attribution is decided **per place** by the Director, stored as a `show_credit` column). Field tidying (fixing a name, adding hours/neighborhood) happens **in the Sheet**, not inline in the dashboard — keeps the dashboard light. **Duplicate submissions are caught by the Director during vetting**, not by a client-side check.
  - **Architecture (auth/Roster side — deliberate reuse):** the in-app-dashboard requirement forces this onto the auth script, because vetting needs user identity, the `isStaff` gate, and a write path — all of which already exist on the Roster/auth side and none of which the content script has (and the content script can't reach the Roster by design). New **`Places`** tab on the Roster spreadsheet with columns roughly: `place_id`, `name`, `category`, `why`, `address`, `lat`, `lng`, `maps_url`, `neighborhood`, `hours`, `link`, `status` (pending/approved/rejected), `submitted_by_cwid`, `submitted_by_name`, `show_credit`, `source` (seed/community), `submitted_at`, `vetted_by`. (`lat` / `lng` power the Phase 1 "Near you" distance sort above; the Director fills them during vetting alongside any field tidying, so student submissions don't need to carry coordinates.) Migrate today's Explore BA rows in as `source=seed, status=approved`. New endpoints on `AuthCode.gs`: `?action=places` (everyone → approved list), POST `submit_place` (student → pending row), `?action=admin_places` (staff-only queue), POST `vet_place` (staff → flips `status`, sets `show_credit`). New front-end cache at a `bap-places-cache` key with a short TTL so reads stay instant and the last-approved list survives offline. A `validatePlaces()` editor helper mirroring `validatePrompts()`. **No `CACHE_VERSION` bump** (the content-sheet data shape is untouched; Places gets its own cache key). **Manual cutover when built:** add the `Places` tab to the Roster sheet, migrate the seed rows, re-deploy `AuthCode.gs`. *Acknowledged tradeoff:* this co-locates low-stakes public-ish place content on the more-sensitive Roster spreadsheet — accepted because it's not PII and the reuse is large; the alternative (places on the content sheet) would force a manual "Director copies approved rows across sheets" step since the two scripts can't see each other's spreadsheets.
  - **Threat model:** unchanged calibration — submissions are authenticated by the cohort-token + CWID + birthday triple and gated by mandatory human staff review before anything goes public.
  - **Scope note:** this is a "bigger change" per the workflow rules (new tab, new endpoints, schema, multi-component UI) — build on a branch, not straight to `main`.

- **Push notifications** for calendar events. *Considered and deferred 2026-05-02.* The PWA half is essentially free — the existing `vite-plugin-pwa` service worker just needs a `push` event handler and a `Notification.requestPermission()` flow. The cost lives in the backend half, which would be the first feature to break the project's "no real backend" architecture. Three viable paths: (1) **all-Google** — Apps Script + a Subscriptions sheet tab storing endpoints + a time-driven trigger that reads Calendar/Classes and fires VAPID-signed pushes via `UrlFetchApp`. Fits the architecture and adds no new bills, but implementing the Web Push protocol's VAPID signing (ECDSA + JWT + payload encryption) in Apps Script with no first-class library is non-trivial — roughly 2–3 days. (2) **Vercel serverless function + Vercel Cron + a small KV store** (Upstash Redis free tier or Vercel KV) using the `web-push` npm package. Standard and well-trodden, ~1 day, but introduces a new deploy surface and a vendor footprint. (3) **OneSignal or Firebase Cloud Messaging** — fastest to ship but adds a third-party SDK and a glue layer to schedule per-event pushes from the sheet. **iOS caveat:** Web Push on iOS only fires for PWAs added to the home screen on iOS 16.4+, so the install-to-home-screen step becomes load-bearing rather than purely enhancement. Held until there's a concrete daily-use case the current pull-based model genuinely can't serve (per-class meeting reminders, urgent-announcement push on top of the Today banner, finals-day morning-of nudge).
- **Spanish UI toggle** with Argentine `vos` forms; English remains the default.
- **Custom domain** (e.g., `app.buenosaires.pepperdine.edu`) via Vercel's domain settings.
- **Semester switching** (multiple sheets, one per semester).
- **Quick-action "Contact Staff" section** on the home screen.

### Personality / UX moves not yet shipped

Five "small moves" landed on 2026-04-25: editorial section titles with Spanish gloss, pulsing synced dot, rotating loading tips, whimsical empty days in the Weekly Overview, and press feedback on actions. Five medium-scope moves landed later the same day: Today hero in the Weekly Overview, mini-illustration library, Southern Cross constellation in the header, animated bottom nav with per-tab color identity, and dot-grid pattern fill on the Apps section header. Later that same day, the Today dashboard also shipped as a dedicated tab (the leftmost, default-on-open tab), bringing with it a time-of-day greeting strip, live weather and dólar blue tiles, a live "Próximo en X min" countdown, a steaming mate empty state, and a rotating tip card. Documented under their respective sections above.

The candidates below were considered alongside them and held for later. None require new dependencies.

**Bigger scope (brand-shifting, worth a pre-build conversation):**

- **Bolder color confidence — phase 2.** Phase 1 shipped 2026-05-01b (BAP Blue accent rule on every section title, 4 px BAP Blue left stripe on Today's activity + tip cards, soft white-to-Ice gradient on the quick-stat tiles). Phase 2 is the deeper move that still needs a mockup pass: pushing BAP Blue into card *backgrounds* across the app (today's class cards, schedule rows, section header bands) and inverting white-as-secondary. Identity-shifting; harder to dial in without seeing it side-by-side with the current palette first.
- **Spanish UI toggle** (also listed in the general Roadmap above). Biggest personality jump because the bicultural identity becomes load-bearing rather than decorative. Translation work plus a settings persistence layer; affects copy on every screen, including buttons, empty states, status labels, and tab names.
- **Phrase of the Day tile on Today.** A dedicated tile on the Today dashboard surfacing one curated Argentine *vos* phrase per day with a one-line gloss. Could either reuse the existing Tips sheet (with a `featured: true` flag on rows curated for daily exposure) or live in a new `Phrases` tab. Today already shows a rotating Tips card, so this would be a sibling tile dedicated to language acquisition rather than a replacement.

## Removed / dormant features

Features that shipped and were later pulled from the UI but are intentionally easy to bring back. Kept here so the "why" and the resurrection steps aren't lost.

### Apps sub-view (Local tab) — removed 2026-06-10j

The Local tab used to carry an **Apps** sub-view: a curated catalog of recommended phone apps (Google Maps, SUBE, Mercado Pago, Cabify, WhatsApp, etc.), grouped "Getting around / Transporte" vs "Daily life / Día a día," with essentials sorted first and per-row "📱 iOS / 🤖 Android / 🌐 Website" store buttons. It was the fifth button on the Local category hub.

**Why removed:** trimmed from the Local hub to keep that surface focused; the Director can resurrect it if a future cohort wants the app catalog back.

**What was removed (UI only, `App.jsx`):**

- The `apps` entry in the `LOCAL_SECTIONS` config array (a commented-out placeholder marks the spot).
- The entire `sub === "apps"` render block inside `<LocalView>` (the `renderAppCard` helper, the dot-grid section header, the category filter pills, and the transport/daily grouping).
- The now-dead derived locals: `appsFilter` state, `appsCategories`, `filteredApps`, `sortedApps`.

**What was deliberately kept (so resurrection is cheap):**

- **The data plumbing is fully intact.** The `Apps` tab still exists on the content sheet, is still listed in `Code.gs`'s `TABS`, is still fetched, and is still parsed into `data.apps` by `normalizeData`. Because the fetched-data shape never changed, **`CACHE_VERSION` was not bumped** and no Apps Script re-deploy was needed.
- `AppGridIcon` (the hub glyph) — still referenced by the Places category-picker grid's "All" tile, so it stays regardless.
- `<SectionDivider>` and `ColectivoIcon` — orphaned by the removal but retained as dormant; they were used only by the Apps grouping and would be needed again on resurrection.
- The **Apps tab schema** documentation (above) and the operations-guide entry remain accurate.

**To resurrect:** (1) re-add the `{ key: "apps", en: "Apps", es: "Para el día a día", Icon: AppGridIcon, accent: C.mountain }` entry to `LOCAL_SECTIONS` (the comment marks where); (2) restore the `sub === "apps"` render block and its four derived locals (recoverable from git history at the 2026-06-10j commit's parent). No sheet, `Code.gs`, or `CACHE_VERSION` change is required — `data.apps` is already populating.

## Changelog

| Date | Changes |
|------|---------|
| 2026-06-14b | **Bottom-nav home-indicator fix — nav moved to normal flow (iOS safe area).** `App.jsx` + `index.html`; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. The fixed `bottom: 0` bottom nav kept leaving the parchment page background showing through the iOS home-indicator zone. Root cause: **iOS won't reliably paint a `position: fixed` element (or its box-shadow) into the home-indicator safe area**, so the 2026-06-13c follow-up (centering via `left/right/margin` + a downward white `box-shadow` filler) was non-deterministic — it rendered clean after one reinstall and regressed on the next launch. Fix: the nav is now a **normal in-flow flex child** at the bottom of the `100dvh` column (`position: relative; flexShrink: 0`) instead of `position: fixed`, so it physically occupies the true bottom and its white background + `var(--bap-nav-pad-bottom)` bottom padding own the home indicator with **no gap possible**. The content area (`flex: 1`, `overflowY: auto`) is the scroll container; its big fixed-nav clearance padding was dropped (`calc(84px + var(--bap-nav-pad-bottom))` → `24px`). `.bap-nav-pill` still anchors to the nav (now `position: relative`). The Places FAB and `<PlaceToast>` stay `position: fixed` and float above the in-flow nav unchanged. Belt-and-suspenders: `body` background `#F5F3F0` → `#FFFFFF` so any sub-pixel `100dvh` shortfall shows white, never parchment. Supersedes the 2026-06-13c box-shadow filler (removed). |
| 2026-06-14 | **Greeting-strip polish (Today): refresh button nested in the sun + Sol de Mayo–style sun.** `App.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. (1) The non-touch refresh button moved from `top:12/right:12` to `top:5/right:5` so it centers on the decorative greeting sun (which sits ~22px from the top-right corner at `scale(1.7)`), reading as the sun's disc with the rays radiating around it. (2) `<SunIcon>` (the day-hours greeting watermark) reworked from 8 straight Pep-Blue spokes on a Bap-Blue dot into a **Sol de Mayo–inspired** form: 12 straight triangular rays alternating with 12 wavy "flame" rays (each an S-curve built from two quadratics), generated in a loop like `<SolDeMayoIcon>`. Rendered **white/celeste** (gold stays exclusive to the Mundial `<SolDeMayoIcon>` so that treatment keeps its punch), with the day-watermark opacity bumped `0.22 → 0.3` so the finer rays read around the button. Night `<MoonIcon>` and the Mundial Argentina path are unchanged. |
| 2026-06-13c | **iOS safe-area insets (review backlog Tier 4 #12) — shipped after two parked attempts.** `index.html` + `App.jsx`; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. Closes the last open Tier 4 item (only the long-parked #12 remained). **How it was done differently this time:** a temporary in-app `<SafeAreaProbe>` diagnostic (gated, auto-shown only in the installed standalone PWA) read the device's *real* `env(safe-area-inset-*)` values — top **59** / bottom **34** / sides **0** on a Dynamic-Island iPhone — so the layout was tuned against actual numbers instead of guessed in a desktop preview (the root cause of the 09h/09i failures). The diagnostic was removed in the same ship. **The design (the decoupling both prior attempts missed):** `index.html` now carries `viewport-fit=cover` plus two parse-time `:root` tuning vars — `--safe-top: env(safe-area-inset-top)` and `--bap-nav-pad-bottom: max(16px, calc(env(safe-area-inset-bottom) - 10px))` (deliberately *less* than the full home-indicator inset, which the Director found over-padded). The main header **and** the three full-screen overlay headers (`<ProfileModal>`, `<DirectorResponsesView>`, `<DirectorPlacesView>`) bleed the navy gradient the full top inset *behind* the status bar via `padding-top: max(16px, var(--safe-top))`, while the gear button is offset (`top: max(12px, calc(var(--safe-top) - 4px))`) to clear the battery. The bottom nav reserves `var(--bap-nav-pad-bottom)`, and the `.bap-nav-pill` `bottom` now **tracks** that reservation (`calc(var(--bap-nav-pad-bottom) - 10px)`) — fixing the 09i stranded-pill bug — with the content scroll padding-bottom, the Places FAB, `<PlaceToast>`, the three overlay footers, and the `<BottomSheet>` body all shifted by the same reservation. On non-notched / non-iOS the `env()`s resolve to 0, so `max(16px, …)` preserves the prior flat layout byte-for-byte. Looseness stays tunable in one line via `--safe-top`. `<PasscodeGate>` / `<UserGate>` were left untouched (centered full-bleed; cover only improves them). Verified on a real Dynamic-Island standalone PWA. **Same-day follow-up fix:** the fixed bottom nav's `translateX(-50%)` centering made iOS anchor `bottom: 0` to the safe-area inset (not the true screen bottom), leaving the parchment page background showing through the home-indicator zone — re-centered via `left/right/margin` and added a downward white `box-shadow` (`0 60px 0 0 #fff`) as a self-correcting safe-area filler (paints white over any residual gap, renders off-screen if `bottom: 0` already reaches the physical bottom). |
| 2026-06-13b | **Category-disc contrast fix (review backlog Tier 4 #13) — WCAG 1.4.11 / 1.4.3.** One front-end pass (`App.jsx` + `PlacesMap.jsx`); **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. Verified by a clean production build and a WCAG sRGB contrast recomputation (every fixed surface now clears its bar). **The problem (corrected from the original note):** category glyphs are white SVGs on a disc filled with the category's `color` token; on the *light* tokens white-on-fill is only ~2.2–3.0:1, under the 3:1 non-text bar. Precise math showed the worst offenders were **`bapBlue` #64B5F6 (2.21)** — our marquee color, used by `outdoors`/`sights` and Events `service` — and **`sky` #6CACE4 (2.43)** (`culture`, Events `exhibit`); `stone` (`study`/`other`) was borderline at 3.01. Separately, category captions rendered *in* the category color failed the 4.5:1 text bar on `bapBlue`/`sky`/`stone`/`pepOrange`. **The fix (two coordinated moves, both covering Places *and* Events category surfaces):** **(A)** new `discGlyphColor(fillHex)` helper (+ `relLuminance(hex)`) flips the glyph ink to **navy (`pepBlue`) on light fills, white on dark fills** (threshold 3.5:1 white-on-fill cleanly separates the groups — `bapBlue`/`sky`/`stone`→navy at 5.1–7.0:1; `pepOrange`/`ocean`/`mountain`/`pepBlue`→white). Applied at all five disc sites — `<PlaceCard>`, `<PlaceSubmitForm>` category picker, the `<LocalView>` category-picker grid (both the glyph and the ♥ tile), `<EventCard>`, `<EventsTodayTile>` — and the **Places map pins** via a new `_glyphInk` field on the enriched map-place objects (consumed in `PlacesMap.jsx` as `p._glyphInk || "#fff"`; the campus pin and white ring are untouched). **(B)** category caption **text** recolored from the category color to neutral **Mountain** gray (`#425563`, 6.8–7.8:1) at three sites — `<EventCard>` date badge, `<PlaceCard>` uppercase caption, `<DirectorPlacesView>` chip — since the colored disc still carries the category cue. **Deliberately unchanged:** brand color tokens (no global `C.*` mutation), all disc *fills* (still the brand color), the `<PlaceSubmitForm>` selection border, the `<DirectorPlacesView>` 7px legend dot, and the map ring/campus pin. **Closes Tier 4 #13;** Tier 4 is now fully shipped except #12 (iOS safe-area insets, parked). |
| 2026-06-13 | **Add-to-calendar (.ics) export + reduced-motion inline-transition fix (review backlog Tier 6 #25 + Tier 4 #14 leftover).** One front-end pass (`App.jsx` only); **no `CACHE_VERSION` bump** (no fetched-data shape change), no Apps Script change, no new dependency. Verified with a production build (clean) and by running the *actual* ICS source functions in Node against seven vectors (timed, all-day single, multi-day span, late-night UTC day-rollover, finals time-range, finals single-time, finals blank-time fallback) — all produced valid RFC5545 output. **(1) Add-to-calendar (.ics).** A compact **"Agendar"** pill (new `<AddToCalendarButton>` + `<CalendarPlusIcon>` glyph) on **every Calendar-tab event row** (`<CalendarView>`) and on **assigned Finals rows** (`<FinalsCard>` — TBD rows are skipped since there's no date to add) downloads a standards-compliant single-VEVENT `.ics`. **Time model:** Buenos Aires is a constant GMT-3 (no DST), so timed events convert local→UTC by `Date.UTC(y, m-1, d, hh+3, mm)` and emit `DTSTART:…Z` / `DTEND:…Z` (hour-rollover handled by `Date.UTC`); all-day events (no `start_time`, or a finals row with blank/unparseable `final_time`) emit `DTSTART;VALUE=DATE` with an **exclusive** `DTEND` (last day + 1). Calendar-event mapping: title→`SUMMARY`, `description`→`DESCRIPTION`, no location; timed when `start_time` is set (end = `end_time` or start + 1 h), else all-day spanning `date`..`end_date`. Finals mapping: `Final · {code} {title}`, `Examen final / Final exam` description, `location`; `final_time` parsed by splitting on en-dash/hyphen and running each part through `toMinutes` (end = second part or start + 1 h). Stable per-item `UID` via the existing `djb2` hash; download via a Blob + `URL.createObjectURL` + a synthetic `<a download>`. New helpers: `pad2`, `icsEscape`, `icsUtcStamp`, `parseYMD`, `nextDayStr`, `icsDate`, `icsUtcFromBAMinutes`, `buildEventIcs`, `slugifyForFilename`, `downloadIcs`, `downloadCalendarEventIcs`, `downloadFinalIcs`; new components `<CalendarPlusIcon>` and `<AddToCalendarButton>`. **(2) Reduced-motion fix (Tier 4 #14 leftover, now closed).** New `prefersReducedMotion()` + `useReducedMotion()` hook (a `matchMedia('(prefers-reduced-motion: reduce)')` read with a live change listener) gates the **inline** `transition:` styles the CSS media-query block couldn't reach: the `<BottomSheet>` slide + backdrop fade, the FAQ accordion chevron, both collapsible-finals chevrons (`<TodayFinalsTile>` / `<FinalsCard>`), and the settings toggle thumb (background + position). Each gated transition becomes `reduced ? "none" : "<value>"`; the hook is placed before each component's early returns (Rules of Hooks). **Open caveat:** the generated `.ics` is valid, but iOS Safari's download/handoff into the Calendar app on an installed PWA wants a real-device check (per the original Tier 6 #25 note). |
| 2026-06-11b | **PWA auto-update reliability on installed Android PWAs.** `App.jsx` only; **no `CACHE_VERSION` bump**, no `vite.config.js`/Apps Script change, no new dependency. **The problem:** the service worker is already configured for silent auto-updates (`registerType:'autoUpdate'` + `skipWaiting` + `clientsClaim`), but the browser only re-checks the SW *script* for a new version on a real navigation. An installed Android PWA resumed from the background usually just *restores* the existing page rather than navigating, so a freshly deployed build can sit **undetected** indefinitely — the field report was a 24h-cold open still showing stale content until a manual *double* hard-close (first reopen detected + activated the new worker but the loaded page kept running the old JS; the next cold launch finally loaded the fresh bundle). **The fix:** a new app-level `useEffect` at the top of `<App>` (runs once on mount) that forces an update check — `navigator.serviceWorker.getRegistration().then(reg => reg.update())` — on every `visibilitychange → visible` (app regains focus), plus an **hourly `setInterval`** backstop while open and once on mount. Each listener is cleaned up on unmount; the whole effect early-returns when `navigator.serviceWorker` is unavailable (preview / unsupported). **Detect-only by design** (per the Director's call): there is **no** `controllerchange`-driven reload, so a student session is never interrupted mid-task — the activated new worker's build simply shows on the next **cold launch** rather than live in the resumed session. **Caveat:** each device must pick up *this* build once (the old way) before the on-resume checking applies; from the next deploy onward those devices self-check on resume. iOS is unchanged (this helps there too, but iOS separately evicts installed-PWA service workers after ~7 days of non-use — OS behavior, not overridable). A controllerchange-driven silent auto-reload remains a ~5-line future addition if the "fresh on the same reopen" experience is ever wanted. Production build clean (`dist/sw.js` + `registerSW.js` generated as before). |
| 2026-06-11 | **Mundial game-day treatment on Today (Argentina).** `App.jsx` only; **no `CACHE_VERSION` bump** (stays 7), no sheet schema change, no Apps Script change, no new dependency. **Trigger:** a `mundial`-typed Calendar event dated *today* whose `title` contains "Argentina" (e.g. `Mundial: 🇦🇷 Argentina vs Austria 🇦🇹`). Detection is a title-match over the Calendar data the Director already maintains — `getArgentinaGameForDate(data, dateStr)` finds it, `cleanMundialTitle(title)` strips a leading "Mundial:" label for display. **When it fires, the Today tab puts on the albiceleste jersey:** **(1) Greeting strip** — the time-of-day gradient is replaced by a celeste-forward gradient (`#6CACE4 → #5795D6 → #0057B8 → #00205B`; it still ends in Pep Blue so the white greeting text keeps its contrast, and a faint text-shadow is added for safety), the rotating `<SunIcon>` is replaced by a rotating `<SolDeMayoIcon>` (the flag's sun) at higher opacity, drifting `<Papelitos>` (celeste/white/gold confetti) fall over it, and the DM-Mono label flips from "Hoy / Today" to "¡Hoy juega Argentina! / Game day". The personalized greeting (`Buen día, Cris`) and Spanish date are unchanged; the text is wrapped in a `position:relative; zIndex:2` layer so it sits above the papelitos. **(2) `<MundialGameTile>` hero** — rendered between the greeting strip and the weather/dólar row: a jersey-stripe `repeating-linear-gradient` background, an oversized low-opacity number-**10** watermark bleeding off the corner, the matchup (flags kept), and a row with a live "Arranca en X" kickoff-countdown pill (computed from the row's `start_time` via `toMinutes` + `formatCountdown`; falls back to "¡En cancha!" once kickoff passes, "¡Vamos!" when no time is set) plus the kickoff time and an optional description. Tapping the tile calls `onJumpToTab("schedule")`, where the game also appears in the Weekly Overview (its `visibility: week`). **New visuals:** `<SolDeMayoIcon>` (64×64, a 32-ray flag sun with a small face, rays generated in a loop) and `<Papelitos>` (a fixed-config confetti array so the minute-tick re-render doesn't reshuffle it) + a `bap-papel-fall` keyframe and `.bap-papelitos` rules in the personality-styles block. **Accessibility:** all motion honors `prefers-reduced-motion` — papelitos are hidden, the Sol de Mayo goes static (via the existing `.bap-sun-rotate` disable) — while the gradient skin and the static game tile still render, so reduced-motion users still get the festive read. Verified in a dev preview (preview mode + a seeded today-dated Argentina `mundial` event): greeting skin, Sol de Mayo, 11 papelitos, and the hero tile with a live "Arranca en 5 h 46 min" countdown all render; production build clean. |
| 2026-06-10m | **Places List/Map/Near toggles → icon-only.** `App.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. The three controls at the top of a Places listing dropped their text labels for glyphs: **List** = a bulleted-list icon (`ListViewIcon`), **Map** = a folded-map icon (`MapViewIcon`), **Near** = a navigation-arrow icon (`NavArrowIcon`) — three new inline-SVG components (`{size,color}`). Each button keeps its active/inactive pill treatment and ≥34 px height and gains a bilingual `aria-label`/`title` + `aria-pressed` (the row is now wordless). **Scoped to Places:** a Places-only `nearMeIconButton` was added next to the existing text `nearMeButton`, so the "Cerca / Near" pill on Healthcare / Churches / Explore is unchanged. Verified in a dev preview: the three icons render with correct active states, the List/Map toggle switches views (map renders with the campus + category pins), and the Near arrow toggles the distance sort. |
| 2026-06-10l | **Pull-to-refresh (and the Today refresh button) now also refresh Places.** `App.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. **The gap:** Places has its own fetch path — the auth-script `?action=places` endpoint, wired to a *mount-only* background effect (`useEffect … [cohortToken]`) plus the 10-min `bap-places-cache`. The Today pull-to-refresh / refresh button calls `refreshAllData`, which re-pulled **only** the content endpoint (`fetchAllData` with `?bust=1`) and never touched Places. So a Director who edited a place row directly in the Roster `Places` tab (e.g. added `lat`/`lng` + notes) could not see it update in-app via the refresh gesture — only a full app reload (a true React mount) would re-run the Places fetch, and on an installed iOS PWA even reopening often just *resumes* without remounting, so the stale cached row persisted. The auth-script `handlePlaces` has **no** server cache (reads the sheet live every request), so the staleness was entirely client-side. **The fix:** `refreshPlaces()` (relocated to just above `refreshAllData` so it can sit in the dep array without a temporal-dead-zone error) is now kicked off in parallel inside `refreshAllData` and awaited in its `finally`, so a single refresh re-pulls content **and** Places, the spinner covers both, and `refreshAllData`'s deps gained `refreshPlaces`. Verified in a dev preview with a `window.fetch` spy: one refresh fires exactly one `action=places` call alongside the content `?bust=1` (plus weather/dólar) — before the change, no `action=places` call fired on refresh. |
| 2026-06-10k | **Glyph-only contact/link buttons + Contacts reorder.** `App.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. **(1)** Six new inline-SVG **link/action glyphs** — `PhoneGlyph`, `WhatsAppGlyph`, `EnvelopeGlyph`, `MapPinGlyph`, `GlobeGlyph`, `InstagramGlyph` (each `{size,color}`) — replacing the old emoji (`📞 💬 ✉ 📍 📷 →`). **(2)** `<ActionBtn>` (Contacts + Resources) refactored from `icon`+text to `Glyph`+bilingual `aria-label`/`title`, rendered **icon-only** with a ≥40 px tap target; a new optional `value` keeps real data visible beside the glyph — the **Emergency phone number** and the **office email** (per the Director's call). **(3)** `<LinkButton>` (all Local sub-views — events / places / health / churches) is now **icon-only**: globe = website, the WhatsApp / Instagram marks, phone = `tel:`, with the label in `aria-label`/`title`. **(4)** `<AddressLink>`, the two `<PlaceCard>` / `<DirectorPlacesView>` "Open in Maps" fallbacks, the Courses "Email Prof." button, and the "Cerca / Near" distance-sort toggle pin all swapped from emoji to the SVG glyphs for visual consistency. **(5)** **Contacts tab reordered** — the **Emergency** card is now pinned to the **top**, with the **Buenos Aires Program** office card moved below it (Staff / Local Emergency Numbers / Resources unchanged). **Note:** in the Local link pills the WhatsApp/Instagram marks render in the uniform Pep-Orange link-pill color; the Contacts WhatsApp button keeps its brand-green. Verified in a dev preview across Contacts, Churches, Healthcare, and a Places listing; esbuild syntax-checked clean. |
| 2026-06-10j | **Removed the Apps sub-view from the Local tab** (documented under "Removed / dormant features" as a resurrectable former feature). `App.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. Dropped the `apps` entry from the Local hub (`LOCAL_SECTIONS`, replaced by a comment placeholder), the `sub === "apps"` render block, and the now-dead derived locals (`appsFilter`, `appsCategories`, `filteredApps`, `sortedApps`). **Data plumbing kept intact** — the `Apps` sheet tab, the `Code.gs` `TABS` entry, and `normalizeData`'s `data.apps` parsing all remain, so the fetched-data shape is unchanged (hence no cache bump) and resurrection is just re-adding the hub entry + render block. `AppGridIcon` retained (the Places "All" category tile still uses it); `<SectionDivider>` + `ColectivoIcon` retained as dormant. Verified in a dev preview: the Local hub now shows four sections (This Week, Places, Healthcare, Churches), Apps is gone, and the remaining sub-views open cleanly. |
| 2026-06-10i | **Local-tab reset reliability fix.** `App.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change. Tapping the Local bottom-nav tab from any Local sub-view returns to the category hub more robustly: `<LocalView>`'s reset effect now compares the `resetSignal` value against a `lastResetSignal` ref instead of a boolean "skip first run" guard. The old guard was defeated by React StrictMode's double-effect-invoke — the second mount invoke fired the reset and wiped a Today deep-link (e.g. the "This Week" tile), landing on the hub instead of the intended listing. The value-compare is StrictMode-safe (mount runs are no-ops) and still fires on a genuine re-tap. Verified in a dev preview (deep-link now lands on the listing; Local-tap-to-hub works from Healthcare, Places category listings, and a deep-linked listing). |
| 2026-06-10h | **Weekly Overview bottom week-nav chevrons.** `App.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. The Schedule tab's Weekly Overview now carries forward/backward week chevrons at the **bottom** of the day list (mirroring the existing top nav: same bounded `MIN_WEEK_OFFSET`/`MAX_WEEK_OFFSET`, same disabled treatment, with a centered week-range label between them), so a student who has scrolled through the week can move to the next/previous week without scrolling back up. |
| 2026-06-10g | **Bilingual day headers on the Schedule tab.** `App.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. Day-of-week headers now read Spanish-primary "Lunes / Monday" on both Schedule sub-views: the **Weekly Overview** day cards (`WEEK_DAYS_FULL_ES[d.getDay()] / WEEK_DAYS_FULL[d.getDay()]`) and the **Class Schedule** Mon–Fri grid (the per-day ternary now yields "Lunes / Monday" … "Viernes / Friday", uppercased by the existing header style). New module-scope `WEEK_DAYS_FULL_ES` constant (capitalized Spanish weekdays indexed by `Date.getDay()` to parallel `WEEK_DAYS_FULL`). Matches the app's Spanish-first posture. Verified in a dev preview. |
| 2026-06-10f | **Android Places-map stacking fix.** `PlacesMap.jsx` only; **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. On Android, tapping a pin on the Places map opened the place-info `<BottomSheet>` *behind* the map; iOS was fine. Root cause: the sheet portals to `<body>` at `z-index: 200`, while Leaflet's internal z-indices (panes ~200–700, control containers ~1000) live inside the `.leaflet-container`, which Leaflet leaves at `position: relative; z-index: auto` — i.e. it does **not** form a stacking context. On iOS an ancestor scroll container (`-webkit-overflow-scrolling: touch`) happens to form one that traps those high z-indices below the body-level sheet; Android has no such trap, so Leaflet's `1000` leaked to the root stacking context and painted over the sheet's `200`. **Fix:** the map container div now sets `position: relative; zIndex: 0; isolation: isolate`, forcing its own stacking context so every Leaflet z-index is contained beneath it — the map (and its controls) sit at the root's `z-index: 0`, well under the portaled sheet (`200`) and the bottom nav (`100`), on every platform. (As a bonus this also prevents Leaflet's controls from ever covering the bottom nav on Android.) Verified in a dev preview: map renders, pins/campus anchor intact, tapping a pin opens the card cleanly above the dimmed map. *The desktop preview can't reproduce the Android-only bug, but `isolation: isolate` is the standard remedy for this z-index-leak class.* |
| 2026-06-10e | **Local-tab polish (four small fixes).** One front-end pass (`App.jsx` only); **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. Verified in a dev preview against live data. **(1) Re-tapping the Local bottom-nav tab now returns to the Local hub.** Previously, tapping **Local** while already inside a Local sub-section (e.g. a Places category listing) did nothing visible — `<LocalView>`'s internal `sub` / `placesFilter` state only initialized from the `initialSub` prop once and never reacted to a re-tap (a null → null prop change fires no effect). Added a `localResetSignal` counter in `<App>`, bumped only when the Local nav button is tapped *while `tab === "local"`*, threaded to `<LocalView>` as `resetSignal`; a `useEffect` there (guarded by a `didLocalReset` ref so it skips the initial mount and doesn't clobber a Today deep-link) resets `sub` and `placesFilter` to `null`, collapsing all the way back to the category hub. Deep-links from Today (`jumpToTab`, which doesn't touch the signal) are unaffected. **(2) Nightlife glyph de-alcoholized.** The Places `nightlife` category glyph was a cocktail; replaced with a crescent-moon-and-stars (`MoonStarsIcon`, was `CocktailIcon`) — non-alcoholic, still reads as "a night out." **(3) Museum painting glyph reworked.** `PictureFrameIcon` (used by Places `museum` and the Events `exhibit` category) previously relied on a white canvas inside a coloured frame, which collapsed to a featureless white square when drawn in a single colour on the category disc; redesigned as an *outlined* frame (surface shows through as the canvas) with a sun + rolling hills painted inside, so it reads as a framed landscape in both render contexts. **(4) Places pills on one line.** The list/map toggle and the "Near you" pill on a Places listing wrapped to two rows; they now sit side-by-side on a single line. The shared "Near you" button label was shortened from **"Cerca tuyo / Near you"** to **"Cerca / Near"** (applies on Healthcare / Churches / Explore too, where it's alone on its row), and on Places the button is composed into the list/map toggle row (the optional location-permission note still renders beneath). New `nearMeButton` / `nearMeNote` pieces split out of the existing `nearMeControl` (which Healthcare / Churches / Explore still use unchanged). |
| 2026-06-10d | **Tier 2 #5 security hardening (GET→POST + vet audit log).** `AuthCode.gs` + `App.jsx`; **no `CACHE_VERSION` bump**, no new dependency. ⚠️ **Requires a manual `AuthCode.gs` re-deploy, and the re-deploy must land BEFORE this front-end commit reaches production** — otherwise the new POST calls hit the old script (which only routes these actions on GET) and identify/prompts break for the gap. The front-end push was held until the Director confirmed the re-deploy. **(#5a — credentials out of the URL.** `identifyUser` / `fetchPrompts` / `fetchAdminResponses` / `fetchAdminPlaces` now POST a `text/plain` JSON body (the same CORS-preflight-dodging pattern as `submitResponse` / `vetPlace`) instead of putting `token` + `cwid` + `birthday` in the GET query string, where they landed in the Apps Script execution log. Server side, `doPost` now routes `identify` / `prompts` / `admin_responses` / `admin_places` to the **same handlers** (they read `.cwid`/`.birthday` off their arg regardless of GET vs POST origin). `doGet` keeps routing them too, for backward compatibility with students on an older cached build — once those roll over, the GET routes can be deleted. The `places` read (cohort-token only, no per-user PII) intentionally stays GET. **(#5b — `handleVetPlace` hardening.** New **append-only `PlacesVetLog` tab** (auto-created on first vet, headers `logged_at, place_id, place_name, prev_status, new_status, show_credit, vetted_by_cwid, vetted_by_name`) records every vet action with its `prev_status → new_status`, so the history that the single `vetted_by` cell used to overwrite is now preserved. `handleVetPlace` reads the current status first (a deliberate transition, not a blind write), makes a no-change tap an **idempotent no-op** (`{ ok: true, unchanged: true }`, no write, no log row), and logs best-effort (a log hiccup never fails the vet — the authoritative status is already written). **`rejected → approved` stays allowed** by design — the dashboard's "one-tap flip to undo" depends on it — but is now fully audited in the log rather than silent. New `appendVetLog(entry)` helper + `PLACES_VET_LOG_TAB_NAME` / `PLACES_VET_LOG_HEADERS` constants. **Manual cutover:** re-deploy `AuthCode.gs` (Apps Script editor → Deploy → Manage deployments → pencil → New version); the `PlacesVetLog` tab auto-creates on the next Approve/Reject. No content-sheet change. |
| 2026-06-10c | **Calendar blank-tab fix + collapsible finals tiles.** One front-end pass (`App.jsx` only); **no `CACHE_VERSION` bump**, no Apps Script change, no new dependency. Verified against live data in a dev preview (Schedule→Calendar no longer lands past content; both finals tiles collapse/expand cleanly) — boots clean, no console errors. **(1) Calendar blank-tab bug.** All tabs render inside a **single persistent scroll container** (only the children swap on a tab change), so it inherited the previous tab's `scrollTop`. The Schedule tab auto-scrolls that container *down* to today's week card; switching to Calendar kept that offset, and on days when the live calendar has no upcoming-event "Hoy · Today" divider to re-anchor to (or on a taller viewport), Calendar was left scrolled past its content → blank. Reached from Local (container at top) it looked fine, which is why it was intermittent. **Fix:** a new `contentRef` on the content `<div>` + a `useEffect` keyed on `tab` that resets `scrollTop = 0` on every tab change; the views that intentionally auto-scroll to "today" (Weekly Overview, Calendar divider) then re-anchor from a clean baseline via their existing `requestAnimationFrame` + `scrollIntoView` effects. Confirmed via the live scroll trajectory: without the reset Calendar started at the inherited offset (611 px) and stuck; with it, it starts at ~0 and animates to the divider. **(2) Collapsible finals, default collapsed.** `<TodayFinalsTile>` (Today) and `<FinalsCard>` (Schedule) each gained a `const [open, setOpen] = useState(false)` and now render as a tappable header (a `<button aria-expanded>` with a ▼/▲ chevron matching the FAQ-accordion pattern) over a conditionally-rendered body. The collapsed header shows a quiet summary — `<TodayFinalsTile>`: "Exámenes finales / Finals coming up" + "{count} · {finals-window}"; `<FinalsCard>`: "Exámenes finales / Finals" + the window pill — and expands to the per-class exam rows (date · time · location). The Today tile, no longer a whole-tile jump-to-Schedule button, keeps a "Ver en Schedule / See all →" link at the bottom when expanded (still wired through `onJumpToTab`). The gating (`shouldShowFinalsUI` + non-empty `getStudentFinals`) is unchanged, so a non-personalized student or a term outside the finals window still sees nothing. **Tier 1–6 batches unaffected.** |
| 2026-06-10b | **Tier 6 batch (review backlog items 23, 24, 26).** One front-end pass (`App.jsx` only); **no `CACHE_VERSION` bump** (no fetched-data shape change), no Apps Script change, no new dependency. **`PROFILE_VERSION` 2 → 3** (item 24 adds a profile field). Verified against live data in a dev preview (FAQ + Places search filtering, share clipboard fallback, saved-count tile, and the announcement cue's mark-seen write + djb2 key + nav-dot markup) — boots clean, no console errors. **(23) Client-side search.** New reusable **`<SearchInput>`** (bordered field, magnifier glyph, clear ×, `type="search"`, bilingual `aria-label`). **FAQ** (`<FaqView>`) gains a search box that filters on title **+** content (case-insensitive), with a bilingual "N resultados / results" line and a "Probá con otra palabra / Try another word" empty state; the expand state keys on the row's original index so filtering never reshuffles which card is open. **Places** gains a search box **on the category-picker hub**: while the query is non-empty it replaces the grid with a flat list of `<PlaceCard>`s matching name / why / neighborhood / category label across **all** categories (the high-value "find it by name without knowing its bucket" case); list-only (no map / near-you on results). `placesQuery` state clears whenever the student drills into a category or leaves the Places sub. **(24) Announcement unread cue.** A Pep-Orange dot on the **Today bottom-nav tab** when an active announcement hasn't been seen yet; shown only while the student is on another tab (on Today the banner is already in view) and cleared the moment Today is the active tab. New `seenAnnouncements: []` profile field (→ the `PROFILE_VERSION` bump, with a **v2 → v3 salvage migration** that keeps `enrolledClasses` / `filterEnabled`; defaults `seenAnnouncements` to `[]` so the deploy doesn't light the dot for announcements already on screen for days). Helpers: `isAnnouncementActive(a, todayStr)` (shared with `<AnnouncementBanner>`, which was refactored to use it so the two never disagree), `djb2(str)` (short stable hash), `announcementKey(a)` (`message|start_date|end_date` → key), `activeAnnouncementKeys(announcements, todayStr)`. App-level: a `hasUnreadAnnouncements` `useMemo` + a mark-seen effect (fires on the Today tab, writes only still-active keys so the stored list doesn't grow unbounded, guarded against re-render loops). The dot carries a bilingual `aria-label` on the Today nav button; the dot span itself is `aria-hidden`. **(26) Saved places: count + share.** The **♥ Saved hub tile** shows its count once > 0 ("Saved · 3"). **`<PlaceCard>`** gains a **share button** beside the ♥: `navigator.share` where available (native sheet), else a clipboard-copy fallback (text + a Maps URL — `maps_url` if present, otherwise a Maps-search URL built from the address) with a brief "¡Copiado! / Copied" confirmation; user-cancel (AbortError) and blocked-clipboard are swallowed silently. The save + share buttons now sit in a small right-aligned button group. **Tier 1–5 unaffected.** |
| 2026-06-10 | **Tier 5 UX-consistency & accessibility batch (review backlog items 18–22).** One front-end pass (`App.jsx` only); **no `CACHE_VERSION` bump** (no fetched-data shape change), no Apps Script change, no new dependency. Verified in a dev preview with synthetic keyboard/pointer events (focus-trap both directions, Escape-closes-topmost on stacked dialogs, focus-return, status pill, nav landmark, FilterPill/FAQ aria, refresh button aria-busy) — boots clean, no console warnings/errors. **(18) Client-side required validation in `<PromptForm>`.** `handleSubmit` now walks the prompt's fields and rejects a blank required field with the same `"Falta completar: ${label} / Missing: ${label}"` inline error *before* the 2–4 s round trip, matching `<PlaceSubmitForm>`. Type-aware empties: `multi_select` → empty array, everything else → empty/whitespace string (an unanswered boolean is `""` per the 2026-06-09k init fix, so it's caught here too). The server-side `validation_failed` mapping stays as the authoritative backstop. **(19) Non-touch refresh affordance on Today.** A real circular refresh `<button>` sits at the top-right of the greeting strip (above the decorative sun/moon), runs the existing `triggerRefresh` path (force-refetch weather + dólar bypassing the 30-min TTL, plus `onRefreshData()` with `?bust=1`), spins via `bap-spin` while in flight, and carries `aria-label="Actualizar / Refresh"` + `aria-busy` + `disabled` during refresh. The pull-to-refresh gesture is unchanged — this is an addition for desktop / keyboard / assistive-tech users and the laptop-bound Director workflow. **(22) Centered confirm card replaces `window.confirm`.** New reusable `<ConfirmDialog>` (portaled to `<body>`, `role="dialog"` + `aria-modal`, Pep-Orange destructive button + neutral cancel, bilingual ES-primary title/body via `lang="es"`) drives ProfileModal's profile-reset and sign-out, replacing the two cramped native dialogs. Built on the new dialog-a11y baseline (focus defaults to Cancel — the safe choice on a destructive prompt — Escape cancels, focus returns to the trigger). Stacks correctly over ProfileModal (zIndex 300 vs 200). **(21) Modal + nav semantics.** New shared **`useDialogA11y(ref, { open, onClose })`** hook: stashes the trigger, moves focus into the dialog, traps Tab/Shift+Tab, Escape-closes, and restores focus on close. A module-level **`__dialogStack`** makes Escape close only the *topmost* dialog (verified: a ConfirmDialog over ProfileModal closes alone on Escape). Wired into `<BottomSheet>`, `<ProfileModal>`, `<ConfirmDialog>`, `<DirectorResponsesView>`, and `<DirectorPlacesView>` — the last two (and ProfileModal) also gained `role="dialog"` / `aria-modal` / a labelled focusable container. Cheap aria wins: `aria-pressed` on `<FilterPill>`, `aria-expanded` on the FAQ accordion header (its ▼ chevron now `aria-hidden`), a `<nav aria-label>` landmark with `aria-current="page"` on the active tab, `role="status"` + `aria-live="polite"` on the header status pill, and the three `role="button"` divs (the "My classes only" pill, `<TodayFinalsTile>`, `<EventsTodayTile>`) converted to real `<button>`s (zero `role="button"` divs remain). **(20) Wayfinding bilingual sweep.** The six status-pill labels are now **Spanish-only** (Director's call: terse Spanish fits the narrow 10 px DM Mono pill and matches the app's Spanish-first posture) — `Sincronizado` / `Actualizando…` / `Cargando…` / `Guardado (offline)` / `Sin conexión` / `Vista previa`, wrapped in `lang="es"`. English-only `aria-label`s (`Close`, `Previous/Next week`, `Open settings`, `Required`, the stat-tile labels, the dólar amount input, etc.) swept to the bilingual `"Español / English"` convention. `lang="es"` added to the cleanly-Spanish greeting line + date on Today; the root stays `<html lang="en">` and interleaved bilingual strings were intentionally left untagged (low value, high churn). **Known remaining gaps (documented, not fixed):** the inline-`transition` reduced-motion leftover from Tier 4 (BottomSheet slide, FAQ chevron, toggle thumb) and a fuller ProfileModal-body bilingual pass are out of scope here. **Tier 1/2/3/4 unaffected.** |
| 2026-06-09l | **Tier 4 phone-polish batch (review backlog items 13–17; #12 stays parked).** One `App.jsx` + `PlacesMap.jsx` + manifest + icon-asset pass; **no `CACHE_VERSION` bump** (no fetched-data shape change), no Apps Script change, no new *shipped* dependency. **(14) `prefers-reduced-motion` gaps closed.** The reduce media-query block now also sets `.bap-sun-rotate { animation: none }` (the 80 s Today-greeting sun spin) and `.bap-steam { animation: none }` (the mate-gourd steam loop) — both *infinite* animations that were slipping the gate. The steam wisps' base style is `opacity:0`, so with the animation off they simply don't render. The doc's greeting-strip claim that the sun is "suppressed under `prefers-reduced-motion`" is now actually true. *Known remaining gap (documented, not fixed):* a handful of **inline** `transition:` styles (BottomSheet slide, FAQ chevron, toggle thumb) bypass the class-based gate; suppressing those needs a JS `matchMedia` check and was left for a focused follow-up. **(17) Touch targets.** `<FilterPill>` (the primary filter UI on This Week / Healthcare / Churches / Apps / Places) went from `3px 11px` padding (~19–22 px tall) to `9px 14px` + `minHeight:38` + inline-flex centering + `borderRadius:19` so it reads as a proper pill; its inactive text also moved stone→mountain (part of #13). The `<PlaceCard>` ♥ save toggle grew its hit area to ~40 px (`padding:10` + negative margin to absorb the layout shift) while keeping the glyph visually 19 px. **(13) Stone-as-text contrast → AA.** Tertiary text in stone (`#7A99AC`, ~3:1 on white / ~2.7:1 on parchment — below the 4.5:1 AA bar at the 10–13 px sizes it's used at) was darkened to mountain (`#425563`, ~7.8:1) across **~96** inline-style `color: C.stone` sites (captions, hours, labels, inactive nav, empty-state copy, the "Logged in as" card). **Scope guard:** the sweep touched **text only** — `C.stone` is kept for borders, dividers, SVG strokes, backgrounds, and disabled-state ternaries (checkbox/credit-toggle borders, `submitting`/`busy` button fills, the unsaved-♥ glyph). The two `PLACE_CATEGORIES` rows whose `color` token is `C.stone` (`study`, `other`) were **deliberately left as stone** — that token also paints the category's filled disc/map-pin, so repainting it is a brand decision, not a text-contrast fix. *Separate narrower finding flagged for follow-up:* white glyphs on those stone-tinted category discs sit at ~2.9:1 (just under the 3:1 non-text bar) — a category-palette recolor, out of scope here. Sky-as-caption (`culture` category label rendered via `meta.color`) left for the same reason. **(16) Manifest gaps.** Added `id:"/"`, `lang:"en"`, `orientation:"portrait"`, `categories:["education"]`, marked the existing 192/512 icons `purpose:"any"`, and added a **maskable** 512 variant (`/icon-512-maskable.png`, badge scaled into the ~80 % safe zone on an extended Pep-Blue field) so Android adaptive launchers stop letterboxing the logo in a white circle. **(15) Heavyweight assets.** **(a)** `public/icon-512.png` recompressed **302 KB → 37 KB** via 128-colour palette quantization (no visible banding on the sun gradient; verified), and `icon-192` (54→6 KB), `apple-touch-icon` (48→5 KB), `logo.png` (47→6 KB) the same way; the SW precache dropped from ~750 KB to **460 KiB** (every first install is lighter). **(b)** The lazy `PlacesMap-*` chunk went **68 → 45.5 KB gz** by dropping `react-dom/server`: glyph pins are now mounted from the live category components via `react-dom/client`'s `createRoot` (`makeDiscEl` returns an element; `discIcon` wraps it; roots tracked and unmounted via `queueMicrotask` on cleanup) instead of `renderToStaticMarkup`. `react-dom/client` is already the app's entry, so it's shared at no chunk cost, and the pins can never diverge from the app's icon set. The campus pin keeps its static `CAP_SVG` string. **Verified in a dev preview:** boots clean (no console warnings/errors), darker captions read well, taller filter pills, and the **map renders** — CARTO tiles, the Casa-Holden campus pin, and category pins all mount via `createRoot` with no errors, and toggling Lista↔Mapa unmounts cleanly. **Tooling note:** icon recompression + maskable generation used `sharp` installed with `--no-save` (not added to `package.json`; not a shipped dependency). **(12) iOS safe-area insets stays parked** (cosmetic, needs on-device iteration — see the Tier 4 #12 write-up). **Tier 1/2/3 unaffected.** |
| 2026-06-09k | **Tier 3 bug-fix batch (review backlog items 6–11).** One batched `App.jsx` + `PlacesMap.jsx` pass; **no `CACHE_VERSION` bump** (no fetched-data shape change), no Apps Script change, no new dependency. **(6) Boolean prompt fields no longer pre-select "No."** `initFormFromPrompt` now initializes a *never-answered* boolean to `""` (not `false`), so neither Sí/No pill lights up until the student chooses, and a skipped required boolean is caught as `missing_field` server-side instead of being silently recorded as a `FALSE` decline (matters for the documented "¿Vas?" excursion sign-up driving a bus headcount). A genuinely-stored `TRUE`/`FALSE` still reflects as the selected pill; the readonly renderer already shows `""` as "Sin respuesta." **(7) `<PromptForm>` no longer resurrects abandoned edits.** The reset effect now also fires on the open→true transition (deps `[open, promptKey]` with an `if (!open) return` guard), so open → edit a field → close without submitting → reopen re-initializes from the *stored* responses rather than showing the unsaved edits as if saved. The sticky-`lastPromptRef` close-animation behavior and the "a background prompts refresh while open won't wipe in-progress edits" guarantee are both preserved (`submitted_at` is still not a dep). **(8) Finals UI now turns off after the window passes.** `shouldShowFinalsUI` gained a lower bound: it returns `false` once `daysUntil(finals_window_end) < 0` (falling back to `finals_window_start` when `finals_window_end` is blank, e.g. a single-day Summer window). Previously the gate was `d <= 14` with no floor, so the FinalsCard and Today tile persisted forever after finals ended (invisible for Summer since the term ends, but wrong for Fall/Spring). The no-window "any class has a final assigned" fallback is unchanged. Date math uses the existing local-noon (`+ "T12:00:00"`) anchoring via `daysUntil`. **(9) `parseDays` handles weekends and whitespace.** `DAY_LETTER_MAP` gained `S: "Sat"` and `U: "Sun"` (S taken by Sat, so Sunday is `U`, mirroring the existing `R`=Thu convention); the named-day branch now matches Sat/Sun and splits on commas **or** whitespace and normalizes each token to the canonical three-letter abbrev. Fixes: a `days` cell of `Sat` rendered the class on **Tuesday** (it missed the Mon–Fri-only regex, fell to the single-letter path, and `t`→Tue); `Sun` vanished; a comma-less typo like `Mon Wed` silently dropped the class everywhere. `DAY_ABBREV` also gained `Sat: "S"` / `Sun: "U"` so the "All Courses" compact schedule renders cleanly. Weekend classes now surface correctly on **Today** and the **Weekly Overview** (both iterate real day-of-week via `WEEK_DAYS_SHORT`, which already included Sat/Sun). **The Mon–Fri "Class Schedule" grid was intentionally left Mon–Fri** (Director's call — `DAYS_ORDER` unchanged): it's a catalog view, weekend classes are rare, and the parser fix is correct regardless; weekend columns can be added later if a cohort actually needs them. **(10) Error boundary around the lazy Places map.** New `<MapErrorBoundary>` class component (React error boundaries must be classes; `Component` added to the `react` import) wraps the `<Suspense>`/`<PlacesMap>` mount inside the `showMap` branch. `Suspense` doesn't catch dynamic-`import()` *rejections*, so after a deploy a student still on the old build who taps "Mapa" for the first time would 404 on the old hashed `PlacesMap-*.js` chunk and the uncaught rejection white-screened the **whole** app; now the failure is scoped to the map with a bilingual "No se pudo cargar el mapa. Probá recargar / Couldn't load the map — try reloading" fallback, and toggling to Lista and back remounts a fresh boundary. **(11) Grab-bag:** `<DirectorPlacesView>`'s local `creditMap` now clears on open/close and on every fresh `places` payload (so a sheet-side `show_credit` edit isn't silently overwritten by a stale in-memory toggle on the next Approve); `formatPromptCutoff` compares the close time in **minutes** via `toMinutes` instead of lexically (a non-zero-padded `end_time` of `9:00` no longer reads as still-open at 10:00); `PlacesMap` markers look the place up by `place_id` at click time (a `placesByIdRef` mirror of the `selectRef` pattern) so a background `fetchPlaces` refresh that edits a place's name/why/hours no longer opens a stale card; both Today live-data cache writers (mount effect + pull-to-refresh) re-read `loadTodayCache()` inside each `.then` instead of merging onto a captured snapshot, so concurrent weather/dólar writes can't clobber each other; the `PlaceToast` auto-clear now uses a `placeToastTimerRef` cleared before each new toast (a second toast within 4 s no longer gets cut short by the first's timer); the non-interactive empty-state stat tile dropped its misleading `bap-press` scale-on-tap; and `<BottomSheet>`'s body-scroll lock is now a module-level ref-count (`__bottomSheetLockCount`) so stacked sheets restore `document.body.style.overflow` in the right order (latent today, correct for the first programmatic sheet-over-sheet). **NON-bugs deliberately left alone (verified, do not "fix"):** date parsing already uses local-noon anchoring throughout; the 2026-05-16 `useMemo` deps are correct; `safeExternalUrl` already allowlists URL schemes. **Tier 1 (geolocation/pinch-zoom, shipped 2026-06-09) and Tier 2 (`AuthCode.gs` formula-injection guard + identify throttle, code shipped 2026-06-09, ⚠️ still needs a manual Apps Script re-deploy to go live) are unaffected by this batch.** |
| 2026-06-09j | **Reverted the iOS edge-to-edge attempt (09h + 09i).** The `viewport-fit=cover` + `env(safe-area-inset-*)` pass fought the device's safe areas on a real iPhone — content sat too close to the status bar at the top and a persistent white patch remained under the bottom nav — and didn't settle across two on-device tuning rounds (additive→`max()`, nav-pill tracking, reduced reservation). Reverted `App.jsx` to byte-for-byte the pre-09h layout (flat paddings, gear `top: 12`, nav `8px 0 16px`, pill `bottom: 6px`, the three overlay headers/footers + BottomSheet/ProfileModal bodies all back to their flat values) and dropped `viewport-fit=cover` from `index.html` so iOS goes back to auto-insetting content clear of both the status bar and the home indicator — the original known-good behavior the app shipped with for months. **The pinch-zoom fix is retained** (viewport is `width=device-width, initial-scale=1.0`, no `maximum-scale`/`user-scalable=no`). Net effect on layout vs. before this whole episode: identical, plus zoom is now allowed. Tier 4 #12 in the roadmap is back to **not-done**, with the lesson recorded: cover-based edge-to-edge needs deliberate, iterative on-device testing (env() can't be eyeballed in a desktop preview), and the payoff (gradient behind the status bar) is cosmetic — low priority relative to the rest of the backlog. No `CACHE_VERSION` bump. |
| 2026-06-09i | **iOS safe-area insets — spacing tuning (follow-up to 09h).** On-device the 09h pass was too loose at the top and showed a gap at the bottom; both came from using **additive** `calc(base + env(safe-area-inset-*))`, which stacks the base on top of the (large, ~59 px on Dynamic Island phones) inset. Switched every inset to **non-additive `max(base, env(...))`** so content sits flush at the safe boundary (the standard pattern): main + overlay header tops, gear `top`, ProfileModal/BottomSheet bodies, and both Director footers. Two bottom-specific fixes: (a) the bottom nav's reservation is now `max(16px, calc(env(safe-area-inset-bottom) − 10px))` (tighter than the full inset, which the Director found too padded), and (b) the active-tab **pill** (`.bap-nav-pill`, CSS `bottom`) now tracks that same reservation — it had been pinned at `bottom: 6px` while the labels rode up on the increased padding, leaving the pill stranded as a lone line near the home indicator with a white gap above it. The Places FAB, `<PlaceToast>`, and the scroll-content `padding-bottom` were reverted to flat values (`90` / `84` / `100px`) since the tighter nav no longer needs them pushed up by the inset. Pure layout; no `CACHE_VERSION` bump. |
| 2026-06-09h | **iOS safe-area insets (Tier 4 #12 of the review backlog).** Makes the installed PWA sit correctly on notched iPhones. Re-added `viewport-fit=cover` to the `index.html` viewport meta and now consume `env(safe-area-inset-*)` at every fixed/full-screen edge in `App.jsx`: the main header gets `padding-top: calc(16px + inset-top)` and the absolutely-positioned gear's `top` is offset by the same inset (it was landing under the status-bar battery); the fixed bottom nav's `padding-bottom` becomes `calc(8px + inset-bottom)` (replacing a flat `16px` that, post-cover, read as a dead white strip over the home indicator) and the scrolling content area's `padding-bottom` becomes `calc(100px + inset-bottom)` so the last card still clears the now-taller nav; the Places "+" FAB (`bottom: calc(90px + inset-bottom)`) and `<PlaceToast>` (`bottom: calc(84px + inset-bottom)`) float up by the inset to stay above the nav; the three full-screen overlay headers (`<ProfileModal>` + `<DirectorResponsesView>` + `<DirectorPlacesView>` — an identical gradient-header block, updated with a single `replace_all`) get the same top inset so their ×/back/title row clears the status bar, and their footers + the `<ProfileModal>` body + the `<BottomSheet>` body get `inset-bottom` added so buttons clear the home indicator. `<PasscodeGate>` / `<UserGate>` were intentionally left alone (vertically centered, full-bleed gradient — cover is harmless there). **Backstory:** a 2026-06-09 attempt added `viewport-fit=cover` alone without the padding and regressed both ends (gear under the battery, dead bottom strip); it was reverted same-day, and this commit re-adds cover together with the full padding pass, the way it must ship. Pure layout — **no `CACHE_VERSION` bump**, no Apps Script change, no sheet-schema change, no new dependency. *Still worth a real-device check:* exact inset spacing on a notched iPhone (env() can't be eyeballed in a desktop preview) |
| 2026-06-09g | **Places map (Phase 2 of the spatial roadmap).** Realizes the deferred embedded-map half: a **Lista / Mapa** toggle on every Places listing opens an interactive **Leaflet** map. **New file `src/PlacesMap.jsx`** holds the map so `App.jsx`'s `lazy(() => import("./PlacesMap.jsx"))` **code-splits Leaflet into its own chunk** — it never touches first paint or the main bundle, and `workbox.globIgnores: ["**/PlacesMap-*"]` keeps it out of the SW precache (online-only; tiles need network and aren't cached). **Tiles:** CARTO Positron raster (`light_all`, no key at cohort volume; OSM + CARTO attribution). **Pins:** raw Leaflet `divIcon` discs in the place's `PLACE_CATEGORIES` color, each with the category's own glyph rendered to static SVG via `react-dom/server`'s `renderToStaticMarkup` (kept entirely inside the lazy chunk — ~23 KB gz there, accepted for legibility since the chunk is online-only and browser-cached after first open); an injected `bap-leaflet-styles` block strips Leaflet's default `.leaflet-div-icon` white box. **Casa Holden** (Pepperdine BA campus) is a hardcoded `CAMPUS_ANCHOR` constant (coords `-34.5687288,-58.4416504`, resolved from the program-office Maps link) rendered on **every** map with a distinct Pep-Blue, Pep-Orange-ringed mortarboard pin + a permanent label; because the campus always anchors the map, the Lista/Mapa toggle is offered on every listing (not only ones with located rows). **Tapping a place pin** opens the real `<PlaceCard>` (with a working ♥ save toggle, `saved` read live from `savedPlaces`) in a `<BottomSheet>` — `<PlacesMap>` calls an `onSelectPlace(place)` prop, `<LocalView>` holds `selectedMapPlace` state. The map is **online-only**: the Map toggle disables (and the view falls back to the list) when `navigator.onLine` is false, with a bilingual "El mapa necesita conexión / The map needs a connection" note; the list remains the canonical offline experience. **`<App>`/`<LocalView>` additions:** `lazy`/`Suspense` import; `PlacesMap` lazy ref; `placesView` ("list"/"map") + `online` + `selectedMapPlace` state; the `viewToggle`; `mapPlaces` (located rows enriched with `_color`/`_catLabel`/`_Icon`). **New dependency: `leaflet@^1.9.4`** (the project's first beyond React; flagged + approved). **No `CACHE_VERSION` bump** (data shape untouched; the map reads existing `lat`/`lng`), no `Code.gs`/`AuthCode.gs` change, no sheet-schema change. *Real-device note still open:* iOS-PWA gesture interplay (Leaflet touch/drag vs. page scroll + bottom-nav) on an installed home-screen PWA |
| 2026-06-09c | **Places — Stage 2 (submit + vet pipeline).** Second half of the Places feature: students can now suggest places in-app and staff vet them, closing the submit → queue → vet → publish loop. **Submission:** a Pep-Blue **"+" floating action button** (rendered whenever the Local tab's Places sub is open, over both the category grid and any listing; `onOpenSuggest` threaded from `<App>`) opens **`<PlaceSubmitForm>`** — a self-contained four-field `<BottomSheet>` (Name, Category as tappable cards from `PLACE_CATEGORY_ORDER`, "Maps link or address", one-line "Why"), **not** the dynamic prompts machinery. On submit the single location field is split client-side (a value starting `http(s)` → `maps_url`, else → `address`) and POSTed via `submitPlace(...)`; on success the sheet closes and a new **`<PlaceToast>`** confirmation pill ("¡Gracias! Enviado para revisión / Thanks — sent for review") slides up for 4 s. The row lands as `status=pending, source=community` with `submitted_by_cwid` + `submitted_by_name` (preferred-or-first) and a blank `show_credit` (= default-on). **Vetting:** a second staff-only **"Revisar lugares / Review places"** button in `<ProfileModal>` (gated on `isStaff`, beside "Ver respuestas") opens **`<DirectorPlacesView>`** — a near-clone of `<DirectorResponsesView>`'s overlay shell (gradient header "Director · Solo personal / Lugares · Revisión", top-pinned loading/error rows, footer Refresh + Close). It reads `fetchAdminPlaces(...)` (all statuses) and renders **Pendientes** first (Pep-Orange-striped cards: name, category chip, why, `<AddressLink>`, submitter, a per-place **"Mostrar su nombre / Show their name" credit toggle** defaulting on, and **Aprobar / Rechazar** buttons), then lighter **Aprobados** / **Rechazados** history sections with a single flip action each. Approve/Reject call `handleVetPlace(...)` → `vetPlace(...)`, which on success **reloads the admin payload AND refreshes the public list** (`refreshPlaces` → `fetchPlaces` + `savePlacesCache`) so an approval reaches students immediately. **Helpers:** `submitPlace` / `fetchAdminPlaces` / `vetPlace` (next to `fetchPlaces`, mirroring `submitResponse` / `fetchAdminResponses` — same `AuthError` / `NoMatchError` / `ForbiddenError` / `SubmitError` conventions); `sortAdminPlaces` (pending → approved → rejected, newest-first within a band); `placeCreditOn` (blank `show_credit` = on). **App state/handlers:** `suggestOpen` / `placeToast` + the `placesAdmin{Open,Payload,Loading,Error}` quartet; `handleSubmitPlace`, `loadAdminPlaces`, `handleOpenPlacesAdmin` / `handleClosePlacesAdmin`, `handleVetPlace`, `refreshPlaces` (all mirroring the `director*` handlers' gate-clearing). New `bap-toast-in` keyframe (honors `prefers-reduced-motion`). **`AuthCode.gs`:** `handleSubmitPlace` (identity gate + `validatePlaceSubmission` + `LockService` + `appendPlaceRow`, which auto-creates the `Places` tab from `PLACES_HEADERS` if missing; `place_id` = `c_<slug>_<uuid8>` via new `slugifyPlaceName`), `handleAdminPlaces` (staff-only, `curateAdminPlaceRow` keeps the submitter + vetting columns), `handleVetPlace` (staff-only, `LockService`, header-indexed in-place update of `status` / `show_credit` / `vetted_by`); router wiring for `submit_place` / `vet_place` (doPost) and `admin_places` (doGet). **No `CACHE_VERSION` bump** (content shape untouched; Places keeps its own cache). **Manual cutover:** re-deploy `AuthCode.gs` (Apps Script editor → Deploy → Manage deployments → pencil icon → New version) so the three new endpoints are live; the `Places` tab auto-creates on first submission, but Stage 1 seed rows should already exist; run `validatePlaces()` after any manual edits. No `Code.gs` change; no sheet-schema migration; no new dependencies |
| 2026-06-09 | **Places: category-picker grid + dedicated "Places" page header.** Reworks the Local tab's Places navigation from "drop straight into the listing behind a filter-pill row" to a **two-level flow**: tapping **Places** in the Local hub now lands on a **two-column grid of category glyph tiles** (each a solid category-color circle with a white glyph, matching `<PlaceCard>`, and the label beneath), and tapping a tile drills into that category's listing. **Tile set & order:** **All** first (neutral Pep-Blue tile using the `AppGridIcon` grid glyph), then **all twelve** categories in a new module-scope `PLACE_CATEGORY_ORDER` constant — **sightseeing-first** (Sights, Museum, Culture, Theater, Markets, Neighborhood, then Café, Restaurant, Nightlife, Outdoors, Fitness, Study spot) — plus a **♥ Saved** tile that appears only once the student has saved a place. **This supersedes the 2026-06-08c present-gating for Places navigation:** the grid intentionally shows **all twelve** categories regardless of whether each currently has approved rows (Director's call — a complete, inviting menu that also advertises the buckets students can suggest places for), so an empty bucket now renders the existing "Todavía no hay lugares acá. / Nothing here yet." empty state when tapped rather than being hidden. The old `FilterPill` row in the places branch is removed; the `placesFilter` state now starts at `null` (= show the grid) and holds `"all" | <category key> | "saved"` once a tile is chosen. **Two-level back nav:** the grid's "← Volver / Back" returns to the Local hub; the listing's "← Lugares / Places" returns to the grid. The reused `nearMeControl` ("Cerca tuyo / Near you") and `<PlaceCard>` rendering are unchanged on the listing. **Header:** while anywhere in Places the App-level `<SectionTitle>` reads **"Places"** with the Spanish gloss suppressed, instead of "Local Resources / BUENOS AIRES"; once a category is chosen, its label is appended as a lighter **breadcrumb sub-header** beside the headline (**"Places · Café"**, "Places · All", "Places · ♥ Saved") rather than sitting in the in-listing back row; every other Local sub-section is unchanged. Implemented by: `<SectionTitle>` gaining an optional `override` prop (`{ en, es, sub }`; a null/blank `es` hides the gloss line, an optional `sub` renders the breadcrumb beside the headline); `<LocalView>` gaining an `onSubChange(sub, placesViewLabel)` callback that reports its active `sub` and chosen-view label up via `useEffect`; and `<App>` tracking `localSub` + `localPlacesLabel` state (set synchronously in `jumpToTab` and the bottom-nav handler to avoid a one-frame header flash on remount, then kept in sync by the callback). Today's empty-state "Explorar BA →" deep-link now lands on the Places **grid** rather than the listing. Front-end-only; **no data-shape change; `CACHE_VERSION` stays at 7**; no sheet edits; no Apps Script cutover; no new dependencies |
| 2026-06-08c | **Places — Stage 1 (read path).** First half of the roadmap Places feature: the Local tab's "Explore BA" category is renamed **Places** and now reads its rows from a new **approved-places list on the auth/Roster side** (`?action=places`, cohort-token-gated — no per-user identity needed to read public-ish approved content), instead of the content sheet's `Explore` tab. **Categories:** a new `PLACE_CATEGORIES` constant + `getPlaceCategory(key)` (same `{ label, es, color, Icon }` shape as `EVENT_CATEGORIES`, `other` fallback) defines a **blended 12-category set** — daily-life buckets Café · Restaurant · Nightlife · Outdoors · Fitness · Study spot, plus sightseeing buckets Museum · Culture · Theater · Sights · Markets · Neighborhood. The blend was chosen after comparing against the live Explore content (43 rows, ~77% cultural/sightseeing — Theater, Historic Site, Museum, Cultural Center, Landmark — which would have collapsed into a single "Culture" bucket under a tighter set). Filter pills are **present-gated**, so the empty student-life buckets stay hidden until submissions arrive (at launch only the ~7 seed-present categories show). Seven new inline-SVG glyphs join the library: `CoffeeCupIcon`, `CocktailIcon`, `DumbbellIcon`, `BookIcon`, `MarketIcon`, `NeighborhoodIcon`, `ColumnsIcon` (Restaurant reuses `ForkPlateIcon`, Outdoors `PalmIcon`, Museum `PictureFrameIcon`, Theater `TheaterMaskIcon`, Sights `ObeliscoIcon`; Culture uses the new `ColumnsIcon`). **`<PlaceCard>`:** category circle + glyph, name, ♥ save toggle, the one-line `why`, neighborhood, `<AddressLink>` (honors `maps_url`, falls back to an "Open in Maps" link when only `maps_url` is set), `hours`, `<LinkButton>`, and a "Sugerido por … / Suggested by …" credit line for community submissions the Director attributed (default-on: a blank `show_credit` on a community row shows the name; seed rows never show one). **`<LocalView>` places branch:** category `FilterPill` row (ordered by `PLACE_CATEGORIES`, only categories present) + a ♥ Saved pill (appears once the student has saved anything) + the reused `nearMeControl`; the "Cerca tuyo / Near you" distance sort is reused, with place coords (strings from the script) normalized via `parseCoord` before sorting. Empty states for "nothing here yet" and "no saved places yet." **Front-end plumbing:** `fetchPlaces({token})`; a device-local places cache (`bap-places-cache`, `{places,ts}` envelope, 10-min TTL, `loadPlacesCache`/`savePlacesCache`); a device-local saves list (`bap-places-saves`, `loadSavedPlaces`/`toggleSavedPlace`, never synced); App-level `places` (lazy-init from cache) + `savedPlaces` state and a background fetch effect that fires once the cohort token is present and rewrites the cache; the dead `exploreFilter`/`exploreTypes`/`filteredExplore`/`displayExplore` locals were removed. Today's empty-state "Explorar BA →" deep-link now targets the Places listing (`jumpToTab("local", "places")`). **`AuthCode.gs`:** `PLACES_TAB_NAME`/`PLACES_HEADERS`/`PLACE_CATEGORY_KEYS`/`PLACE_STATUSES`/`PLACE_SOURCES` constants, `handlePlaces` (filters to `status=approved`, `curatePlaceRow` drops the submitter CWID + internal vetting columns and blanks the submitter name when credit is off), and `validatePlaces()` editor helper mirroring `validatePrompts`. `normalizeData` still parses the content `Explore` tab (now unused) so **`CACHE_VERSION` stays at 7** — no bump. **Stage 2 (shipped 2026-06-09c, see the row above):** the "+ Suggest a place" FAB submission form (`submit_place`) and the staff-only vetting dashboard (`admin_places` + `vet_place`). **Manual cutover for Stage 1:** (a) create a `Places` tab on the Roster spreadsheet with the `PLACES_HEADERS` columns, (b) migrate the current Explore BA rows in as `source=seed, status=approved` with a mapped `category` and (where known) `lat`/`lng`, (c) re-deploy `AuthCode.gs` (Apps Script editor → Deploy → Manage deployments → pencil icon → New version) so `?action=places` is live, (d) run `validatePlaces()` to confirm no warnings. Front-end + new auth-side read endpoint; no content-sheet schema change; no new dependencies |
| 2026-06-08 | **Local tab category hub (menu-first navigation).** The Local tab no longer drops the student straight into a listing behind a five-pill, two-row selector; it now opens to a **hub of five stacked, full-width category buttons** (This Week, Explore BA, Healthcare, Churches, Apps), each with a glyph in a tinted Ice container, an EB Garamond English label + Roboto Spanish gloss, a 4 px left accent stripe in the category's color, and a → chevron. Tapping a button drills into that category's existing listing; a "Volver / Back" header (DM Mono pill + the EB Garamond section title) returns to the hub. **Forward-looking design:** the hub is driven by a single module-scope `LOCAL_SECTIONS` config array (`{ key, en, es, Icon, accent }`), so the roadmap **Places** feature renames/replaces the "Explore BA" entry in one line, and the per-category listing screen becomes the natural home for Places' future "+ Suggest a place" entry point. **Glyphs:** three new inline-SVGs `HealthCrossIcon` / `ChurchIcon` / `AppGridIcon`; the previously defined-but-unused `ObeliscoIcon` now anchors Explore BA and `SparkleIcon` anchors This Week. **Plumbing:** `<LocalView>` gains an `initialSub` prop (default `null` → hub); `<App>` threads it via a new `jumpToTab(tabKey, sub)` wrapper (passed to `<TodayView>` as `onJumpToTab`) and `localInitialSub` state, so Today's deep-links land on a specific listing — the "Esta semana / This Week" events tile → This Week listing, the empty-state "Explorar BA →" button → Explore BA listing — while a normal Local-nav tap clears the flag and lands on the hub. All listing internals (`<EventsView>`, the health/churches/explore/apps cards, `<FilterPill>` rows, and the entire "Cerca tuyo / Near you" distance-sort path) are unchanged. Front-end-only navigation/display change; **no data-shape change; `CACHE_VERSION` stays at 7**; no sheet edits; no Apps Script cutover; no new dependencies |
| 2026-06-07 | **"Cerca tuyo / Near you" distance sort on the Local tab (Explore BA, Healthcare, Churches).** Realizes Phase 1 of the Places spatial roadmap — spatial value (*what's closest*) with **no map and no new dependency**. **Data:** new optional `lat` / `lng` columns on the Explore, Health, and Churches content tabs; `normalizeData` parses them via a new `parseCoord(v)` helper (blank / non-numeric → `null`). New columns flow through the existing Apps Script automatically, so **no `Code.gs` change and no re-deploy** — but `CACHE_VERSION` bumps **6 → 7** because the app now reads new fields. **Helpers:** `haversineKm(lat1,lng1,lat2,lng2)` (great-circle km); `formatDistance(km)` (language-neutral `~600 m` / `~1,4 km`, Spanish decimal comma, sub-1km rounded to nearest 10 m); `getUserLocation()` (Promise wrapper around `navigator.geolocation.getCurrentPosition` — `enableHighAccuracy:false`, 8 s timeout, 5-min `maximumAge`; rejects with a typed `{reason}` of `denied` vs `unavailable`); `sortByDistance(list,userLoc)` (stable nearest-first; coordless rows get `Infinity` distance and an index tiebreak so they sink to the bottom keeping their original order). **UI (`LocalView`):** one shared geolocation state (`nearMe` / `userLoc` / `geoStatus`) so a single permission prompt covers all three sub-tabs; a `handleNearMe` toggle that requests location on first tap (a user gesture, which iOS requires) and reuses the fix thereafter. A `nearMeControl` "📍 Cerca tuyo / Near you" toggle renders only on sub-tabs where `anyCoords(list)` is true (so the feature stays invisible until the sheet is backfilled). When active, that tab's list is run through `sortByDistance` and each located card shows a small Ocean DM Mono distance caption (`distCap` style) above its address via the `distanceCaption(row)` closure. Off-state ordering is byte-for-byte unchanged (Health keeps its facility/insurance treatment, Explore/Churches keep sheet order). Permission `denied` / `unavailable` shows an inline bilingual EB Garamond note and reverts to the default order. Geolocation is read on-device only for the sort and never transmitted; fully offline-safe (a failed fix just reverts to default order). Events sub-tab intentionally excluded (time-bound, low payoff). **Note:** geolocation requires a secure context — works on `localhost` and the production HTTPS URL, but a plain-HTTP LAN dev URL on a phone will report `unavailable`. Front-end + sheet-schema change; no new dependencies; no Apps Script cutover. Manual cutover: add `lat` / `lng` columns to the Explore, Health, and Churches tabs and fill coordinates (highest-value rows first) |
| 2026-06-01 | **Mundial event type on the calendar.** New `mundial` value for the Calendar tab's `type` column, for surfacing World Cup games in the schedule. Added a `mundial` entry to `EVENT_STYLES` (bg `#FFF8E1` championship gold, border `#F9A825` amber, icon `⚽`). Mundial events render with a ⚽ soccer ball prefixed to the title in both the Schedule tab's Weekly Overview day cards (where `visibility: week` games surface) and the Semester Calendar event list — the inline glyph is scoped to `e.type === "mundial"` so every other event type renders exactly as before (all other types are still distinguished by bg/border only, never an inline glyph). The new type auto-appears as a "Mundial" filter pill in the Calendar tab because the pill list is derived from `Object.keys(EVENT_STYLES)`. Unknown types already fell back to `EVENT_STYLES.academic`, so the live sheet wouldn't have broken before this — the change just gives the new type its own identity. No data-shape change (a new value within the existing `type` column, not a new column or tab); no `CACHE_VERSION` bump; no sheet schema migration (the Director just types `mundial` in the `type` cell); no Apps Script cutover; no new dependencies. Front-end-only change |
| 2026-05-21 | **Read-only-after-submit + post-cutoff visibility on prompts.** Closes the loop on the "I want my t-shirt size to stay visible after I submit, and I want to see my meal selection on the day of the dinner" use cases without extending the prompts schema with anything more invasive than two scalar columns. **Two new optional columns on the Prompts tab**: `lock_after_submit` (TRUE/FALSE, default FALSE) freezes a prompt once a student has submitted — no more edits, no second writes (the auth script returns `already_submitted` as a belt-and-suspenders check on top of the front-end's hidden submit affordance); `event_date` (YYYY-MM-DD, optional) marks the date a prompt should resurface on Today in read-only mode (the meal happens that day), even after `end_date` has passed. **Server (`AuthCode.gs`)**: renamed `promptIsActive` to `promptAcceptsSubmissions` (the submission gate, used by `handleSubmit` to reject writes outside the window) and added `promptIsVisible(prompt, today, hasResponses)` as the broader visibility gate — accepting submissions OR profile/both-surface-with-a-stored-response OR event_date-matches-today-with-a-stored-response. A third helper `promptIsReadonly(prompt, today, hasResponses)` collapses the two cases that wire up as read-only on the wire (past-window OR `lock_after_submit` AND prior responses). `buildPromptResponse` projects `event_date`, `lock_after_submit`, and a wire-only `readonly` boolean computed per request. `loadPromptsForUser` returns visible-but-not-active prompts with `readonly: true` instead of dropping them. `handleSubmit` walks the Responses tab on `lock_after_submit` prompts and rejects a second write with `{ error: "already_submitted" }`. `?action=admin_responses` projects the new raw fields too. `validatePrompts` flags malformed `event_date`, `event_date` before `start_date` (the prompt would never reach its event date in an open state), `event_date` before `end_date` (the submission cutoff would be after the event date — almost certainly a typo; kitchen needs the tally before service), and unrecognized `lock_after_submit` values. **Front (`App.jsx`)**: new helpers `hasAnyResponse(prompt)` (true when at least one field has a non-empty stored value); `isPromptReadonly(prompt)` (null-safe wrapper around the wire flag, so older cached prompt objects without the field still default to editable); `getPromptDisplayState(prompt, todayStr)` returning one of `editable | locked_readonly | inline_event | hidden` (single source of truth for how each surface should render the prompt); and `formatFieldValueForDisplay(field, rawValue)` (resolves select values back through `option_labels_es` / `option_labels_en` so a stored 'M' renders as its bilingual display label, and multi-select arrays render as comma-joined labels). **`<PromptFieldInput>`** gains a `readonly` prop: every field type renders as a static Ice-filled readout instead of an interactive control (`Sin respuesta / No answer` in italic stone for optional fields the student left blank). The required-field orange dot is suppressed in readonly mode. **`<PromptForm>`** reads `isPromptReadonly` on mount and switches into read-only mode automatically: a small Ice-filled banner with a `<LockGlyph>` glyph and DM Mono caption ("Cerrado · No editable / Closed · View only") sits above the description, every field receives `readonly={true}`, and the Pep Blue submit button is replaced with a Fog-bordered "Listo / Done" close button. New error mapping for `already_submitted` ("Ya enviaste este formulario. / You already submitted this form."). **`<PromptCard>`** on Today gains a `todayStr` prop and filters out `inline_event` and `hidden` prompts; locked rows render with Ice background + Ocean stripe + "Ver tu selección →" CTA + 12 px lock glyph beside the title. **`<PromptProfileSection>`** drops never-answered locked prompts (otherwise the gear modal nags about content with nothing to show) and renders saved-but-locked profile prompts with the same calmer Ice+Ocean treatment + lock glyph + "Ver →" CTA. **Inline-event display** is the new presentation on Today on `event_date`: locked meal prompts render inside the activity card as a small Ice-filled caption block ("Tu selección · {title}" with a 11 px lock glyph in the DM Mono caption row, comma-joined responses in 13 px Roboto beneath) appended below the regular event rows. The block is tappable and opens the read-only `<PromptForm>` for a fuller view. When the day is otherwise empty (`items.length === 0`) but an inline meal exists, the activity card renders a minimal "Agenda" card with just the meal block; "¡Día libre!" is suppressed. The class-cancelling-holiday suppression branch also yields to inline meals — a feriado with a planned program meal still shows the locked selection. **New `<LockGlyph>` SVG** (14×14 stroke-only padlock) sits next to `<AlertIcon>`. **No `CACHE_VERSION` bump**: content data shape unchanged, and the prompts payload's two new scalar fields degrade gracefully when absent. **Manual cutover**: (a) add `lock_after_submit` and `event_date` columns to the existing `Prompts` tab in the Roster spreadsheet (type the headers in the next two blank columns; existing rows stay blank and behave exactly as before because both default to inert), (b) re-deploy `AuthCode.gs` (Apps Script editor → Deploy → Manage deployments → pencil icon → New version), (c) for the t-shirt prompt: set `lock_after_submit: TRUE`, leave `event_date` blank — locks the moment a student submits and stays visible on Profile forever in read-only mode, (d) for a meal prompt: leave `lock_after_submit: FALSE` (editable until cutoff) and set `event_date` to the date of the meal — resurfaces inline on Today on that date showing the locked selection, (e) run `validatePrompts()` to confirm no warnings |
| 2026-05-17b | **Director response dashboard.** New strictly-staff-gated surface for reading every prompt's submissions across the cohort, closing the loop on the prompts/responses primitive that shipped 2026-05-09c. Reached via a "Ver respuestas / View responses" button inside `<ProfileModal>` that only renders when `isStaff(currentUser)` is true. Faculty are intentionally excluded — visiting professors who teach in BA don't need to read other students' RSVP / dietary / size submissions — so the gate is the strict `role === "staff"` check rather than the more permissive `isStaffOrFaculty` helper. The new `isStaff` helper sits next to `isStaffOrFaculty` as a separate single-source-of-truth for any staff-only surface added later. Tapping the button closes the gear modal and opens `<DirectorResponsesView>` — a full-screen overlay matching ProfileModal's visual identity. **Two internal screens**: (1) prompt list with response counts ("12 / 18"), per-prompt active/closed pill, and the prompt's window dates; sort puts active prompts first then closed, descending by end_date within each band so the freshest live data leads. (2) drill-in detail view with per-field sections — select fields render a horizontal tally bar (Ocean fill on Ice background) followed by individual responses with names and values; non-select fields skip the tally and just list responses. A "Falta responder / Awaiting response" section at the bottom of the detail view lists who's expected to respond but hasn't, pulled from `expected_cwids` minus the set of CWIDs with at least one stored value. A small "Fuera de la audiencia / Outside audience" section appears only when there are responders whose CWID isn't in the current expected audience (happens when the Director narrows audience after some students have already submitted — surfaces the data instead of quietly dropping it). Names render as "Preferred-or-first Last-initial." to match the rest of the app's first-names-only default. **New auth-script action** `?action=admin_responses` — `verifyUserIdentity` gate + strict staff-only role check (`role !== "staff"` returns `{ error: "forbidden" }`), returns `{ prompts: [...with fields/expected_cwids/responses/is_active...], roster: { cwid: { preferred_name, first_name, last_name, role } } }`. All prompts (active and expired) are returned so the Director can look at historical submissions; `expected_cwids` is computed server-side by walking the active roster against each prompt's audience expression so the front end doesn't reimplement `userMatchesAudience`. The Roster (program_status=active) is sent in full because the Director already has full Roster access via the spreadsheet; sending it makes name lookup O(1) on the client without duplicating the audience-matching logic. **New App.jsx machinery**: `class ForbiddenError extends Error` (so a stolen cohort token + non-staff CWID+birthday triple gets a clear inline message instead of bouncing through the gates); `fetchAdminResponses({ token, cwid, birthday })` async function next to `fetchPrompts`; `loadDirectorResponses` callback (sets `directorLoading`, swallows error into `directorError` with bilingual copy), `handleOpenDirector` (closes ProfileModal first so the dashboard doesn't stack under it) and `handleCloseDirector`. App-level state: `directorOpen` / `directorPayload` / `directorLoading` / `directorError`. **Helpers**: `isStaff` (strict staff-only gate, sits next to the existing `isStaffOrFaculty`), `sortDirectorPrompts` (active-first + recency), `groupResponsesByCwid` (response array → `{ cwid: { field_id: value } }`), `respondedCwidSet` (set of CWIDs that have at least one stored value), `tallySelectField` (counts per declared option preserving order + appends "other" values that don't match any declared option), `directorPromptTitle` / `Alt` / `Description`, `directorFieldLabel`, `directorResponderName`. No localStorage cache: the dashboard is opened a handful of times per week and staleness is more confusing than a 1-second loading state on each open. AuthError on any admin call clears cohort token + user + prompts cache in lockstep (matches the existing sheet-data AuthError handler — same root cause, same response) AND closes the dashboard so the next render goes to the cohort gate. NoMatchError closes the dashboard and bounces to the user gate. ForbiddenError keeps the dashboard open with an inline error. **Manual cutover after this commit**: re-deploy `AuthCode.gs` (Apps Script editor → Deploy → Manage deployments → pencil icon → New version) so the new `?action=admin_responses` endpoint is live. No data-shape change to the content side; no `CACHE_VERSION` bump; no sheet schema change |
| 2026-05-17 | **Two calendar views auto-anchor on today on open.** Semester Calendar (Calendar tab) scrolls to the "Hoy · Today" divider that shipped 2026-05-16b on mount, and again whenever the active filter pill changes — a filtered list has its own first-not-past event, so re-anchoring keeps the upcoming-events part of the list in view rather than leaving the student looking at past events from a different filter. Weekly Overview (Schedule tab) scrolls to today's day card on mount, and again whenever the student taps "← Back to This Week" (weekOffset returns to 0 from a neighboring week). Navigating to a different week via the chevrons does NOT pull the student back to today — the effect gates on `weekOffset === 0`, so a deliberate look-ahead or look-back stays put. **Implementation:** new `todayRef` (`<WeeklyOverviewView>`) and `todayDividerRef` (`<CalendarView>`) both threaded through the existing `useRef` import; `useEffect` deps are `weekOffset` and `firstNotPastKey` respectively, so the scroll re-fires on the specific state changes that should re-anchor and is a no-op on unrelated re-renders. Both effects wrap the `scrollIntoView` call in `requestAnimationFrame` (iOS Safari can otherwise miss the target when called the same paint as the mount, while the parent scroll container is still being sized) and return a `cancelAnimationFrame` cleanup. Both targets carry `scrollMarginTop: 12` so the anchor lands with a small breathing space below the section title rather than flush to the viewport top. `<CalendarView>`'s effect also gates on `firstNotPastKey` being truthy: a filter pill where everything is past (e.g. an old `orientation` filter mid-semester) leaves `firstNotPastKey` `null`, no divider renders, and the effect no-ops so the student lands at the natural top of the list rather than getting an awkward jump-to-bottom. No data-shape change; no `CACHE_VERSION` bump; no sheet edits required; no Apps Script cutover; no new dependencies |
| 2026-05-16b | **Semester Calendar gains a "Hoy · Today" anchor.** A thin BAP Blue horizontal rule with a centered DM Mono "Hoy · Today" pill renders before the first not-past event in the filtered, chronologically-sorted, month-grouped list, and every past event dims to opacity 0.55 (same treatment Today's activity card already uses for items earlier in the day). "Not-past" is defined as `(e.end_date || e.date) >= todayStr` so an ongoing multi-day event reads as current rather than past — and the divider lands before it rather than after the last fully-past event, which matters when today falls inside a multi-day run. **Implementation:** new `firstNotPastKey` `useMemo` in `<CalendarView>` keyed on `grouped + todayStr` walks the per-month arrays in order and returns the first matching ``${date}|${title}`` key; the per-event render compares its own key against that and renders the divider via a `Fragment` wrapper above the event row. When no events are upcoming under the active filter (everything past, or empty filter), `firstNotPastKey` is `null` and no divider renders. `Fragment` added to the React import. No data-shape change; no `CACHE_VERSION` bump; no sheet edits required; no new dependencies |
| 2026-05-16 | **Performance audit pass: three concrete wins.** (1) **Logo out of the JS bundle.** The 160×160 BAP header logo (previously inlined as a ~63KB base64 string in `App.jsx` as `LOGO_URI`) moved to `public/logo.png` and is now served as a static asset. The base64 blob was adding ~63KB to the JS the phone had to parse before first paint; pulling it out drops the bundled JS by the same amount (production build went from ~386KB raw to 323.54KB / 91.28KB gzipped) and lets the asset cache independently. The SW precache picks it up alongside the rest of the shell. `LOGO_URI` is still the constant referenced at the three header `<img>` sites; only its value changed from `data:image/png;base64,...` to `"/logo.png"`. (2) **Today tab clock-tick memoization.** The 1-minute `setInterval` in `<TodayView>` previously caused `getTodayItems` (`eventOverlaps` over every calendar event + holiday lookup + class day-of-week filter + finals merge + time-sort), `<TodayFinalsTile>` (`shouldShowFinalsUI` + `getStudentFinals` + `formatFinalsWindow`), `<EventsTodayTile>` (`getThisWeekEvents` = filter + chronological sort), and `computeWeatherAlert` (48-hour hourly scan) to all re-run on every tick — even though none of them produce different output until the calendar day rolls over or the underlying data changes. Each is now wrapped in `useMemo` keyed on the actual inputs (`data` / `profile` / `todayStr` or `data` / `profile` / `weather`). `<EventsTodayTile>` gained a `todayStr` prop from `<TodayView>` so its memo key matches the rest of the tile family. Also lifted the per-render `loadTodayCache()` call (a `JSON.parse` of the cached weather/dólar blob) into a lazy `useState` initializer so it runs once on mount rather than every tick. Hooks-rule check: `<TodayFinalsTile>`'s early returns now sit AFTER its three `useMemo` calls so hooks are called unconditionally on every render. (3) **Hot view filter/sort memoization.** `<WeeklyOverviewView>` previously ran 7 nested filters per render to build `eventsByDate` / `activeClassesByDate` / `finalsByDate` (each chevron tap re-did the full pipeline before any visual change); the whole pre-compute block is now memoized on `[data, profile, weekOffset, todayStr]`. `<CalendarView>`'s filter → sort → group-by-month pipeline memoized on `[data.calendarEvents, filter]`. `<EventsView>`'s upcoming → category-filter → split-by-week pipeline memoized on `[events, activeFilter, todayStr]`; its `loadTodayCache()` call (which was running on every filter-pill tap just to pluck the Blue compra rate for ARS→USD pricing) lifted into a lazy `useState` initializer. **Net effect:** the most-trafficked re-render paths (Today every minute, WeeklyOverview / Calendar / Events filter taps) collapse from full pipeline recompute per render to nearly free after the first paint, and the bundle shrinks by ~63KB. Added `useMemo` to the React imports. No data-shape change; no `CACHE_VERSION` bump; no sheet edits required; no new dependencies; no Apps Script cutover. Front-end-only change |

> Older entries (2026-05-15 and earlier, back to 2026-04-03) are archived in `BAP_App_Changelog_Archive.md` to keep this doc lean. Read that file directly when you need older history.
